# 判断ログ: 画面に出す文言の言語を決める

- 対象: `docs/spec/usecases/particular_case/画面に出す文言の言語を決める.rucm.md`
- 作成日 / 作成モデル: 2026-08-29 / Claude Opus 5 (1M context)
- 参照した根拠資料: `docs/plans/continuo_design.md`（3-35 / 3-35b / 3-35c）、`internal/i18n/i18n.go`、`internal/cli/cli.go`、`internal/config/validate.go`、`internal/config/types.go`、`internal/config/default.go`

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | 配置先ディレクトリ | `particular_case/` | 「画面に出す文言の言語を1つ決める」という単一目的である。他のユースケースを跨がない | `docs/plans/continuo_design.md`（3-35） | 95% |
| 2 | 独立した1本として立てるかどうか | 立てる | **言語の決定は、どのサブコマンドを打っても起動の直後に必ず走る。**`internal/cli/cli.go` の `RunWith` が引数を振り分けるより前に呼ぶ。既存の16本のどれかに入れると、ほかのコマンドでの決定が仕様から漏れる | `internal/cli/cli.go` の `RunWith` | 95% |
| 3 | USE CASE NAME | 画面に出す文言の言語を決める | 動詞で終わる名詞句にする規約に合わせた。設計 3-35 の見出し「画面に出す文言は資源から引く」の語をそのまま採り、この1本が決めるもの（言語）を主語にした | `docs/plans/continuo_design.md`（3-35） | 95% |
| 4 | BRIEF DESCRIPTION | 起動直後の環境変数からの決定・設定ファイルによる決め直し・どちらでも決まらないときの英語・訳の無いキーの落とし先の4点 | この4点で `internal/i18n` と `internal/cli` の言語決定の全経路が尽きる。ほかに言語を書き換える場所は無い（`i18n.Use` の呼び出し元はこの2箇所とテストだけである） | `internal/i18n/i18n.go` の `FromEnv` / `Resolve` / `Lookup`、`internal/cli/cli.go` の `RunWith` / `useLanguage` | 90% |
| 5 | PRECONDITION | 実行ファイルを実行できる。実行ファイルには言語ごとの資源が埋め込まれている | 資源は `//go:embed messages` で実行ファイルの中にある。外部ファイルを置く手順が無いので、事前条件は実行できることと埋め込みがあることの2つで足りる | `internal/i18n/i18n.go` の `messagesDir` の埋め込み宣言 | 90% |
| 6 | PRIMARY ACTOR | 利用者 | 決定を起こすのは利用者のコマンドの実行である。巡回タイマーからもエージェントからも起こらない。**既存16本の主アクター3種のどれにも当てはまらないので、起点を「利用者がコマンドを実行する」に置いた** | `internal/cli/cli.go` の `RunWith` | 90% |
| 7 | SECONDARY ACTORS | なし | システムが値を引く相手は、自分のプロセスの環境変数・自分に埋め込んだ資源・設定ファイルの3つだけである。外部のサービスもコマンドも呼ばない | `internal/i18n/i18n.go` の `FromEnv` / `init`、`internal/cli/cli.go` の `useLanguageFromConfig` | 90% |
| 8 | DEPENDENCY | なし | 他ユースケースを INCLUDE しない。設定ファイルを読む処理は `config.Load` の呼び出し1つであり、`設定ファイルを作る` とは呼び出し関係を持たない | `internal/cli/cli.go` の `useLanguageFromConfig` | 90% |
| 9 | GENERALIZATION | なし | 汎化関係にあるユースケースが存在しない | - | 90% |
| 10 | ステップ分割の方針 | 「読む」「決める」「検証する」「引く」「応答する」を1ステップずつに割る | R4（1文1動作）に従い、実装の分岐点がそのままテストの分岐点になるように割った。環境変数からの決定（3〜8）と設定からの決め直し（9〜18）を分けたのは、実装が別の関数（`FromEnv` と `Resolve`）で別のタイミングに呼ぶためである | `internal/i18n/i18n.go` の `FromEnv` / `Resolve`、`internal/cli/cli.go` の `RunWith` / `useLanguage` | 90% |
| 11 | 基本フローの範囲 | コマンドの実行の要求から、決めた言語の文言の応答まで | ハッピーパスは「資源が揃っていて、設定ファイルを読めて、`language` の値が受け付けられ、引いたキーに訳がある」ときである。ほかは全部代替フローへ出した | `internal/i18n/i18n.go` の `Resolve` / `Lookup` | 90% |
| 12 | 基本フロー 2 の VALIDATES THAT | 埋め込んだ資源に正の言語である日本語のファイルと既定の言語である英語のファイルがある | `init` が2つの条件で `panic` する。正の言語の資源が空のとき（`len(source) == 0`）と、既定の言語の資源が無いとき（`catalogs[DefaultLang] == nil`）である。**どちらも同じ「文言を1つも引けない」なので1つの条件にまとめた** | `internal/i18n/i18n.go` の `init` | 95% |
| 13 | 資源の検査を基本フローの1番目の検証に置いたこと | 引数の振り分けより前に置く | `init` は package の初期化なので、`RunWith` が動くより前に走る。**利用者がどのコマンドを打っても、この検査が最初に来る** | `internal/i18n/i18n.go` の `init` | 95% |
| 14 | 読む環境変数 | `LANG` だけ | **`LC_ALL` と `LC_MESSAGES` は読まない。**`FromEnv` が引くのは `EnvLangName`（`"LANG"`）の1つだけで、ほかの名前を引く経路は無い。設計 3-35 も「読む変数を増やすとどれが効いたのかを説明できなくなる」として1つに絞っている | `internal/i18n/i18n.go` の `FromEnv` / `EnvLangName`、`docs/plans/continuo_design.md`（3-35） | 100% |
| 15 | 基本フロー 4 の IF | `LANG` の値が資源のある言語を表すかどうかで分ける | `FromEnv` は `ParseLocale` の結果が空文字か `Supported` を通らなければ `DefaultLang` を返す。**どちらの枝も正常なので基本フロー内の IF にした** | `internal/i18n/i18n.go` の `FromEnv` / `ParseLocale` / `Supported` | 100% |
| 16 | `C` と `POSIX` と空の扱い | 資源のある言語を表さないものとして扱う | `ParseLocale` が `c` と `posix` と空文字を空の言語として返す。**「ロケールを使わない」の意味であって言語ではない**ためである。RUCM では 4 の条件が偽になる枝に入る | `internal/i18n/i18n.go` の `ParseLocale` | 100% |
| 17 | 基本フロー 9 の IF | 要求されたコマンドが設定ファイルの `language` を読むかどうかで分ける | 決め直す経路は6つである（`runMain` / `runDoctor` / `runAbandon` / `runTrust` / `runPrompt` / `runSetup`）。**「設定ファイルを読むか」ではなく「設定ファイルの `language` を読むか」と書いた。**設定を読むが言語を決め直さないコマンドと区別するためである | `internal/cli/cli.go` の `runMain` / `runDoctor` / `runAbandon` / `runTrust` / `runPrompt` / `runSetup` | 90% |
| 18 | 基本フロー 11 の VALIDATES THAT | 設定ファイルを読み取れる | `useLanguageFromConfig` は `config.Load` が失敗したら黙って戻り、環境変数から決めた言語のまま進む。`runTrust` は `config.Load` の失敗をその場で報告して終わる。**どちらも「設定の値で決め直さない」で一致する** | `internal/cli/cli.go` の `useLanguageFromConfig` / `runTrust` | 90% |
| 19 | 基本フロー 12 の VALIDATES THAT を 11 と分けたこと | 読み取れることと `language` の値が受け付けられることを別々に検証する | 実装では `config.Load` の中の `validateLanguage` が弾くので、失敗としては同じ経路を通る。**だが利用者に見える結果が違う。**読み取れないときは直し方が読めない理由ごとに変わり、`language` が不正なときは書ける値の一覧が出る。1つにまとめると、その違いをテストで確かめられなくなる | `internal/config/validate.go` の `validateLanguage`、`internal/config/types.go` の `Config` の `Language` | 85% |
| 20 | 受け付けられる `language` の値 | 空文字・`auto`・資源のある言語（`ja` / `en`） | `validateLanguage` がこの3つだけを通す。既定値は `auto` である | `internal/config/validate.go` の `validateLanguage`、`internal/config/default.go` の `language` の既定値 | 100% |
| 21 | 基本フロー 13 の IF | `language` の値が言語を名指しするかどうかで分ける | `Resolve` は値が空文字か `auto` なら `FromEnv` を呼び直す。**呼び直した結果は 4〜8 で決めたものと同じ**なので、その枝は「そのまま残す」と書いた | `internal/i18n/i18n.go` の `Resolve` | 95% |
| 22 | 空文字と `auto` を1つの枝にまとめたこと | まとめる | `Resolve` が `v == "" \|\| v == LangConfigAuto` の1つの条件で同じ処理へ入れている。**分けても振る舞いが1文字も変わらない** | `internal/i18n/i18n.go` の `Resolve` / `LangConfigAuto` | 100% |
| 23 | 基本フロー 20 の IF | 決めた言語の資源にキーの文言があるかどうかで分ける | `Lookup` が自分の言語の資源を先に引き、無ければ正の言語の資源を引く。**どちらの枝も正常な経路**なので基本フロー内の IF にした（訳が無いことは誤りではない。設計 3-35b） | `internal/i18n/i18n.go` の `Catalog.Lookup`、`docs/plans/continuo_design.md`（3-35b） | 95% |
| 24 | 基本フロー 23 の VALIDATES THAT | 正の言語である日本語の資源にキーの文言がある | `pattern` は `Lookup` がどちらの言語からも引けなかったときだけ `recordMissing` を呼ぶ。**この条件が偽になるのは実装の誤りである** | `internal/i18n/i18n.go` の `Catalog.pattern` / `recordMissing` | 95% |
| 25 | 基本フローの POSTCONDITION | 言語が1つ決まっていること・設定と環境変数のどちらが効いたか・応答が決めた言語の資源の文言であること | **「設定が主、環境変数が従」を事後条件で観測できる形にした。**どちらが効いたのかを書かないと、設定を無視する変更が入っても落ちない | `docs/plans/continuo_design.md`（3-35）、`internal/cli/cli.go` の `useLanguage` | 90% |
| 26 | 代替フロー 資源が欠けている | 欠けているファイルの名前を応答して ABORT | `init` の `panic` の文面がファイル名を含む（`messages/en.json` の形）。**終端を `ABORT` にしたのは、このユースケースがそこで途切れるからである。**コマンドの処理は1つも走らない | `internal/i18n/i18n.go` の `init` | 95% |
| 27 | 代替フロー 設定ファイルを読み取れない | 環境変数から決めた言語のまま残して RESUME STEP 19 | **言語は決まっているので続きがある。**`useLanguageFromConfig` は黙って戻り、`runDoctor` はそのまま検査を続けて結果を出す。復帰先を 19 にしたのは、決め直しの区画（9〜18）を抜けた最初の段だからである | `internal/cli/cli.go` の `useLanguageFromConfig` / `runDoctor` | 90% |
| 28 | 設定ファイルを読み取れないときに黙るかどうか | 言語の決定は黙る。読めないこと自体はコマンドが報告する | `useLanguageFromConfig` のコメントが「設定を読めなくても止めない。読めないこと自体は、それぞれのサブコマンドが自分の文言で報告する」と書いている。**事後条件にそのまま写した。****`runSetup` は共有の関数を使わず自分で `config.Load` を呼ぶが、読めなければ理由を1行出すので、この事後条件を同じように満たす** | `internal/cli/cli.go` の `useLanguageFromConfig` / `runSetup` | 95% |
| 29 | 代替フロー 対応していない言語の指定 | 対応していないことと書ける値の一覧を応答して ABORT | 設計 3-35 が「資源の無い言語を `language` に書いたら起動を止める」「黙って落とすと、書いたつもりの設定が効いていないことに無人運用では気づけない」と決めている。`validateLanguage` のエラーが書ける値の一覧を文面に持つ | `docs/plans/continuo_design.md`（3-35）、`internal/config/validate.go` の `validateLanguage` | 95% |
| 30 | 対応していない言語のときに何語で報告するか | 環境変数から決めた言語 | **`config.Load` が先に失敗するので、`useLanguage` も `i18n.Use` も呼ばれない。**起動直後に環境変数から決めた言語のまま報告される。`Resolve` は対応していない値に対して既定の言語とエラーを返すが、この経路には到達しない | `internal/cli/cli.go` の `useLanguageFromConfig` / `useLanguage`、`internal/i18n/i18n.go` の `Resolve` | 90% |
| 31 | 対応していない言語のときの POSTCONDITION に「常駐プロセスは起動しない」と書いたこと | 書く | `runMain` は `daemon.Run` に設定のパスを渡し、`daemon.Run` が設定を読めずに `ErrStartup` で戻る。**設計 3-35 の「起動を止める」は、この経路のことである** | `internal/cli/cli.go` の `runMain`、`docs/plans/continuo_design.md`（3-35） | 90% |
| 32 | 代替フロー 文言が登録されていない | 控えに記録し、文言が無いと分かる文字列を採って RESUME STEP 26 | `pattern` は `recordMissing` を呼んだあと「（文言が登録されていません: キー）」を返す。**生のキーは画面に出さない**（設計 3-35）。応答そのものは行われるので、終端は `RESUME STEP` である | `internal/i18n/i18n.go` の `Catalog.pattern` / `recordMissing` / `Missing`、`docs/plans/continuo_design.md`（3-35） | 95% |
| 33 | 任意時点代替フローを置かないこと | 置かない | **中断で失うものが無い。**言語の決定はメモリ上のポインタを1つ差し替えるだけで（`current` は `atomic.Pointer`）、ファイルも外部の状態も書き換えない。途中で終わっても半端な状態が残らないので、`BRANCH FROM` を置く先が無い | `internal/i18n/i18n.go` の `current` / `Use` | 85% |
| 34 | 資源の言語を並行して差し替える経路を書くかどうか | 書かない | `Use` と `T` は別の goroutine から呼ばれうるので `atomic.Pointer` で持つが、**言語を差し替えるのは起動時の2箇所だけである。**要求ごとに言語を変える経路は無い（`NewCatalog` の呼び出し元はテストだけである） | `internal/i18n/i18n.go` の `current` / `NewCatalog` | 90% |
| 35 | 用語の統一（R9） | 「画面に出す文言の言語」「資源」「正の言語である日本語」「既定の言語である英語」 | 設計 3-35 と `internal/i18n/i18n.go` のコメントが使っている語をそのまま採った。**「正の言語」と「既定の言語」を言い換えない。**この2つは別物であり、言い換えると同じものに見える | `docs/plans/continuo_design.md`（3-35b）、`internal/i18n/i18n.go` の `SourceLang` / `DefaultLang` | 95% |
| 36 | 環境変数とキーの書き方 | `LANG` と `language` はそのままの綴りで書く | 環境変数の名前と設定のキーの名前であり、訳すと指すものが変わる。mermaid のラベルにもそのまま書いた | `internal/i18n/i18n.go` の `EnvLangName`、`internal/config/types.go` の `Config` の `Language` | 95% |
| 37 | `messages/en.json` がいま日本語の複製であることを RUCM に書くか | 書かない | **資源の中身の話であって、言語を決める処理の分岐ではない。**英語を選んでも日本語が出るのは訳文が未完成だからであり、`Lookup` の経路は変わらない。この事実は `.rucm.md` の根拠資料と設計 3-35b に置いた | `docs/plans/continuo_design.md`（3-35b） | 90% |
| 38 | `_source_sha256` を RUCM に書くか | 書かない | `_` で始まるキーは文言として扱わず、`Lookup` でも引けない。**言語を決める処理にも文言を引く処理にも現れない**ので、ユースケースの分岐にならない | `internal/i18n/i18n.go` の `MetaKeyPrefix` / `Catalog.Meta` | 95% |
| 39 | `continuo trust` と `continuo setup` だけ決め直しの場所が違うことを RUCM の段に書くか | 書かない | `runTrust` と `runSetup` は自分で `config.Load` を呼んでから `useLanguage` を呼ぶが、**外から見た振る舞いは残りと同じである**（読めたら設定の言語、読めなければ環境変数の言語）。**止まるか続けるかは違うが、それは言語の決定の結果ではなく、そのコマンド自身の仕事の話である。**段に書くと、コマンドごとの分岐が仕様に増える。`.rucm.md` の表に残した | `internal/cli/cli.go` の `runTrust` / `runSetup` / `useLanguageFromConfig` | 85% |
| 40 | エラーとログの文言を含めるか | 含めない | いま資源へ移してあるのは画面に出す文言だけである（`continuo doctor` の検査結果・CLI の案内・ダッシュボードの HTML）。**エラーとログは移していない**ので、この1本の対象にしない | `docs/plans/continuo_design.md`（3-35）、`internal/i18n/i18n.go` の package コメント | 90% |
