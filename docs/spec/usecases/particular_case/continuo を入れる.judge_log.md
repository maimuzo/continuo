# 判断ログ: continuo を入れる

- 対象: `docs/spec/usecases/particular_case/continuo を入れる.rucm.md`
- 作成日 / 作成モデル: 2026-08-21 / Claude Opus 5
- 参照した根拠資料: `docs/plans/continuo_design.md`（3-36 / 3-32b）、`install.sh`、`.github/workflows/release.yml`、`README.ja.md`

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | USE CASE NAME | continuo を入れる | 動詞で終わる名詞句。「導入する」より、利用者が使う言葉に近い | `docs/plans/continuo_design.md` 3-36 | 90% |
| 2 | PRIMARY ACTOR | 利用者 | インストーラーを起動するのは人間である。continuo 自身は関与しない | `install.sh` の `main` | 95% |
| 3 | SECONDARY ACTORS | 配布サーバ、パッケージ管理 | 実行ファイルの取得先と、道具を入れる手段。どちらも外部にある | `install.sh` の `download` と `install_pkg` | 90% |
| 4 | PRECONDITION に curl / wget と tar を置いた | 手元に curl または wget があること、tar があること | `fetch` は curl も wget も無ければ `die` する。`tar` が無ければ展開できない | `install.sh` の `fetch` と `install_binary` | 95% |
| 5 | PRECONDITION に Go を入れなかった | 入れない | 配布された実行ファイルを取るので、利用者に Go は要らない。これがこの仕組みの目的である | `docs/plans/continuo_design.md` 3-36 | 95% |
| 6 | 段2 と段3 を分けた | OS と命令セットを別の VALIDATES THAT にした | 弾かれる理由が違い、応答する内容も違う（WSL2 の案内 / 対応する命令セットの一覧）。1つにまとめると、どちらで落ちたかが読めない | `install.sh` の `detect_platform` | 90% |
| 7 | 段4 を「配布された版が1つ以上ある」にした | VALIDATES THAT にした | タグを打つまで release は1つも無い。**その状態が普通に起こる**ので、異常ではなく分岐として扱う | `install.sh` の `resolve_version`、`docs/plans/continuo_design.md` 3-36 | 90% |
| 8 | 段7 を VALIDATES THAT にした | チェックサムの一覧を取得できたかを条件にした | 一覧が無いのは「照合できない」であり、「照合して外れた」とは別である。**残す状態も違う**（前者は続ける、後者は止める） | `install.sh` の `install_binary` | 85% |
| 9 | 一覧が無くても続けることにした | 続ける（注意を出す） | 止めると、checksums.txt を載せ忘れた release で誰も入れられなくなる。**取得元は同じサーバなので、照合の有無で守れる範囲は変わらない** | `install.sh` の `warn "checksums.txt がありません"` | 70% |
| 10 | 段9 で一致しなければ ABORT | 展開せずに止める | 中身が入れ替わっていたら、置いてはならない。**展開する前に止める**（展開すると一時ディレクトリに実行可能なものが残る） | `install.sh` の `die "チェックサムが合いません"` | 95% |
| 11 | 段13 を「端末から応答を受け取れる」にした | VALIDATES THAT にした | `curl … \| sh` では標準入力がパイプであり、`read` が使えない。**無人で流されたときに `sudo` を走らせないこと**が、この段の目的である | `install.sh` の `ask`、`docs/plans/continuo_design.md` 3-36 | 95% |
| 12 | 端末が無いときの復帰先を段17 にした | RESUME STEP 17 | 実行ファイルは既に置かれている（段11）。**PATH の案内と次の手順は、道具の有無と関係なく要る** | `install.sh` の `main` の並び | 90% |
| 13 | `herdr` と `claude` を入れない | 案内だけにした | どちらも独自の配布経路と認証を持つ。**勝手に入れると利用者の既存の設定を壊しうる** | `docs/plans/continuo_design.md` 3-36 | 90% |
| 14 | RUCM に `herdr` / `claude` の区別を書かなかった | 書かない | 段12 の「前提の道具の有無を調べる」に含まれる。**どの道具を自動で入れるかは設定であって、流れではない** | - | 65% |
| 15 | BRANCH FROM を段12 にした（依存を入れない指定） | 段12 | `--no-deps` が効くのは道具を調べる段からである。**それより前で分岐させると、実行ファイルを置く段を飛ばしてしまう** | `install.sh` の `SKIP_DEPS` の使われ方 | 85% |
| 16 | BRANCH FROM を段1 にした（案内 / 知らないオプション） | 段1 | 引数の解釈は、OS を見分けるより前に終わる。**配布サーバへ1度も繋がずに止まる**ことが、この2つの要点である | `install.sh` の引数を解釈する while | 90% |
| 17 | 「知らないオプション」を任意時点代替フローにした | GLOBAL にした | 特定代替フロー（RFS）にすると、参照先の段が条件ステップである必要がある。段1 は条件ステップではない | rucm-checker の E021 | 80% |
| 18 | POSTCONDITION に「一時ディレクトリは残っていない」を入れた | 入れた | `trap` で消している。**途中で失敗しても残さない**ことが、この仕組みの約束である | `install.sh` の `trap 'rm -rf "$tmp"' EXIT INT TERM` | 90% |
| 19 | 段15 を利用者のステップにした | 利用者はシステムに入れるかどうかを答える | 尋ねるのはシステム、答えるのは利用者。**1文1動作にすると主語が変わる**ので段を分けた | R1 / R4 | 90% |
| 20 | Windows の扱いを代替フローにした | 対応しないOS で ABORT | herdr の Windows 版が安定していないため、対象から外している。**インストーラーは黙って失敗せず、WSL2 を案内する** | `docs/plans/continuo_design.md` 3-32b | 95% |
| 21 | ステップの分割方針 | 外から見て結果が変わるところで切った | 内部の関数の区切り（`detect_platform` など）ではなく、**利用者に見える結果が変わるところ**で切った。テストが書ける粒度になる | - | 75% |
| 22 | `--dir` / `--version` を流れに入れなかった | 入れない | どちらも「同じ流れの入力が変わる」だけで、分岐を作らない。**代替フローにすると、テストパスが意味なく増える** | `install.sh` の引数の扱い | 75% |

## テストにしなかったテストパスと、その理由

| パス | 何の経路か | なぜ書かないか |
| --- | --- | --- |
| P001 | 端末があり、道具を入れて完走する | **擬似端末（pty）を用意しないと再現できない。**`ask` は `/dev/tty` から読むため、標準入力を差し替えても通らない |
| P006 | 照合を省き、端末があって道具を入れる | 同上。**照合を省く経路そのものは P008 と P007 で通っている** |

**残りの13本はテストにしてある**（`test/install/install_test.go`）。
**この2本を書くために `github.com/creack/pty` を足すことはしない。**外部依存を1つ増やす価値が、
「端末があるときの対話」を自動で確かめる価値を上回らないと判断した。**人間が1度手で確かめる。**

## 安全性のレビューを受けて変えたこと

**2026-08-21 のレビューで、実際に起こせる欠陥が5件見つかった。**RUCM を先に直し、そのあと
CFG を作り直し、テストを書き直した。

| 何が起きていたか | RUCM をどう変えたか |
| --- | --- |
| **取得先を環境変数で丸ごと差し替えられた。**偽の実行ファイルを置けることが実証された | **流れには書かない。**差し替えはテスト専用のフラグにし、製品の既定は固定にした |
| **`--version` に `../` を入れると別のリポジトリの release へ届いた**（本物の github.com で 200） | 任意時点代替フロー「使えない版の指定」を足した。**配布サーバへ1度も繋がずに止まる** |
| **checksums.txt に対象の行が無くても「照合しました」と表示していた** | 段7 の代替フローを「続ける」から「止まる」に変えた。**照合を省くには明示の指定を要る**ようにした |
