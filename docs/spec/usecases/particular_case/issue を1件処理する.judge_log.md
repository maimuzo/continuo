# 判断ログ: issue を1件処理する

- 対象: `docs/spec/usecases/particular_case/issue を1件処理する.rucm.md`
- 作成日 / 作成モデル: 2026-08-20 / Claude Opus 5 (1M context)
- 参照した根拠資料: `docs/plans/continuo_design.md`（3-2 / 3-3 / 3-3b / 3-5 / 3-6 / 3-8 / 3-15 / 3-16 / 3-16b / 3-18 / 3-21 / 3-23 / 3-25 / 3-29 / 3-34 / 4-1）、`internal/orchestrator/dispatch.go`、`internal/orchestrator/failure.go`、`internal/orchestrator/turn.go`、`internal/orchestrator/hookinput.go`、`internal/orchestrator/lifecycle.go`、`internal/orchestrator/reconcile.go`、`internal/orchestrator/comment.go`、`internal/orchestrator/signal.go`、`internal/workspace/prepare.go`、`internal/tracker/adapter.go`、`internal/tracker/query.go`

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | USE CASE NAME | issue を1件処理する | 依頼で指定された名前をそのまま使う。動詞で終わる名詞句の規則も満たす | 依頼文 | 100% |
| 2 | 配置ディレクトリ | `particular_case/` | 「1件の issue を dispatch して turn を回し、表明で Status を動かす」という単一目的の操作単位である。複数ユースケースを跨ぐ時系列ではない | rucm スキルの粒度ガイド | 95% |
| 3 | BRIEF DESCRIPTION | 巡回・候補の取得・着手・turn ループ・表明の反映の5文 | 設計の「1つの turn で何が起きるか」の図をそのまま5文へ落とした。R12（単文のみ）を守るために文を分けた | `docs/plans/continuo_design.md#3-5` | 90% |
| 4 | PRECONDITION | 常駐している。flock を取っている。選択肢名が設定と一致する。dispatch_state に issue が1件以上ある。herdr が待ち受けている | 起動時の検査を全部通ってからでないと巡回が始まらない。選択肢名が合わないと GraphQL がエラーを出さずに0件を返す | `docs/plans/continuo_design.md#3-6`、`#3-17` | 90% |
| 5 | PRIMARY ACTOR | 巡回タイマー | このユースケースを起こすのは人間ではなく、`poll_interval_ms` ごとに走る巡回である。**利用者を主アクターにすると、実在しない「人間が dispatch を要求する」経路を記述することになる** | `docs/plans/continuo_design.md#3-8`（巡回のループがやることは3つだけである）、`internal/orchestrator/orchestrator.go` の `Run` と `Tick` | 70% |
| 6 | SECONDARY ACTORS | GitHub Projects v2、herdr、Claude Code | ボードを読み書きし、pane を操作し、turn を送る相手がこの3つである。git は `workspace` の内部で使うだけなのでこのユースケースでは挙げない | `docs/plans/continuo_design.md#3-1` | 85% |
| 7 | DEPENDENCY | なし | 他の particular_case を取り込まない。INCLUDE を持つのは scenario 側である | rucm スキルのファイル規約 | 95% |
| 8 | GENERALIZATION | なし | 汎化関係にあるユースケースが無い | - | 90% |
| 9 | ステップ分割方針 | 着手の13段を1段1ステップに割り、turn ループを DO-UNTIL に収める | 設計が着手の順番を「段と段の間で落ちたとき外側に何が残るか」で決めている。**段をまとめると、落ちた位置ごとの事後条件が書けなくなる** | `docs/plans/continuo_design.md#3-16` | 90% |
| 10 | ステップ2 | システムはボードから active_states の issue の一覧を取る | 巡回の GraphQL リクエストの1本目である。**`In Progress` も active_states に含める**ので、実行中の issue も候補の配列に入る | `docs/plans/continuo_design.md#3-10`、`internal/orchestrator/orchestrator.go` の `Tick` | 95% |
| 11 | ステップ7（VALIDATES THAT） | 空きスロットが1つ以上ある | 着手の段-1 である。**印を付ける前に評価する**（印を付けてから弾くと印が残る） | `docs/plans/continuo_design.md#3-16`、`internal/orchestrator/dispatch.go` の `hasFreeSlot` | 95% |
| 12 | ステップ8（VALIDATES THAT） | 対象リポジトリが Claude Code に信頼登録されている | 着手の段0 である。**Status を書く段2 より前に置く**。書いてから飛ばすと `In Progress` が毎巡回で候補に上がり、30秒ごとにコメントが積まれる | `docs/plans/continuo_design.md#3-6`、`#3-16`、`internal/orchestrator/dispatch.go` の `preflight` | 95% |
| 13 | ステップ11 と 13 の順序 | 印を先に付け、ボードの Status をそのあとに書く | 印はメモリなので落ちると消える。Status は残るので、再起動後の識別に使える。**逆にすると、印の無い状態で Status だけが動く** | `docs/plans/continuo_design.md#3-16` | 100% |
| 14 | ステップ15 から18 の順序 | worktree → workspace → 設定ファイル → 身元ファイル | 身元ファイルには herdr の workspace の ID（段3 で確定）と設定ファイルのパス（段5 で確定）が要る。**先に書けない** | `docs/plans/continuo_design.md#3-16`、`#3-18`、`internal/orchestrator/dispatch.go` の `startRun` | 95% |
| 15 | ステップ19 と20 | pane を新しく作らず、`worktree.open` が作った pane を引いて label を書く | 1 worktree を1 herdr workspace にすると決めており、`pane.split` も `tab.create` も呼ばない。label は `owner/repo/issues/N` の形の表示名であり、復元の照合には使わない | `docs/plans/continuo_design.md#3-3`、`#4-5`、`internal/orchestrator/dispatch.go` の `resolvePane` | 95% |
| 16 | ステップ27（VALIDATES THAT） | agent_status が idle または done である | 着手の段10 である。**done も合格**（continuo は tab をフォーカスしないので実運用ではほぼ常に done 側になる） | `docs/plans/continuo_design.md#3-16`、`internal/orchestrator/dispatch.go` の `confirmStartup` | 95% |
| 17 | turn ループを DO-UNTIL にしたこと | ステップ30 から40 を DO-UNTIL で囲む | turn は「表明が working でなくなるまで」繰り返す。**IF で書くと繰り返しが表現できない** | `docs/plans/continuo_design.md#3-8`、`internal/orchestrator/turn.go` の `turnLoop` | 90% |
| 18 | ステップ30（VALIDATES THAT） | turn 数が max_dispatch_turns に達していない | 打ち切りの判定は turn ループの先頭で行う。**送る前に判定しないと、上限を1回超えて送る** | `internal/orchestrator/turn.go` の `turnLoop` | 95% |
| 19 | ステップ32 | turn の本文を送る（1回目と2回目以降を1ステップにまとめた） | 送る本文の違い（1回目の本文か継続の指示か）は「いまのセッションに会話履歴があるか」で決まる内部の分岐であり、外から見た相互作用は同じ1回の送信である | `docs/plans/continuo_design.md#3-8`、`internal/orchestrator/turn.go` の `buildTurnText` | 75% |
| 20 | ステップ34 と36 を分けたこと | 「Stop hook を受ける」と「settle_ms のあいだ待つ」を別ステップにした | R4（1文1動作）。**待ちは判定の一部であり、受信とは別の動作である** | `docs/plans/continuo_design.md#3-2` | 90% |
| 21 | ステップ37（VALIDATES THAT） | settle_ms のあいだに `task-notification` で始まる UserPromptSubmit が届かない | turn の終わりの分かれ目がここである。途中の Stop では 0.033〜0.037 秒後に届き、最終 Stop の後に来るのは `SubagentStop` である | `docs/plans/continuo_design.md#3-2`（実測8/8） | 95% |
| 22 | ステップ38 | transcript から表明の行を読む | `last_assistant_message` は使えない（印を書いた17件すべてで印が入っていなかった）。**transcript を `promptSource == "typed"` から遡って読む** | `docs/plans/continuo_design.md#3-25`、`internal/orchestrator/transcript.go` | 100% |
| 23 | ステップ41（UNTIL の条件） | 表明の値が working でない | `working` は「まだ続きがある」であり、次の turn を送る。`review` と `blocked` で run が終わる | `docs/plans/continuo_design.md#3-25` | 95% |
| 24 | ステップ42 | Status に表明の値の遷移先の選択肢を書く | Status を書くのは continuo の Go のコードである。エージェントは1行書くだけである。**書く前に必ず ID 指定で取り直す**（`UpdateStatus` が内部で行う） | `docs/plans/continuo_design.md#3-25`、`#4-1`、`internal/orchestrator/lifecycle.go` の `applySignals` | 100% |
| 25 | ステップ44（VALIDATES THAT） | issue に今回の run が書いたコメントがある | run が終わるときだけ確かめる（毎 turn ではない）。**marker が付いていて、かつ作成時刻が run の開始より新しいものだけを数える** | `docs/plans/continuo_design.md#3-25`、`internal/orchestrator/comment.go` の `hasRunComment` | 95% |
| 26 | ステップ45 から47 の順序 | after_run → pane を閉じる → 印を外す | `workspace_hooks.after_run` の cwd は worktree であり、run が終わったとき（worker を止める直前）に1回だけ走らせる | `docs/plans/continuo_design.md#3-9` の手順0、`internal/orchestrator/lifecycle.go` の `finishRunClaimed` | 95% |
| 27 | 基本フローに片付けを含めなかったこと | worktree と branch は残したまま run を終える | 片付けの契機は Status が `cleanup.on_states` に入ったときだけである。表明が `review` のときは `In Review` であり、片付けてはならない | `docs/plans/continuo_design.md#3-9` の手順1、`#4-1` | 100% |
| 28 | 基本フローの POSTCONDITION | Status が表明の遷移先。コメントがある。pane が閉じている。印が外れている。worktree と branch が残っている | 「引き渡し」の定義（worker は止めるが worktree は残す）をそのまま事後条件にした | `docs/plans/continuo_design.md#3-5` | 95% |
| 29 | フロー `空きスロット不足` | RFS BASIC FLOW 7。何も dispatch せず ABORT | 空きが0なら、その巡回では以降の候補を1件も dispatch しない | `docs/plans/continuo_design.md#3-16` の段-1 | 95% |
| 30 | フロー `未信頼のリポジトリ` | RFS BASIC FLOW 8。コメントを1件書いて ABORT | 信頼していないフォルダでは hook が1つも動かず turn 終了検知が全滅する。**コメントはそのリポジトリにつき1回だけ**である | `docs/plans/continuo_design.md#3-6`、`internal/orchestrator/dispatch.go` の `noteUntrusted` | 95% |
| 31 | フロー `起動直後の確認画面` | RFS BASIC FLOW 27。esc を送ってから failure_state へ落とす | `blocked` のまま turn を送ると保留中の権限要求が承認されて実行される（3/3 で再現）。**投げた本文のほうは消える** | `docs/plans/continuo_design.md#3-11`、`internal/orchestrator/dispatch.go` の `confirmStartup` と `sendEscape` | 100% |
| 32 | フロー `上限での打ち切り` | RFS BASIC FLOW 30。failure_state へ落として人間へ渡す | 仕様は打ち切りを正常終了として継続を予約するが、continuo は失敗として扱う。**無人で回すので、上限まで使って終わらなかった作業は人間に渡す** | `docs/plans/continuo_design.md#8-1`（打ち切りを失敗として扱う） | 95% |
| 33 | フロー `turnの継続` | RFS BASIC FLOW 37。RESUME STEP 34 で Stop hook を待ち直す | `task-notification` が届いたら turn は続いている。**turn 数は増やさない** | `docs/plans/continuo_design.md#3-2` | 95% |
| 34 | フロー `コメントの取り戻し` | RFS BASIC FLOW 44。pane を閉じてからセッションを復帰させ、書かせてから RESUME STEP 45 | 先に worker を止めないと同じセッション UUID が2つ生きる。一度使った UUID をもう一度渡すと起動に失敗する | `docs/plans/continuo_design.md#3-25` の9段、`#3-3`、`internal/orchestrator/comment.go` の `ensureAgentComment` | 95% |
| 35 | フロー `コメントの取り戻しの失敗` | RFS コメントの取り戻し 6。failure_state へ落として ABORT | 9段の最後が「それでも書かれなければ failure_state へ落として人間に渡す」である。**代替フローの中の条件ステップを RFS で指した**（`VALIDATES THAT` の偽ケースは RFS を持つ特定代替フローで書く決まりのため） | `docs/plans/continuo_design.md#3-25` の9段、rucm スキルの厳密規則9 | 90% |
| 36 | フロー `権限の確認` の種別と BRANCH FROM | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 32 | 権限の確認は turn を送ったどの時点でも起こりうるので任意時点代替フローにした。**分岐元はステップ32（turn の本文を送った直後）である。**ここが「保留中の権限要求が承認されて実行される」被害が最大になる時点であり、本文を送った直後に blocked が返る（2.7〜4.1 秒） | `docs/plans/continuo_design.md#3-11`（実測3/3）、`#3-2` | 80% |
| 37 | フロー `無音の打ち切り` の種別と BRANCH FROM | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 34 | 画面が止まる事象はどの時点でも起こりうる。**分岐元はステップ34（Stop hook を待っている時点）である。**ここは待ち時間がいちばん長く、エージェントの作業が最も進んだ状態で失われる | `docs/plans/continuo_design.md#3-21`、`internal/orchestrator/reconcile.go` の `checkStalls` | 80% |
| 38 | `無音の打ち切り` で worktree を残すこと | pane を閉じ、印とバックオフを残す | バックオフ中の issue を印から外すと30秒後の巡回で即座に拾い直され、バックオフが効かない | `docs/plans/continuo_design.md#3-25`（バックオフ中の issue も印に残す） | 95% |
| 39 | 代替フローの POSTCONDITION の書き方 | 「Status」「pane」「worktree」「印」の4つの状態を毎回書く | この4つが、次の巡回の振る舞いを決める全部である。**どれかを書き落とすと、次に何が起きるかがテストから決められない** | `docs/plans/continuo_design.md#3-16`（落ちた段ごとに外側へ残るもの） | 85% |
| 40 | 巡回の3本のリクエストを書かなかったこと | 候補の取得だけを書き、実行中の照合と worktree の照合は書かない | 実行中の照合と worktree の照合は「issue を1件処理する」の外側にある巡回の仕事である。**別のユースケース（`worktree と branch を片付ける`）が受け持つ** | `docs/plans/continuo_design.md#3-9` の手順7 | 80% |
| 41 | ステップ16 で worktree の絶対パスを渡すと明記したこと | `worktree.open` には path だけを渡す。branch は渡さない | herdr は path と branch の**片方だけ**を受け付け、両方来ると `invalid_request: exactly one of path or branch is required` で弾く。**実機で全ての着手が落ちた**（2026-08-20）。テスト用herdr mock が branch を無視していたので、本物で叩くまで気づけなかった | `herdr worktree open` の実測、internal/workspace/prepare.go | 100% |
| 42 | `無音の打ち切り` の WHEN と最初のステップ | WHEN に「`turn_timeout_ms` のあいだ hook が1件も届かず、画面の版も増えない場合」と書き、ステップ1 で `agent_status` と画面の版を一緒に要求する | **打ち切りの物差しは turn の総実行時間ではなく、画面が変わらないまま経った時間である。**閾値に達しただけでは打ち切らず、`agent.get` を1回呼んで画面の版が増えていないことを確かめてから止める。**版が増えている限り、1つの turn に何時間かかっても打ち切らない** | `docs/plans/continuo_design.md#3-21`、`internal/orchestrator/reconcile.go` の `checkStalls`、`internal/orchestrator/runstate.go` の `noteRevision` | 100% |
| 43 | 段13 を条件ステップにした理由 | pane が起動を受け付けるかを先に確かめる | **pane を作った直後は herdr が `agent_pane_busy`（`is not an available shell`）を返す。**シェルの起動が終わるまでコマンドを受け取れない。2026-08-21 に E2E で実測 | `internal/orchestrator/dispatch.go` の `startRun` | 95% |
| 44 | 段15 に `interactive_ready` を足した理由 | `agent_status` だけでは足りない | **`idle` になっても `interactive_ready` が偽の時間がある**（0.5〜2秒）。その間に指示を送ると herdr が `agent_not_ready` で弾く。herdr 自身が `agent start` の説明で入力を受け付けられることを成功の条件としている | `internal/orchestrator/dispatch.go` の `confirmStartup` | 95% |
| 45 | 「paneがまだ使えない」で 30 秒待つ根拠 | 30 秒 | **回数（3回 × 500 ミリ秒 = 1.5 秒）では足りず、実運用で着手できなかった。**シェルの起動はプロファイルの中身とマシンの速さで変わるので、回数ではなく時間で粘る | `internal/orchestrator/dispatch.go` の `agentStartBusyBudget` | 80% |
| 46 | 「起動の待ち直し」でやり直す理由 | 待つだけでなく agent.start をやり直す | **`agent.start` が受け付けられても Claude Code が1文字も起動しないことがある**（`agent.get` が `agent_not_found` を返す）。待つだけでは永久に通らない | `internal/orchestrator/dispatch.go` の `confirmStartupWithRestart` | 85% |
| 47 | 起動に失敗したとき人間へ渡さない理由 | agent.max_retries までバックオフして着手をやり直す | **1回の失敗で人間へ渡すと、待てば通ったはずの issue が毎回止まる**（2026-08-21 に実運用で発生）。回数を使い切ってから渡す | `internal/orchestrator/dispatch.go` の `ErrStartupRetryable` と `abandonRun` | 90% |
| 48 | ステップ9（VALIDATES THAT）を足した理由 | worktree の置き場所をそのまま使えるかを、Status を書く前に見る | **着手が確定して失敗する検査が Status を書いたあとにあると、`In Progress` と `Blocked` の往復が永久に続く。**`In Progress` は active_states なので次の巡回でまた候補に上がる | `docs/plans/continuo_design.md#3-16b`、`internal/workspace/prepare.go` の `CheckWorktreeUsable` | 100% |
| 49 | ステップ9 を読み取り専用にしたこと | `git worktree list` と `git rev-parse --abbrev-ref HEAD` を読むだけにする | **段0 は「まだ何も書かない」段である。**`prune` も `worktree add` も呼ばない。段3（`Prepare`）の同じ検査は保険として残す | `docs/plans/continuo_design.md#3-16`、`internal/workspace/prepare.go` | 95% |
| 50 | ステップ12（VALIDATES THAT）を足した理由 | 書く前に取り直した Status が terminal_states にも failure_state にも入っていないことを見る | **拒否リストが terminal_states だけだと、人間が `Blocked` に置いた issue を `In Progress` へ上書きできてしまう。**候補の一覧はサーバ側の検索結果であり、載っていること自体は着手してよい根拠にならない | `docs/plans/continuo_design.md#3-16b`、`#3-34`、`internal/orchestrator/dispatch.go` の `dispatchBlockedStates` | 100% |
| 51 | 書かなかったときに failure_state へ落とさないこと | 印を外して静かにやめる（`書かずに取りやめる`） | ボードは continuo が触る前のままなので、人間へ伝えるべきことが1つも無い。**ここで落とすと、人間が置いた `Blocked` を continuo が上書きしたことになる** | `internal/orchestrator/dispatch.go` の `ErrStatusNotWritten` | 95% |
| 52 | ステップ4（VALIDATES THAT）を足した理由 | 候補の Status が active_states に入っていることを自分で確かめる | **`items(query:)` の絞り込みはサーバ側の検索であり、直前に書いた値の反映が遅れる。**`Blocked` にした issue がそのまま返る | `docs/plans/continuo_design.md#3-34`、`internal/orchestrator/dispatch.go` の `dispatchCandidates` | 95% |
| 53 | 食い違った候補で巡回を止めないこと | その item だけ落として続ける。落とすのは1件でも、止めるのは大半が外れたときだけ | **1件の食い違いで一覧ごとエラーにすると、正しく絞り込めていた他の issue の dispatch まで止まる。**絞り込みのキーを解決できないときは条件ごと無視されてボードのほぼ全件が返るので、割合で見分けられる | `docs/plans/continuo_design.md#3-34`、`internal/tracker/adapter.go` の `dropUnrequestedStates` | 85% |
| 54 | ステップ5（VALIDATES THAT）を足した理由 | 同じ issue の失敗の回数をメモリ上に持ち、max_retries を超えたら拾わない | **印は run が終わると消えるので、印の中の `RetryCount` では止まらない。**次の巡回が0回目として拾い直し、同じ失敗を30秒ごとに繰り返す | `docs/plans/continuo_design.md#3-16b`、`internal/orchestrator/failure.go` | 90% |
| 55 | ステップ16 にリポジトリ本体を足したこと | 「worktree の絶対パスとリポジトリ本体の作業ディレクトリを渡して workspace として開き、その label に owner/repo/issues/N を書く」（label の側は判断55の対象ではなく、判断15 と同じ表示名の規則である） | **`cwd` は外せない。**省くと herdr が `worktree_not_found`、worktree のパスを渡すと `linked_worktree_source` で断る | 実測 2026-08-25（`test/live/herdr_test.go` の `TestLive_WorktreeOpen_cwdはリポジトリ本体しか受け付けない`） | 100% |
| 56 | 親 workspace を控える `workspace.list` を独立したステップにしなかったこと | ステップ16 の前後に入る読み取りとして、本文の節で説明した | **読むだけで、外から見える結果を1つも変えない。**控えた ID の行き先（身元ファイル）はステップ16 が既に受け持っている。ステップを足すと、以降40箇所の RFS 参照を振り直すことになり、振り間違いのほうが害が大きい | `internal/workspace/prepare.go` の `Prepare`、`internal/workspace/repoworkspace.go` | 80% |
| 57 | 控えるのを「先に親が無かったとき」に限ったこと | 先からあったなら控えない（空のままにする） | 先からあった親は**人間が自分で開いたもの**である。片付けで閉じると、その人の pane ごと消える | `docs/plans/continuo_design.md#3-9b`、issue #19 | 90% |
| 58 | ステップ10（VALIDATES THAT）を足した理由 | その branch を置き場所以外の worktree が使っていないかを、Status を書く前に見る | **目的のパスに何も無くても着手は落ちる。**git は1つの branch を2つの worktree に出せないので、別の場所の worktree がその branch を出していると `git worktree add` が `fatal: is already used by worktree at` で必ず失敗する。**ステップ9 は目的のパスしか見ないのでこの経路を拾えず、`In Progress` を書いてから落ちていた。**mock では出ず、実機で1件通して初めて分かった | `docs/plans/continuo_design.md#3-16b`、`internal/workspace/prepare.go` の `checkBranchFree` | 100% |
| 59 | ステップ10 で目的のパス自身を除外したこと | その branch を出しているのが目的のパスなら通す | **除外しないと、continuo が自分で作った worktree の再利用（ステップ12 の再利用の経路）が全部落ちる。**2回目以降の着手が1件も通らなくなる | `internal/workspace/prepare.go` の `checkBranchFree` | 100% |
| 60 | フロー `使われているbranch` の対処に `continuo abandon` を書いたこと | エラー文で `continuo abandon <issue の URL>` を案内する | **その issue の worktree と branch をまとめて片付けるのが `abandon` である。**検査は読み取りだけなので、実体を消すかどうかは人間が決める | `docs/spec/usecases/particular_case/着手を取り消す.rucm.md`、`internal/i18n/messages/ja.json` の `workspace.prepare.branch_in_use_elsewhere` | 95% |
| 61 | フロー `壊れたref` の種別と BRANCH FROM | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 15 | ステップ15 は条件ステップではないので特定代替フローにできない。**分岐元は worktree を作る唯一の時点である** | `docs/plans/continuo_design.md#3-22b`、issue #28、`internal/workspace/git.go` の `gitWorktreeAdd` | 90% |
| 62 | WHEN に「まだその ref のファイルを消していない」を入れたこと | やり直しを1回だけにした | 入れないと、同じ失敗で無限にやり直す経路ができる。**消したあとも失敗するなら、原因は ref ではない** | `internal/workspace/git.go` の `gitWorktreeAdd` | 95% |
| 63 | 壊れた ref を消す条件を7つにしたこと | 接頭辞・refname の正しさ・`show-ref` の失敗・`rev-parse` の失敗・**解決後の**置き場所・通常のファイルであること・中身が ref として読めないこと | どれか1つでも落とすと、**正常な branch・利用者の `main`・`.git` の外のファイル**を消す経路ができる。packed-refs は書き換えない | `docs/plans/continuo_design.md#3-22b`、`internal/workspace/brokenref.go` の `brokenBranchRef` | 95% |
| 64 | 別の branch 名へ逃げる案を採らなかったこと | 名前は変えず、壊れた ref のファイルを消す | 置き場所も branch 名も issue 番号から決まる。**名前を変えると片付け・復元・`continuo abandon` がその issue の worktree を引けなくなる** | `docs/plans/continuo_design.md#3-22`、`#3-22b` | 90% |
| 65 | フロー `消さないref` の事後条件 | Status は `failure_state` の選択肢になり、issue に理由のコメントが1件付く | ステップ13 で `running_state` を書いたあとの失敗なので、`startRun` の失敗として `failRun` が走る | `internal/orchestrator/dispatch.go` の `startRun`、`internal/orchestrator/lifecycle.go` の `failRun` | 90% |
| 66 | packed-refs 側が生き返ったことをログに残すこと | やり直しの前に branch の存在を確かめ、生き返っていたらその commit で worktree を作ると書く | 生き返った ref は **base ではなく packed-refs が指す（古いかもしれない）commit** を指す。何も書かないと、エージェントの作業が古い地点から再開した理由が誰にも分からない | 実測 2026-08-25（git 2.50.1）、`internal/workspace/git.go` の `gitWorktreeAdd` | 90% |
| 67 | ステップ12 の条件を許可リストにしたこと | 「ID 指定で取り直した Status が active_states に入っている」に変えた | **拒否リストではボードの Status を守り切れない。**`In Review` は active_states にも terminal_states にも failure_state にも入らず（3-9 / 3-10）、人間が自由に増やした Status は設定のどこにも名前が出てこない。**人間がレビューのために引き取った issue を `In Progress` へ上書きし、その worktree で Claude Code を起動し直していた** | `docs/plans/continuo_design.md#3-38`、`internal/orchestrator/dispatch.go` の `dispatchStatusAllowed` | 100% |
| 68 | フロー `送信の失敗` の種別と BRANCH FROM | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 32 | ステップ32 は条件ステップではないので特定代替フローにできない。**分岐元は turn を送る唯一の時点であり、そこで断られると本文は1文字も届いていない。**この分岐を持たないと、届いていない turn を「Stop hook が来なかった」と報告することになる | `docs/plans/continuo_design.md#3-40`、`internal/orchestrator/turn.go` の `turnSendFailed` | 95% |
| 69 | `送信の失敗` を `無音の打ち切り` と別のフローにしたこと | 別フローにし、事後条件に「turn の本文は Claude Code に届いていない」を明記した | 後始末（pane を閉じ、リトライを積み、バックオフの期限を書く）は同じでも、**人間に伝える理由がまったく違う。**片方は設定ファイルを、もう片方は herdr の生死を確かめさせる | `docs/plans/continuo_design.md#3-40` | 95% |
| 70 | フロー `コメントの取り戻し` にステップ3 を足したこと | 「worktree の絶対パスとリポジトリ本体の作業ディレクトリを渡して workspace として開き直し、その中の pane を pane.list で引く」 | **`cwd` を渡さない `worktree.open` は本物の herdr が断る**（判断55 と同じ実測）。開き直す動作を書かないと、着手のステップ16 と同じ制約を持つことが記述から読み取れない | `docs/plans/continuo_design.md#3-41`、`internal/orchestrator/comment.go` | 90% |
| 71 | 打ち切りのときに引き渡しの通知を先に投稿すること | Status を落とす → 理由を投稿する → コメントの取り戻し、の順にした（本文の節で説明。ステップは足していない） | **引き渡しの通知は1つの run につき1件しか投稿しない。**コメントの取り戻しの失敗が先に枠を使うと、打ち切った本当の理由が issue に1文字も残らない。`failRun` は元からこの順である | `docs/plans/continuo_design.md#3-42`、`internal/orchestrator/lifecycle.go` の `abandonRunClaimed` | 95% |
| 72 | フロー `一時的な送信の失敗` を足したこと | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 32。WHEN は「herdr の呼び出しが一時的な理由で失敗した場合」 | **herdr の再起動・socket の一瞬の不通・応答の遅れは、run を捨てる理由にならない。**`送信の失敗` と同じ扱いにすると、herdr を再起動しただけで走行中の run がリトライを消費し、使い切ると issue が failure_state へ落ちる | `docs/plans/continuo_design.md#3-48`、`internal/orchestrator/turn.go` の `turnTransient` | 95% |
| 73 | `一時的な送信の失敗` を `送信の失敗` と別のフローにしたこと | 別フローにし、事後条件を「印は残っている。リトライの回数は増えていない。herdr の pane は閉じていない」にした | **後始末が正反対である。**`送信の失敗` は pane を閉じてリトライを積むが、こちらは何も閉じず何も積まない。**1つのフローにまとめると、どちらの事後条件も書けない**（BOUNDED ALTERNATIVE FLOW は同じ事後条件で終わるものだけをまとめる） | `internal/orchestrator/turn.go` の `turnLoop` の `case turnTransient` | 95% |
| 74 | ステップ2 に「本文を送り直さない」を置いたこと | `agent.prompt` を再送しないことを1ステップとして明記した | **届いていたかどうかは分からない。**届いていた場合に送り直すと turn が二重に投入される。書かないと、実装が再送しても RUCM と食い違わないことになる | `internal/orchestrator/turn.go` の `sendTurn`（`herdr.IsTransient` の分岐） | 90% |
| 75 | `一時的な送信の失敗` を ABORT で終えたこと | RESUME STEP ではなく ABORT | turn ループはここで抜ける。**次の巡回が「turn を送らずに turn の終わりを待つ」ところから入り直す**ので、同じ turn の続きではない。RESUME STEP にすると、抜けずにその場で待ち続ける経路に読める | `internal/orchestrator/turn.go` の `rs.setAwaitTurnEnd()`、`docs/plans/continuo_design.md#3-4` の段5a2 | 85% |
| 76 | 枠待ちの待ち直しが一時的に失敗したときを別フローにしなかったこと | `afterWaitTimeout` の一時的な失敗も `一時的な送信の失敗` に含める | **事後条件が同じである**（印は残り、リトライは増えず、pane も閉じない）。枠待ちの印を外さない点も含めて、turn を送れなかったときと区別する理由が無い | `internal/orchestrator/turn.go` の `afterWaitTimeout` | 80% |
| 77 | フロー `走行中のissue` を足したこと | SPECIFIC ALTERNATIVE FLOW。RFS BASIC FLOW 3。dispatch の対象から外して ABORT | 巡回のループの先頭で `lookupRunByID` を引き、**既に印を持つ issue はそのまま飛ばす**（`if _, taken := o.lookupRunByID(issue.ID); taken { continue }`）。`空きスロット不足` とは別物である。あちらは印の総数を数える検査で、こちらは**この issue に印が付いているか**の検査である | `internal/orchestrator/dispatch.go` の `dispatchCandidates` | 95% |
| 78 | フロー `ラベルの不足` を足したこと | SPECIFIC ALTERNATIVE FLOW。RFS BASIC FLOW 6。dispatch の対象から外して ABORT | `hasRequiredLabels(issue, o.cfg.Tracker.RequiredLabels)` が偽なら `continue` する。**ボードへは1バイトも書かない**（Status を書く段2 より前である） | `internal/orchestrator/dispatch.go` の `dispatchCandidates`、`hasRequiredLabels` | 95% |
| 79 | ステップ6 を空きスロットの検査より前に置いたこと | 失敗の繰り返しの次、空きスロットの前 | 実装も `skipByFailure` → 信頼の検査 → `hasRequiredLabels` → `hasFreeSlot` の順である。**印を付ける前に落とす検査は、印を数える検査より前に置く**（順番を入れ替えると、着手しない issue のために枠を数えることになる） | `internal/orchestrator/dispatch.go` の `dispatchCandidates` | 85% |
| 80 | フロー `turnループの重なり` を足したこと | SPECIFIC ALTERNATIVE FLOW。RFS BASIC FLOW 28。「次の巡回で turn を送り直す印を立てる」だけで ABORT | `startTurnLoop` は `rs.turnLoopRunning || rs.Finished` なら偽を返し、呼び出し側が `rs.setNeedsPrompt()` を立てる。**黙って捨てると、新しい Claude Code を起動したのに turn ループが1本も立たない** | `internal/orchestrator/turn.go` の `startTurnLoop`、`internal/orchestrator/dispatch.go` の `startRun` の段11 | 95% |
| 81 | フロー `本文の組み立ての失敗` を足したこと | SPECIFIC ALTERNATIVE FLOW。RFS BASIC FLOW 31。`failure_state` へ落とし、テンプレートの直し方を1件コメントして ABORT | `buildTurnText` が返すエラーは `missingkey=error` の描画の失敗である。`turnLoop` は `o.failRun(...)` を呼ぶので、**Status は落ち、印も外れる**（`abandonRun` ではないのでリトライは積まない） | `internal/orchestrator/turn.go` の `turnLoop`、`buildTurnText`、`internal/orchestrator/lifecycle.go` の `failRun` | 95% |
| 82 | フロー `turnの終わりの取りこぼし` を足したこと | SPECIFIC ALTERNATIVE FLOW。RFS BASIC FLOW 33。after_run → pane を閉じる → リトライを1つ積む | `confirmTurnEnd(ctx, rs, true)` は `firstWait` のあいだに空の Stop が来なければ `turnStalled` を返し、`turnLoop` が `abandonRun` する。**`無音の打ち切り` とは測るものが違う。**あちらは画面の版が `turn_timeout_ms` のあいだ増えないことを巡回が見るが、こちらは**待ち受けが返った直後の settle_ms** だけを見る | `internal/orchestrator/turn.go` の `confirmTurnEnd`、`turnLoop` の `case turnStalled` | 90% |
| 83 | フロー `リトライの尽き` を足したこと | SPECIFIC ALTERNATIVE FLOW。RFS turnの終わりの取りこぼし 1 | `abandonRunClaimed` は `snap.RetryCount >= o.cfg.Agent.MaxRetries` のとき `failure_state` を書き、理由を投稿し、コメントを確かめてから印を外す。**`失敗の繰り返し` とは数えている器が違う。**あちらは issue ごとの `o.failures`（印の外）で、こちらは run ごとの `RetryCount`（印の中）である。`送信の失敗` と `一時的な送信の失敗` は「リトライを積むかどうか」の分かれ目であって、尽きたときの後始末ではない | `internal/orchestrator/lifecycle.go` の `abandonRunClaimed`、`internal/orchestrator/failure.go` の `skipByFailure` | 90% |
| 84 | `リトライの尽き` を `turnの終わりの取りこぼし` にぶら下げたこと | RFS 無音の打ち切り ではなく RFS turnの終わりの取りこぼし にした | **リトライを積む出口は4つあるが、尽きたときの後始末は `abandonRunClaimed` の1本しかない。**4箇所に同じ枝を書くと、後始末を直すたびに4箇所を直すことになる。**1箇所にだけ書き、残りはそこへ落ちる同じ枝として本文の節で名指しする** | `internal/orchestrator/lifecycle.go` の `abandonRunClaimed`（`turnStalled` / `turnSendFailed` / `checkStalls` / `handleTurnEnd` のすべてが通る） | 75% |
| 85 | フロー `騙りのhook` を足したこと | SPECIFIC ALTERNATIVE FLOW。RFS BASIC FLOW 35。hook を捨てて RESUME STEP 34 で待ち直す | run を引くのは `session_id` だけであり、それはプロセスの引数に載る。`acceptHookCwd` が偽なら `sanitizeHookEvent` が偽を返し、**その hook ごと捨てる。**`cwd` が空の hook と worktree のパスを知らない run は判定できないので通す | `internal/orchestrator/hookinput.go` の `sanitizeHookEvent`、`acceptHookCwd`、`internal/orchestrator/orchestrator.go` の `OnHook` | 95% |
| 86 | フロー `ボードから消えたissue` を足したこと | SPECIFIC ALTERNATIVE FLOW。RFS BASIC FLOW 40。after_run → pane を閉じる → リトライを1つ積む | `refreshIssue` は `len(issues) == 0` のとき偽を返し、`handleTurnEnd` が `abandonRun` する。**印は外さない**（`abandonRunClaimed` はリトライが残っていれば `rs.endTerminal()` で戻す）。issue はボードから見えないので、Status を書きにいく先がない | `internal/orchestrator/lifecycle.go` の `handleTurnEnd`、`refreshIssue`、`abandonRunClaimed` | 90% |
| 87 | フロー `復元の断念` を足したこと | SPECIFIC ALTERNATIVE FLOW。RFS コメントの取り戻し 2。理由を記録して RESUME STEP 8（＝基本フローの片付けへ戻る） | `ensureAgentComment` は身元ファイルを読めない・セッション UUID か設定ファイルのパスが空・worktree の実体が無い、のいずれでも**そのまま return する。**`failCommentRecovery` は呼ばない。**`起動の断念` とは別物である。**あちらは着手の段10（`confirmStartupWithRestart`）で、こちらは復元の材料が足りずに復元そのものをやめる経路である | `internal/orchestrator/comment.go` の `ensureAgentComment`、`failCommentRecovery` | 90% |
| 88 | `コメントの取り戻し` のステップ2 を VALIDATES THAT にしたこと | 「セッション UUID と設定ファイルのパスを読む」→「読める」 | **`復元の断念` をぶら下げるには条件ステップが要る。**GLOBAL ALTERNATIVE FLOW にすると `rucm_validator.py` が W005（glo の RESUME STEP の復帰先は基本フロー固定）を出し、このリポジトリで唯一の警告が残る。**真の側の並びは1つも変えていない** | `maimuzo-rucm 0.6.1` の `rucm_validator.py`、`internal/orchestrator/comment.go` の `ensureAgentComment` | 90% |
| 89 | フロー `着手の途中の失敗` を足したこと | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 15。`failure_state` へ落とし、失敗した段と直し方を1件コメントして ABORT | `startRun` は段3 から段9 のどこでもエラーを返し、`runStartOrFail` が `ErrStatusNotWritten` でも `ErrStartupRetryable` でもなければ `failRun` を呼ぶ。**分岐元は worktree を作る時点1つに絞る。**段ごとに分岐元を並べると経路が掛け算になる | `internal/orchestrator/dispatch.go` の `startRun`、`runStartOrFail`、`internal/orchestrator/lifecycle.go` の `failRun` | 85% |
| 90 | `着手の途中の失敗` の WHEN から2つを除いたこと | 「壊れた ref でも pane の受け付け待ちでもない理由で失敗した場合」と書いた | 除かないと `壊れたref` と `paneの断念` が同じ引き金を主張する。**1つの出来事に2つの分岐が当たると、どちらへ進むかが記述から決まらない** | `internal/workspace/git.go` の `gitWorktreeAdd`、`internal/orchestrator/dispatch.go` の `AgentStartWithRetry` の呼び出し | 80% |
| 91 | ステップ33 を足してステップ34 を残したこと | 「settle_ms のあいだに Stop hook が届く」を検査してから「Stop hook を受ける」 | **待ち受けが返ったあとに Stop が来なかったことだけが「届かなかった」と言ってよい場所である**（`turnSendFailed` と混ぜてはならない）。既存のステップ「Stop hook を受ける」は1文字も変えず、その手前に検査を1つ足した | `internal/orchestrator/turn.go` の `confirmTurnEnd` の `firstWait`、`sendTurn` | 90% |
| 92 | `無音の打ち切り` の分岐元 | BRANCH FROM BASIC FLOW 34（「Stop hook を受ける」の段） | 「Stop hook を受ける」より手前に検査を足したので段の番号は動くが、**指している時点は「turn の本文を送ったあと、Stop hook を待っている時点」で変わらない** | 判断37 と同じ | 100% |
| 93 | 候補を飛ばす検査を6つに増やしたこと | 本文の節の題を「候補を飛ばす4つの検査」から「6つの検査」へ | `走行中のissue` と `ラベルの不足` を足したぶん、飛ばす検査が2つ増えた | 判断77・78 と同じ | 100% |
| 94 | フロー `既に同じStatus` の種別と BRANCH FROM | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 42 | ステップ42 は条件ステップではないので特定代替フローにできない（`RFS` の参照先は `VALIDATES THAT` のステップでなければならない）。**issue #48 は `SPECIFIC ALTERNATIVE FLOW` + `BRANCH FROM STEP 30` と書いていたが、この組み合わせは文法で通らない。**`BRANCH FROM` と `WHEN` はグローバル代替フロー専用であり、`BRANCH FROM` にはフロー識別子が要る。**分岐元は表明の遷移先を書く唯一の時点である** | issue #48、`rucm_validator.py` の E007 / E018、`internal/tracker/adapter.go` の `UpdateStatus` | 95% |
| 95 | `既に同じStatus` を ABORT ではなく RESUME STEP で終えたこと | RESUME STEP 44 | **目的（issue の Status を表明の遷移先にすること）は果たされている。**書き込みを省いただけなので、コメントの確認から先の手順はそのまま動く。着手や失敗の記録は「書き込みの API を呼んだか」ではなく「目的の Status になっているか」で決める。ABORT にすると、雛形の `active_states`（`["Ready", "In Progress"]`）が `running_state` を含むため、**既に `In Progress` だった issue に着手できなくなる** | issue #48、`internal/scaffold/template.go`、`internal/orchestrator/dispatch.go` の `startRun` | 95% |
| 96 | 同じ値でも書き込みを送る案を採らなかったこと | 取り直した値が遷移先と同じなら mutation を送らない | GitHub は同じ値の書き込みを timeline に残さない。**送ると「Status を書き込みました」とログに出るのに issue には何も現れない**ので、ログを見た人間が「書いたのに反映されていない」と読む | issue #48、`internal/tracker/adapter.go` の `UpdateStatus` | 90% |
| 97 | 同じ値でも書きに行くかを設定で選べるようにしなかったこと | 設定キーを足さない | **同じ値を書きに行きたい場面が1つも無い。**GitHub 側では遷移が起きないので timeline に記録が増えることもなく、増えるのは API の呼び出し回数とログの嘘だけである | issue #48 | 95% |
| 98 | ステップ14 と43（Status を動かした記録）を足したこと | 「システムは Status を動かした記録を issue にコメントする。」を、Status を書く段の直後にそれぞれ置いた | **ボードの Status が動いた事実は、いま issue のどこにも残っていない。**ログには「Status を書き込みました」と出るが、issue を読む人間には見えない。**誰がいつ何を根拠に動かしたのかを追えるようにする** | issue #39、issue #33、`docs/plans/continuo_design.md#3-29`、`internal/orchestrator/comment.go` の `postStatusMove` | 95% |
| 99 | 記録を書く段を「Status を書く段の直後」に置いたこと | ステップ13 の直後（ステップ14）と、ステップ42 の直後（ステップ43） | **書き込みが起きたことを知っているのは、書いた直後だけである。**離すと、書けたかどうかを持ち回る必要が出る。**書けなかったときに記録を出さない条件も、その場でしか判定できない** | `internal/tracker/adapter.go` の `UpdateStatus` の戻り値（`StatusWrite`） | 90% |
| 100 | `既に同じStatus` の RESUME 先をステップ44 にしたこと | 書き込みを省いた経路では、記録のコメントも書かない | **ボードが動いていないので、書くことが無い。**RESUME STEP 43 にすると「動かしていないのに動かしたと書く」経路ができる。判定に使うのは `StatusWrite.Wrote` である | issue #39、issue #48、`internal/orchestrator/comment.go` の `postStatusMove` | 95% |
| 101 | 「何から」を `UpdateStatus` の戻り値から取ることにしたこと | 呼び出し側が持っている issue の写しは使わない | **写しは巡回で読んだ時点の値であり、書く直前に取り直した値とは限らない。**古い値を書くと、この記録そのものが嘘をつく | issue #39、`internal/tracker/adapter.go` の `UpdateStatus`（`StatusWrite.Previous`） | 100% |
| 102 | 起動のステップを IF / ELSE に分けたこと | ステップ22 で「着手の前に読んだ身元ファイルに前回のセッション UUID が入っているか」を見て、真ならステップ23（`--resume`）、偽ならステップ25（`--session-id`）で起動する | **1つのアクションステップのままだと、どちらのフラグで起動したかがテストパスに現れない。**新規の着手と再着手は起動フラグが違うだけで後続の手順が同じなので、条件を書かないと2つの振る舞いが1本の経路に潰れる | `internal/orchestrator/dispatch.go` の `startRun`（段5b の `resumeUUID`）、`internal/orchestrator/settings.go` の `claudeStartArgs` | 95% |
| 103 | 一度使った UUID を `--session-id` に渡さないこと | 復帰する相手があるときは `--resume`、無いときだけ新しく採番して `--session-id` | **同じ UUID をもう一度 `--session-id` に渡すと `Session ID ... is already in use.` で1文字も起動しない。**herdr 経由では `timeout` としか返らないので、起動フラグの選び分けを仕様に書かないと原因を追えない | `docs/plans/continuo_design.md#3-3`、`#3-3b`、`internal/orchestrator/settings.go` の `claudeStartArgs` | 100% |
| 104 | フロー `復帰の失敗` の種別と BRANCH FROM | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 23 | ステップ23 は条件ステップではないので特定代替フローにできない（`RFS` の参照先は `VALIDATES THAT` のステップでなければならない）。**分岐元は `--resume` つきで起動する唯一の時点である** | `internal/orchestrator/dispatch.go` の `startRun`（`startErr != nil && resumeUUID != ""` の分岐）、`internal/orchestrator/dispatch.go` の `restartWithNewSession` | 90% |
| 105 | `復帰の失敗` を ABORT ではなく RESUME STEP 27 で終えたこと | 同じ pane で新しいセッションを立て直し、起動の確認から本線へ戻る | **利用者が `~/.claude/projects/` を消しただけで issue が `failure_state` へ落ちるのは行き過ぎである。**`claude --resume <無い UUID>` は終了コード 1 で終わり、herdr 経由でも pane はシェルのプロンプトへ戻るので、**同じ pane でそのまま起動し直せる**（実測 2026-08-26、Claude Code 2.1.246） | `docs/plans/continuo_design.md#3-3b`、`internal/orchestrator/dispatch.go` の `startRun`（`restartWithNewSession` のあとの2度目の `launchClaude`） | 95% |
| 106 | `復帰の失敗` に身元ファイルの書き直しをステップとして置いたこと | ステップ4 で `session_uuid` を新しい UUID へ書き直す | **書き直さないと、次の再着手も同じ死んだ UUID へ復帰しにいき、毎回 `herdr.startup_timeout_ms` を捨てる。**採り直しとログだけでは足りない | `internal/orchestrator/dispatch.go` の `restartWithNewSession`（`o.ws.SetSessionUUID`）、`internal/workspace` の `SetSessionUUID` | 100% |
| 107 | `復帰の失敗` にトークンの集計の基準の作り直しを置いたこと | ステップ3 に置いた | **transcript のファイル名はセッション UUID である。**復帰したままなら同じファイルなので基準を作り直してはならず、**セッションを変えたここで初めて別のファイルになる** | `docs/plans/continuo_design.md#3-15`、`#3-3b`、`internal/orchestrator/dispatch.go` の `restartWithNewSession`（`rs.foldTokensBase`） | 95% |
| 108 | `着手の途中の失敗` の WHEN に「前回のセッションの不在」を除いたこと | 「壊れた ref でも pane の受け付け待ちでも前回のセッションの不在でもない理由で失敗した場合」に書き換えた | **`復帰の失敗` は「着手の途中で herdr の呼び出しが失敗した場合」にそのまま当てはまる。**除かないと同じ事象に2本の枝が重なり、どちらへ進むかが仕様から決まらない | `internal/orchestrator/dispatch.go` の `startRun`（復帰の失敗を先に見てから `startErr` を返す並び） | 90% |
| 109 | WHEN を「前回のセッションが見つからない」に絞ったこと | 起動の失敗すべてではなく、実測した1つの原因だけを書いた | 実装は復帰つきの `launchClaude` が返すエラーの種類を見ずに始め直すが、**実測で確かめたのは `No conversation found with session ID` の1件だけである。**広く書くと `起動直後の確認画面` と `起動の待ち直し` の WHEN に重なる | `internal/orchestrator/dispatch.go` の `startRun`、`internal/orchestrator/dispatch.go` の `confirmStartupWithRestart` | 70% |
| 110 | 復帰しても送る本文を仕様に書かなかったこと | 基本フローの turn の本文は1回目と継続の指示を分けないまま残す | 判断19 のとおり、**送る本文の選び分けは外から見た相互作用が同じ1回の送信である。**復帰した run も1回目の本文を送るという規則は設計 3-3b が持ち、基本フローの記述は変わらない | `docs/plans/continuo_design.md#3-3b`、`internal/orchestrator/dispatch.go` の `startRun`（`rs.beginAttempt`） | 75% |
| 111 | 起動フラグを決める段を独立したステップにしなかったこと | 設計の段5b（どのセッションで起動するかを決める）は、基本フローでは起動のステップの IF として書く | **段5b の決定は外から見える結果を1つも作らない。**起動フラグとして現れるのが最初であり、そこに条件を置けば「着手の前に読んだ身元ファイル」という入力も一緒に書ける。独立したステップにすると、以降の RFS 参照をもう1つぶん振り直すことになる | `docs/plans/continuo_design.md#3-16` の段5b と段9、`internal/orchestrator/dispatch.go` の `startRun` | 85% |

## 重なりを疑った5組の判定

**言いたいこと。**足す候補10本のうち5本は、本流の既存の枝と後始末が似ている。
**両方の本文を読み比べた結果、5組とも別の枝だった。**1本も落としていない。

| 足した枝 | 疑った相手 | 判定 | 何が違うか |
| --- | --- | --- | --- |
| リトライの尽き | 送信の失敗 | 別 | あちらは `turnSendFailed` の引き金。こちらは `RetryCount >= MaxRetries` の後始末 |
| リトライの尽き | 一時的な送信の失敗 | 別 | あちらは `turnTransient` でリトライを1つも積まない |
| リトライの尽き | 失敗の繰り返し | 別 | あちらは issue ごとの `o.failures`（印の外・dispatch 前）。こちらは run ごとの `RetryCount`（印の中） |
| 復元の断念 | 起動の断念 / 起動の待ち直し | 別 | あちらは着手の段10（`confirmStartupWithRestart`）。こちらは `ensureAgentComment` の材料不足 |
| 着手の途中の失敗 | 書かずに取りやめる | 別 | あちらは `ErrStatusNotWritten`（Status を1バイトも書いていない）。こちらは書いたあとの失敗 |
| 着手の途中の失敗 | 使えないworktree | 別 | あちらは `preflight`（Status を書く前の読み取りだけの検査） |
| 着手の途中の失敗 | paneの断念 | 別 | あちらは `AgentStartWithRetry` の受け付け待ちの尽き。WHEN で明示的に除いた |
| turnの終わりの取りこぼし | 無音の打ち切り | 別 | あちらは画面の版が `turn_timeout_ms` のあいだ増えないことを巡回が見る。こちらは待ち受け直後の `settle_ms` |
| turnの終わりの取りこぼし | turnの継続 | 別 | あちらは `task-notification` が届いた側（turn は続いている） |
| 走行中のissue | 空きスロット不足 | 別 | あちらは印の総数。こちらはこの issue に印が付いているか |
