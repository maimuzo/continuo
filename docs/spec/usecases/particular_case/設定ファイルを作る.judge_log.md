# 判断ログ: 設定ファイルを作る

- 対象: `docs/spec/usecases/particular_case/設定ファイルを作る.rucm.md`
- 作成日 / 作成モデル: 2026-08-20 / Claude Opus 5 (1M context)
- 参照した根拠資料: `docs/plans/continuo_design.md`（3-32 / 5-1）、`docs/trying_it_out.md`、`internal/scaffold/detect.go`、`internal/scaffold/scaffold.go`、`internal/scaffold/fill.go`、`internal/scaffold/template.go`、`cmd/continuo/main.go`

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | 配置先ディレクトリ | `particular_case/` | `continuo init` の1回の実行という単一目的・単一操作である。他のユースケースを跨がない | `docs/plans/continuo_design.md`（3-32） | 95% |
| 2 | USE CASE NAME | 設定ファイルを作る | 動詞で終わる名詞句にする規約に合わせた。`continuo init` が置くのは `WORKFLOW.md` 1つだけであり、それが continuo の設定ファイルそのものである | `internal/scaffold/scaffold.go` の `fileName`、`docs/plans/continuo_design.md`（5-1） | 95% |
| 3 | BRIEF DESCRIPTION | 雛形の書き出しと、gh からの自動記入と、引けないときのプレースホルダ残しの3点 | `runInit` の役割がこの3点で尽きている。終了コードと文言だけを CLI が決め、値の決定は `scaffold.Detect`、書き出しは `scaffold.WriteTemplateWithValues` が行う | `cmd/continuo/main.go` の `runInit`、`internal/scaffold/detect.go` の `Detect` | 95% |
| 4 | PRECONDITION | 実行ファイルを実行できる。書き出す先のディレクトリのパスが決まっている | `continuo init` は他のサブコマンドの実行結果に依存しない。位置引数が0個ならいまいるディレクトリを使うため、パスは常に決まる | `internal/scaffold/scaffold.go` の `resolveDir` | 85% |
| 5 | PRIMARY ACTOR | 利用者 | `continuo init` は人間が明示的に叩くコマンドである。常駐プロセスからは呼ばれない | `docs/trying_it_out.md`、`cmd/continuo/main.go` の `Run` | 95% |
| 6 | SECONDARY ACTORS | gh | システムが値を引くために依存する外部の相手が gh だけである。GitHub API を直接は叩かない | `internal/scaffold/detect.go` の `RunGH` | 95% |
| 7 | DEPENDENCY | なし | 他ユースケースを INCLUDE しない。`continuo doctor` とは呼び出し関係がない | `cmd/continuo/main.go` の `Run` | 95% |
| 8 | GENERALIZATION | なし | 汎化関係にあるユースケースが存在しない | - | 90% |
| 9 | ステップ分割の方針 | 「値を決める」「外部へ要求する」「検証する」「書く」「応答する」を1ステップずつに割る | R4（1文1動作）に従い、実装の分岐点がそのままテストの分岐点になるように割った。ファイルを作る（24）と書き込む（25）を分けたのは、中断の代替フローが両者の間に入るためである | `internal/scaffold/scaffold.go` の `WriteTemplateWithValues` | 90% |
| 10 | 基本フローの範囲 | 引数の受理から WORKFLOW.md の作成と結果の応答まで | ハッピーパスは「gh から両方の値が引けて、書き出す先に何も無い」ときである。他は全部代替フローへ出した | `docs/trying_it_out.md`（`init` の出力例） | 95% |
| 11 | 基本フロー 2 の VALIDATES THAT | 引数が受け付けられる形式である | `runInit` が `--owner` の形式・`--project` の値・位置引数の個数・位置引数のあとのフラグの4つを、gh を叩く前に弾く | `cmd/continuo/main.go` の `runInit` | 95% |
| 12 | 基本フロー 3 の IF | owner オプションの指定の有無で分ける | `scaffold.DetectOptions.Owner` が空でなければ gh を叩かない。どちらの枝も正常なので基本フロー内の IF にした | `internal/scaffold/detect.go` の `detectOwner` | 100% |
| 13 | 基本フロー 6 の gh への要求 | GitHub のログイン名を要求する | 実装は `gh api user --jq .login` を1回だけ呼ぶ | `internal/scaffold/detect.go` の `detectOwner` | 100% |
| 14 | 基本フロー 7 の VALIDATES THAT | user / organization 名として受け付けられるログイン名を応答する | 実装は「実行が失敗」「空文字」「`ValidOwner` を通らない」の3つを同じ扱い（埋めない）にしている。1つの条件にまとめた | `internal/scaffold/detect.go` の `detectOwner`、`internal/scaffold/fill.go` の `ValidOwner` | 95% |
| 15 | 基本フロー 10 の IF | project オプションの指定の有無で分ける | `scaffold.DetectOptions.ProjectNumber` が0より大きければ gh を叩かない。owner の決定より後に置いたのは実装の呼び出し順に合わせるためである | `internal/scaffold/detect.go` の `Detect` / `detectProject` | 100% |
| 16 | 基本フロー 13 の VALIDATES THAT | owner の値が決まっている | `detectProject` は owner が空文字なら gh を1回も叩かずに理由だけを返す。この分岐を書かないと、owner を引けなかったときの経路が実装と食い違う | `internal/scaffold/detect.go` の `detectProject` | 95% |
| 17 | 基本フロー 15 の VALIDATES THAT | gh がボードの一覧を応答する | `gh project list` の実行失敗と、出力を JSON として解釈できない場合をまとめた。どちらも「一覧が得られない」で、案内も同じ形である | `internal/scaffold/detect.go` の `detectProject` / `parseProjectList` | 90% |
| 18 | 基本フロー 16 と 17 の VALIDATES THAT を2つに割ったこと | 「1件以上あるか」と「1件だけか」に割る | 実装は候補0件・1件・複数の3つで案内を変える。1つの条件にまとめると、0件と複数のどちらでも同じ代替フローになり、案内の違いがテストで確かめられなくなる | `internal/scaffold/detect.go` の `detectProject` の候補件数による分岐 | 95% |
| 19 | 閉じたボードの扱い | 候補から落とす | `parseProjectList` が `closed` のボードを落としてから件数を数える。条件文に「閉じていない」と書かないと件数の判定がずれる | `internal/scaffold/detect.go` の `parseProjectList` | 100% |
| 20 | 基本フロー 20 と 21 の VALIDATES THAT | パスの存在とディレクトリであることを別々に検証する | 実装が `ErrDirNotFound` と `ErrNotADirectory` を別の sentinel error にし、案内の文言も分けている | `internal/scaffold/scaffold.go` の `WriteTemplateWithValues` | 100% |
| 21 | 基本フロー 22 の VALIDATES THAT | 書き出す先の WORKFLOW.md が symlink ではない | `syscall.O_NOFOLLOW` を付けて開き、`ELOOP` を `ErrSymlink` に直している。force の有無によらず止まるため、force の判定より前に置いた | `internal/scaffold/scaffold.go` の `openError` | 100% |
| 22 | 基本フロー 23 の VALIDATES THAT | 書き出す先に WORKFLOW.md がない | force を指定しない実行は `O_EXCL` で「無いときだけ作る」を1回の操作にしている | `internal/scaffold/scaffold.go` の `WriteTemplateWithValues` | 100% |
| 23 | 基本フローの POSTCONDITION | ファイルの存在・2つのキーが埋まっていること・終了コード 0 | 終了コードを書いたのは、外から観測できてテストに落とせる唯一の成否の印だからである | `cmd/continuo/main.go` の `runInit` の戻り値 | 95% |
| 24 | 代替フロー 引数指定エラー | 受け付けられない箇所を応答して ABORT。終了コード 2 | 実装は引数の誤りだけを 2 で返し、書き出しの失敗（1）と区別している | `cmd/continuo/main.go` の `runInit` | 100% |
| 25 | 代替フロー ログイン名取得失敗 | プレースホルダを残し、理由と2種類の案内を記録して RESUME STEP 10 | 設計が「gh が無い・認証が無いときも失敗させない」と決めている。復帰先を 10 にしたのは、owner が決まらなくても project オプションの値は使われるためである | `docs/plans/continuo_design.md`（3-32）、`internal/scaffold/detect.go` の `detectProject` | 95% |
| 26 | 代替フロー owner未確定 | ボードの候補を引かずに RESUME STEP 20 | `detectProject` は owner が空文字のとき gh を叩かない。POSTCONDITION に「gh に一覧を要求していない」と書き、無駄な呼び出しが増えていないことをテストで確かめられるようにした | `internal/scaffold/detect.go` の `detectProject` | 95% |
| 27 | 代替フロー ボード一覧取得失敗 | プレースホルダを残し、project の scope の案内と再実行の案内を記録して RESUME STEP 20 | 実装の案内が「`gh auth login -s project` でログインし直す」と「`--project` で直接指定する」の2つである | `internal/scaffold/detect.go` の `detectProject` | 95% |
| 28 | 代替フロー ボード候補なし | ボードを1つ作る案内を記録して RESUME STEP 20 | 候補0件のときだけボードの作り方を出す。設計 3-34 が「ボードは既存のものに合わせる」としているため、案内はボードの作成だけに留めた | `internal/scaffold/detect.go` の `detectProject`、`docs/plans/continuo_design.md`（3-34） | 90% |
| 29 | 代替フロー ボード候補が複数 | 候補の一覧を記録し、番号を指定して実行し直させる。対話しない | 設計が「選ばせない」「標準入力を握らない」と明示している。POSTCONDITION に「問い合わせを出していない」と書き、対話を足す変更が入ったら落ちるようにした | `docs/plans/continuo_design.md`（3-32 の「対話で選ばせない」） | 100% |
| 30 | 代替フロー 書き出し先のパス不在 | ディレクトリを作らずに ABORT。終了コード 1 | force でもディレクトリを作らない。打ち間違えたパスに WORKFLOW.md が生まれると利用者が見失うためである | `internal/scaffold/scaffold.go` の `ErrDirNotFound` のコメント | 100% |
| 31 | 代替フロー 書き出し先がディレクトリではない | 受け取るのはディレクトリであることを応答して ABORT。終了コード 1 | 実装の文言がそのまま「continuo init が受け取るのは WORKFLOW.md を置くディレクトリです」である | `cmd/continuo/main.go` の `runInit` | 100% |
| 32 | 代替フロー 書き出し先がsymlink | force でも辿らずに ABORT。終了コード 1 | 辿ると指定されたディレクトリの外にあるリンク先を潰す。実装は force を勧める文言を出さない | `internal/scaffold/scaffold.go` の `openError` | 100% |
| 33 | 代替フロー 既存の設定ファイルの中に VALIDATES THAT を置いたこと | force オプションの指定を検証する条件ステップにした | 「既にある」の分岐先が force の有無で2つに割れる。基本フローの IF の内側に代替フローを足すより、既存ファイルの発見を1点にまとめたほうが分岐元が明確になる | `internal/scaffold/scaffold.go` の `WriteTemplateWithValues` の force による分岐 | 85% |
| 34 | 代替フロー 既存の設定ファイルの復帰先 | RESUME STEP 27 | 上書きのときは「上書きしました」と応答したうえで、キーごとの値の決め方は作成のときと同じものを出す。26 ではなく 27 へ戻すことで応答の重複を避けた | `cmd/continuo/main.go` の `runInit` / `printDetection` | 95% |
| 35 | 代替フロー 上書きの指定なし | 既存ファイルを変更せずに ABORT。終了コード 1 | 実装は `O_EXCL` で開けなかった時点で書き込みを行わない | `internal/scaffold/scaffold.go` の `WriteTemplateWithValues`、`docs/trying_it_out.md` | 100% |
| 36 | 任意時点代替フロー 書き込み中の中断を置いたこと | 実行の中断を任意時点代替フローとして1本置く | 中断は利用者がいつでも起こせる。実装に signal の受け口はないため、これは「いま観測できる帰結」を書いたものであり、実装済みの挙動の写しではない | - | 60% |
| 37 | 書き込み中の中断の BRANCH FROM | BASIC FLOW 25（雛形の全文を書き込む） | 最悪のタイミングを選ぶ基準に従った。force を伴う実行では `O_TRUNC` で既存の内容を先に捨ててから書くため、この時点の中断だけが元の設定を失わせる。gh を待っている間の中断は値が埋まらないだけで、失うものがない | `internal/scaffold/scaffold.go` の `WriteTemplateWithValues` の `O_TRUNC` | 75% |
| 38 | 書き込み中の中断の POSTCONDITION | 全文と一致しない。force を伴う実行では元の内容が失われる | `O_TRUNC` を付けて開いた時点で既存の内容は消える。書き込みは一時ファイルへ書いてから置き換える形ではない | `internal/scaffold/scaffold.go` の `WriteTemplateWithValues` | 95% |
| 39 | プレースホルダの値の書き方 | owner は `__FILL_ME__`、project_number は 0 | 実装の定数がこの2つである。project_number に文字列を入れると YAML の読み込みで落ちるため、数値の 0 のままにする | `internal/config/placeholder.go` の `Placeholder` / `PlaceholderProjectNumber`、`docs/plans/continuo_design.md`（3-32） | 100% |
| 40 | 「記録する」と「応答する」を分けたこと | 値を埋められない理由と案内は記録し、最後の1ステップでまとめて応答する | 実装は `Detect` が理由と案内を `Field` に溜め、書き出しのあとに `printDetection` が一度に出す。応答をその場で行うと出力の順序が実装と食い違う | `internal/scaffold/detect.go` の `Field`、`cmd/continuo/main.go` の `printDetection` | 95% |
| 41 | 用語の統一 | キーは `tracker.provider.owner` / `tracker.provider.project_number` のフルパスで書く | 雛形の中では `owner:` という短い名前だが、`branch_template` の中にも `owner` が出るため、短い名前では指すものが一意にならない | `internal/scaffold/fill.go` の `ownerPlaceholderCode` のコメント | 90% |
| 42 | コマンドラインオプションの呼び方 | 本文では「owner オプション」「project オプション」「force オプション」と書く | mermaid のラベルにハイフン2つを書くと図の解釈が揺れるため、rucm ブロックと図で同じ呼び方に揃えた | - | 80% |
| 43 | 段20〜24（trust.repositories を埋める）を足した理由 | `continuo init` がボードからリポジトリを集めて並べる | **当初の RUCM には段が無かった。**実装は `gh project item-list` でボードの項目を引き、リポジトリの名前を重複なく集めて `trust.repositories` に並べる（2026-08-21 に突き合わせて発見） | `internal/scaffold/detect.go` の `detectRepositories` | 95% |
| 44 | 引けなかったときに雛形を作る理由 | プレースホルダのまま残して先へ進む | **`trust.repositories` を引けないことは、雛形を作らない理由にならない。**owner とボードの番号が決まっていれば、そこまでは書ける。残りは人間が手で書ける | `internal/scaffold/detect.go` の `detectRepositories` | 90% |
| 45 | 段33（要らない行を消すよう促す）を足した理由 | ボードに載っている全リポジトリが並ぶ | **消さないと、無関係なリポジトリまで Claude Code に信頼登録される。**`continuo init` は「消すこと」を案内する | `internal/scaffold/fill.go` の `repositoriesBlock` | 95% |
