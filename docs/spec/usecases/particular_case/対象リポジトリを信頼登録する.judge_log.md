<!-- 目的: 「対象リポジトリを信頼登録する」の RUCM を書くときに AI が行った判断と、その根拠を記録する -->

# 判断ログ: 対象リポジトリを信頼登録する

- 対象: `docs/spec/usecases/particular_case/対象リポジトリを信頼登録する.rucm.md`
- 作成日 / 作成モデル: 2026-08-20 / Claude Opus 5 (1M context)
- 参照した根拠資料: `docs/plans/continuo_design.md`（1-6 / 3-6 / 3-32 / 3-33 / 4-3）、`docs/trying_it_out.md`（段3 / 段5 / 段6 / うまく動かないとき / いま無いもの）、`internal/workspace/trust.go`、`internal/doctor/checks.go`、`internal/doctor/report.go`

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | USE CASE NAME | 対象リポジトリを信頼登録する | 依頼で指定された名前をそのまま使った。動詞で終わる名詞句の規則も満たす | 依頼文 | 100% |
| 2 | 配置ディレクトリ | `particular_case/` | 「フォルダの信頼を承認する」という単一目的・単一操作の単位であり、複数ユースケースを跨ぐ時系列ではない | rucm スキルの粒度ガイド | 95% |
| 3 | BRIEF DESCRIPTION | 利用者が clone のディレクトリで信頼確認を承認し、システムが信頼登録を読み取って結果を応答する | 設計は「人間が対象リポジトリごとに1度だけ承認する」を採る形と明記し、continuo 側は読むだけと決めている | `docs/plans/continuo_design.md#3-33`、`#4-3` | 95% |
| 4 | PRECONDITION（WORKFLOW.md） | 検査を実行するディレクトリに `WORKFLOW.md` がある | continuo は「いまいるディレクトリの WORKFLOW.md」を読む。設定が読めないと doctor の信頼登録は `!` になり、承認すべき対象が分からない | `docs/trying_it_out.md`（段4・段5 の未記入時の出力） | 95% |
| 5 | PRECONDITION（issue が1件以上） | ボードの `active_states` の Status に対象リポジトリの issue が1件以上ある | 対象リポジトリの集合はボードを読んで決まる。issue が0件だと clone と信頼登録は `!`（検査する対象がありません）になり、このユースケースが成立しない | `docs/plans/continuo_design.md#3-32`（対象リポジトリが0件だったとき）、`docs/trying_it_out.md`（段6） | 95% |
| 6 | PRECONDITION（gh の scope） | `gh` の認証は `project` の scope を含む | ボードを読めないと下流の clone と信頼登録は `!` になる。検査の依存関係が設計に図で示されている | `docs/plans/continuo_design.md#3-32`（検査の依存関係） | 90% |
| 7 | PRIMARY ACTOR | 利用者 | 信頼の承認は人間だけが行える。continuo は自動化しないと決めている | `docs/plans/continuo_design.md#3-33` | 100% |
| 8 | SECONDARY ACTORS | Claude Code | 信頼の記録（`hasTrustDialogAccepted`）を書くのは Claude Code であり、システムは読むだけである。書き手を記述から消さないために副アクターに置いた | `docs/plans/continuo_design.md#4-3`、`internal/workspace/trust.go` の `CheckTrustForClonePath` | 65% |
| 9 | 副アクターと R3 の関係 | 利用者が Claude Code を直接操作するステップ（7・8・9）を残す | R3 はアクター間の直接相互作用を禁じるが、continuo は信頼の承認を仲介しないと決めている（3-33）。システムに仲介させて書くと、実在しない自動化を記述することになる。**記述の正しさを R3 の形式より優先した** | `docs/plans/continuo_design.md#3-33` | 60% |
| 10 | DEPENDENCY | なし | 他のユースケースを取り込まない。INCLUDE を持つのは scenario 側である | rucm スキルのファイル規約 | 90% |
| 11 | GENERALIZATION | なし | 汎化関係にある他のユースケースが無い | - | 90% |
| 12 | 基本フローの起点 | `continuo doctor` の実行から始める | 設計は「doctor が未承認のものを名指しで出すので、何を承認すればよいかは分かる」と書いている。**対象リポジトリの集合はボードを読むまで確定しない**ので、承認から書き始めると「どのリポジトリを承認するか」が決まらない。`docs/trying_it_out.md` は段3（承認）を段5（doctor）より前に置くが、そちらは対象リポジトリが1件と分かっている試用の手順である | `docs/plans/continuo_design.md#3-33`、`#3-32`（対象のリポジトリはボードを読んで決める） | 75% |
| 13 | ステップ分割方針 | 1ステップ1動作にし、doctor の内部処理はステップ2・3・4・5に割る | R4（1文1動作）に従う。信頼を引く鍵の作り方が設計で3段に分解されているので、その3段（clone のパス・root への解決・記録の読み取り）をそれぞれ独立のステップにした | `docs/plans/continuo_design.md#3-6`（信頼を引く鍵の作り方）、`internal/workspace/trust.go` の `CheckTrust` | 85% |
| 14 | ステップ2 | システムはボードから対象リポジトリの一覧を集める | doctor がボードを1回読み、返ってきた issue の `nameWithOwner` を重複なく集めると設計が明記している。**「読んで集める」と書くと1文2動作になる**ので、動詞を「集める」1つにした（R4） | `docs/plans/continuo_design.md#3-32` | 95% |
| 15 | ステップ3（VALIDATES THAT） | 対象リポジトリのすべての clone がローカルにある | clone が無いと信頼の鍵になる絶対パスを引けない。実装も clone の検査が先で、信頼の検査は clone のパスを受け取る | `internal/doctor/checks.go` の `checkClone` と `checkTrust` | 95% |
| 16 | ステップ4 | システムは clone の絶対パスをリポジトリの root へ解決する | 鍵の作り方の2段目。`git rev-parse --path-format=absolute --show-toplevel` でシンボリックリンクを解決する | `docs/plans/continuo_design.md#3-6`、`internal/workspace/trust.go` の `gitToplevel` | 95% |
| 17 | ステップ5 | システムはホームディレクトリの `.claude.json` から信頼登録の記録を読む | 鍵の作り方の3段目。`projects[<鍵>].hasTrustDialogAccepted` を読む。**読むだけで書かない**ことが決着している | `docs/plans/continuo_design.md#4-3`、`internal/workspace/trust.go` の `claudeConfigFile` | 100% |
| 18 | ステップ6 | システムは未承認のリポジトリの名前を応答する | doctor は「対象 N件のうち M件が未承認です」と名指しで出す。何を承認すればよいかを人間に伝えるのがこのステップの役目である | `internal/doctor/checks.go` の `checkTrust` | 90% |
| 19 | ステップ7（承認する場所） | 未承認のリポジトリの **clone のディレクトリ**で Claude Code を起動する | 信頼はリポジトリ単位で記録される（実測）。worktree のパスで引くと必ず未承認になる。1度承認すれば、そこから切る全ての worktree で確認が出ない | `docs/plans/continuo_design.md#1-6`、`#4-3`（実測で確かめたこと） | 100% |
| 20 | ステップ8と9を分けたこと | 「承認を選ぶ」と「Claude Code を終了する」を別ステップにした | R4（1文1動作）。承認だけを行う時点を独立させると、承認を選ばない場合の分岐元をステップ8に固定できる | rucm スキルの R4、`docs/trying_it_out.md`（段3 承認したら `/exit` で抜けてよい） | 90% |
| 21 | ステップ10 | 利用者はシステムに前提の検査を要求する | 承認が効いたかどうかは doctor をもう一度叩いて確かめる。試用手順も「ここでもう一度 doctor を叩くと clone と信頼登録が `✓` になる」と書いている | `docs/trying_it_out.md`（段6） | 95% |
| 22 | ステップ11（VALIDATES THAT） | 対象リポジトリのすべてが信頼登録されている | 承認したつもりで通っていない場合を代替フローで書くために、条件ステップにした（R22） | rucm スキルの R22 | 90% |
| 23 | ステップ12 | システムは信頼登録の項目が `✓` であることを応答する | doctor の出力は `✓` / `✗` / `!` の3記号である。記号をそのまま書くとテストで照合できる | `docs/plans/continuo_design.md#3-32`（出力と終了コード）、`internal/doctor/report.go` | 90% |
| 24 | 基本フローの POSTCONDITION | すべて信頼登録済み。信頼登録の項目が `✓`。システムは対象リポジトリの issue を dispatch する | 信頼の検査は dispatch の直前に行われ、通れば飛ばされない。事後条件は「承認した結果、何が変わるか」まで書く | `docs/plans/continuo_design.md#3-6`（dispatch の直前に検査するもの） | 85% |
| 25 | 代替フロー clone不在 の種別 | SPECIFIC ALTERNATIVE FLOW（`RFS BASIC FLOW 3`） | ステップ3の `VALIDATES THAT` が偽になる場合であり、偽ケースは RFS を持つ特定代替フローで書く決まりである | rucm スキルの厳密規則9 | 95% |
| 26 | clone不在 の対処 | システムが clone を置くコマンドを応答し、利用者が clone を置く | doctor は `ghq get <owner>/<repo> を実行してください` を対処として出す。continuo は勝手に clone しない | `internal/doctor/checks.go` の `checkClone`、`internal/workspace/trust.go` の `CheckTrust` の理由文 | 95% |
| 27 | clone不在 の終端 | `RESUME STEP 1`（検査をやり直す） | clone を置けば検査からやり直せる。ABORT にすると「置いたのに確かめられない」で終わる | rucm スキルの R25 | 90% |
| 28 | clone不在 の POSTCONDITION | clone がローカルにある。その検査では clone が `✗`、信頼登録が `!` | 上流が `✗` なら下流は `!` にして飛ばすと設計が決めている。試用手順の出力にも同じ組み合わせがある | `docs/plans/continuo_design.md#3-32`、`docs/trying_it_out.md`（段5 の出力例） | 90% |
| 29 | 代替フロー 承認後も未承認 の種別 | SPECIFIC ALTERNATIVE FLOW（`RFS BASIC FLOW 11`） | ステップ11の `VALIDATES THAT` が偽になる場合である | rucm スキルの厳密規則9 | 95% |
| 30 | 承認後も未承認 の原因の扱い | 原因を1つに決めず、「未承認と判定した理由」と「信頼の鍵に使った絶対パス」を応答させる | 実装は理由の文字列を返す（鍵が無い / `hasTrustDialogAccepted` が false / `~/.claude.json` が無い）。**承認したのに `✗` になる典型は、clone ではないディレクトリ（worktree など）で承認した場合**だが、原因を1つに固定すると他の原因を取りこぼす。鍵のパスを見せれば、どこで承認すべきかが人間に分かる | `internal/workspace/trust.go` の `CheckTrustForClonePath`、`docs/plans/continuo_design.md#1-6` | 65% |
| 31 | 承認後も未承認 の終端 | `RESUME STEP 10`（検査だけをやり直す） | 鍵のパスで承認し直した後は、doctor の再実行から再開すれば足りる。ステップ1へ戻すと、既に済んだ clone の検査まで巻き戻る | rucm スキルの R25 | 80% |
| 32 | 承認後も未承認 の POSTCONDITION | 鍵に使った絶対パスのディレクトリが信頼登録されている。判定の理由が表示されている | このフローの終了時点の状態を書いた（再検査の結果は復帰先のステップ11で判定する） | rucm スキルの R26 | 85% |
| 33 | 代替フロー 承認の見送り の種別 | GLOBAL ALTERNATIVE FLOW（任意時点代替フロー） | 分岐元のステップ8は利用者の操作であり `VALIDATES THAT` を持たない。特定代替フローの `RFS` は条件ステップしか指せない（E021）ため、GLOBAL を選んだ。「承認を選ばない」はキャンセルに当たる | rucm スキルのフロー種別の表、rucm-checker の E021 | 85% |
| 34 | 承認の見送り の `BRANCH FROM` | `BASIC FLOW 8`（1点だけ） | 選定基準は最悪のタイミングである。ステップ8は**信頼確認まで到達したうえで承認を見送る唯一の時点**であり、ここでの離脱だけが「doctor が名指しした未承認が解消されないまま残る」状態を作る。ステップ7より前で止めた場合は、doctor が応答した直後の状態に戻るだけで、失われるものが無い。原則どおり1点に絞った | rucm スキルの BRANCH FROM 選定基準 | 80% |
| 35 | 承認の見送り の `WHEN` | 利用者が Claude Code の信頼確認で承認を選ばない場合 | 実機の信頼確認は「Yes, I trust this folder」と「No, exit」の2択である。後者を選ぶ場合を条件文にした | `docs/plans/continuo_design.md#1-6`（実測した画面） | 95% |
| 36 | 承認の見送り の終端 | `ABORT` | 承認しない限り信頼登録は成立しないので、このユースケースは目的を果たさずに終わる | rucm スキルの R24 | 95% |
| 37 | 承認の見送り の POSTCONDITION | 信頼登録されていない。信頼登録の項目は `✗` のまま。システムは対象リポジトリの issue を dispatch しない | 未承認のリポジトリは dispatch しないと設計が決めている（`trust.on_untrusted` に従い、その issue だけ飛ばす） | `docs/plans/continuo_design.md#3-6`、`#4-3`（したがって continuo はどうするか） | 95% |
| 38 | 信頼の一括登録を書かなかったこと | 自動化のフローを RUCM に含めない | 設計は自動化を明確に採らないと決めている。将来入れる場合の条件も設計側に書かれており、RUCM は「人間が承認する」形だけを表す | `docs/plans/continuo_design.md#3-33`、`docs/trying_it_out.md`（いま無いもの） | 100% |
| 39 | 「信頼していないと hook が動かない」という記述を採らなかったこと | RUCM の本文に hook の可否を書かない | 設計 4-3 は公式ドキュメントの表を引用し、**settings ファイルの hook と `env` ブロックは信頼していなくても使われる**と述べる。一方で `docs/plans/continuo_design.md#3-6` と `docs/trying_it_out.md`（うまく動かないとき）は「信頼していないと hook が1つも動かない」と書いており、**資料どうしが食い違っている**。どちらが正しくても「未承認なら dispatch しない」という帰結は変わらないので、RUCM には帰結だけを書き、原因の記述を持ち込まなかった | `docs/plans/continuo_design.md#4-3` と `#3-6` の食い違い | 60% |
| 40 | 用語の統一 | 「信頼登録」「clone」「worktree」「dispatch」を英語・日本語のまま混ぜずに固定した | doctor の項目名が `信頼登録` と `clone` である（`internal/doctor/report.go` の `LabelTrust` と `LabelClone`）。資料で使われている語に合わせる（R9） | `internal/doctor/report.go`、プロジェクトの報告ルール | 90% |
| 41 | mermaid 2ブロックの内容 | rucm ブロックの全12ステップと3つの代替フローを両方の図に写した | flowchart は分岐と復帰（`RESUME STEP` / `ABORT`）を、sequenceDiagram は利用者・システム・Claude Code・信頼の記録ファイルの4者のやり取りを表す。信頼の記録を書くのが Claude Code、読むのがシステムであることを図で分けた | `docs/plans/continuo_design.md#4-3` | 90% |
| 42 | 基本フローを `continuo trust` に書き直した理由 | 手作業ではなくコマンドで登録する | **当初の RUCM は「利用者が clone のディレクトリで Claude Code を起動して承認を選ぶ」という手順だった。**実装は `continuo trust` が `~/.claude.json` を直接書き換える。**仕様と実装が食い違っていた**（2026-08-21 に突き合わせて発見） | `internal/trust/trust.go` の `Apply` | 95% |
| 43 | 段8（写しを取る）を入れた理由 | 書き込む前に必ずバックアップ | **`~/.claude.json` は Claude Code 自身が使うファイルである。**壊したときに人間が戻せなければならない | `internal/trust/trust.go` の `writeBackup` | 95% |
| 44 | 段9（もう一度読み直す）を入れた理由 | 調べた内容で上書きしない | **起動中の Claude Code のセッションが同じファイルを書き戻している。**Plan が読んだ内容で上書きすると、その間の変更が消える | `internal/trust/trust.go` の `Apply` | 90% |
| 45 | 段14（書き込んだ項目を確かめる）を入れた理由 | 書いたあとに読み直して確認する | **書き込んだのに読み直せないことがある**（別のプロセスが同時に書き換えた等）。黙って成功として見せると、人間は登録できたと思い込む | `internal/trust/trust.go` の `Apply` の `VerifyProblems` | 85% |
| 46 | 「実行せずに確かめる」を任意時点代替フローにした理由 | `--dry-run` は段7 から分岐する | **`--dry-run` は「何を許すことになるか」を見せるためのもの。**対象が1件以上あっても、そこから先へ進まない | `internal/cli/cli.go` の `runTrust` | 90% |
