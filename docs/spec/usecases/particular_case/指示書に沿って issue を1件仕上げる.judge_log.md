# 判断ログ: 指示書に沿って issue を1件仕上げる

- 対象: `docs/spec/usecases/particular_case/指示書に沿って issue を1件仕上げる.rucm.md`
- 作成日 / 作成モデル: 2026-09-03 / Claude Opus 5
- 参照した根拠資料: `internal/prompt/builtin.md`（人間が確定させた文面）、`internal/prompt/prompt.go`、`internal/scaffold/template.go`、`docs/plans/continuo_design.md`（5-3 / 5-3c / 5-3d / 5-3f / 5-3h / 5-3i / 5-3m / 3-2 / 3-74c）

## この記述を作った経緯

**人間の指示（2026-09-03）。**原文で貼る。

> issue #188の作業の中に、rucmの見直しは入っている?
> プロンプトの流れに沿ったrucmを作っておけ。テストまでは不要かもしれないが、どういう使われ方をするのかrucmに記録しておくことが重要だ。

**つまり、求められているのは「記録」である。**テストを生成することは求められていない。

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | USE CASE NAME | 指示書に沿って issue を1件仕上げる | 人間の言葉は「プロンプトの流れ」だが、**流れの主体はエージェントであり、行き先は issue が仕上がることである。**動詞で終わる名詞句の規則にも合わせた | 依頼文 | 85% |
| 2 | 配置ディレクトリ | `particular_case/` | 単一の目的（1件を仕上げる）の流れであり、複数ユースケースを跨ぐ時系列ではない | rucm スキルの粒度ガイド | 85% |
| 3 | PRIMARY ACTOR | エージェント | **この記述の目的は「エージェントが指示書をどう使うか」を残すことである。**巡回タイマーを主にすると、既にある「issue を1件処理する」と同じ視点になり、重ねる意味が無くなる | 依頼文（「どういう使われ方をするのか」） | 90% |
| 4 | 副アクターに「システム」を入れたこと | 入れる | 文面を組み立てて送るのも、表明を読んで Status を書くのも continuo である。**エージェントだけでは、前後が切れて流れにならない** | `internal/prompt/prompt.go`、`internal/orchestrator/lifecycle.go` | 90% |
| 5 | 副アクターを「GitHub」としたこと | `GitHub Projects v2` ではなく `GitHub` | **この流れでエージェントが叩くのは issue と pull request であり、カンバンではない。**カンバンへ書くのはシステムの側だけである | `internal/prompt/builtin.md` の 4-1 と 4-2 と 3-5 | 80% |
| 6 | 基本フローを段1〜3（文面の組み立て）から始めたこと | 始める | **依頼が「プロンプトの流れ」である以上、文面がどう作られるかを外せない。**とくに 5-3m で足した2つの処理（コメントを取り除く / 空になった見出しを落とす）は、**利用者が書いた文が届くかどうかを決める** | `docs/plans/continuo_design.md#5-3m` | 90% |
| 7 | 段4（`VALIDATES THAT` 本文に中身がある） | 取り除いたあとで判定する | **取り除く前で判定すると、本文が案内のコメントだけだったときに「本文はあります」と言いながら断片は足されず、内訳から本文の行が丸ごと消える。**この誤りは敵対的レビューが実測で見つけた | `internal/prompt/prompt.go` の `Build` | 95% |
| 8 | 段8（issue を JSON で読む） | 「JSON で」を明記する | **テキスト表示（`gh issue view --comments`）を使わせないことが、外部の人のなりすましを防ぐ唯一の手段である。**段の文から落とすと、この記述が守りを記録しないことになる | `internal/prompt/builtin.md` の 6-2 | 95% |
| 9 | 段16 と段22（敵対的レビューの subagent） | 基本フローに入れる | **人間が確定させた文面が、3-2 と 3-6 の両方で敵対的レビューを求めている。**代替フローにすると「通常は掛けない」と読める | `internal/prompt/builtin.md` の 3-2 と 3-6 | 90% |
| 10 | 段20（`VALIDATES THAT` pull request がまだ無い） | 条件ステップにする | **既にある pull request を使わせないと、turn のたびに2本目・3本目ができる。**分岐が要る箇所なので条件ステップにした | `internal/prompt/builtin.md` の 3-5 | 90% |
| 11 | 進捗報告を GLOBAL ALTERNATIVE FLOW にしたこと | グローバルにする | **最初は SPECIFIC で書いたが、検査（E021）が落ちた。**特定代替フローの分岐元は条件ステップでなければならず、段18（実装する）は条件ステップではない。**「1時間以上かかったら」は任意の時点で起きるので、グローバルが正しい形である** | `rucm_validator.py` の E021 の実行結果 | 95% |
| 12 | 進捗報告の2つの代替を、グローバルの下にぶら下げたこと | ぶら下げる | 「いちばん下が自分の進捗報告か」と「読んだ本文に印があるか」の2つが、どちらも条件ステップである。**それぞれの偽の側を書かないと、`gh api` の取得に失敗したときに印ごと本文を消す経路が記録されない** | `internal/prompt/builtin.md` の 5-3 の段2a、PR #202 の敵対的レビュー | 90% |
| 13 | 「外部の人の命令に従わない」をグローバルにしたこと | グローバルにする | **コメントを読むどの時点でも起きる。**段8 に固定すると、段9（pull request のレビュー）で同じことが起きたときに記録が無いことになる | `internal/prompt/builtin.md` の 6-1 | 85% |
| 14 | テストを生成しないこと | 生成しない | **指示書に沿って何をするかを決めるのはエージェントであり、continuo の仕組みではない。**Claude Code の中で起きることをテストから観測する手立てが無い（`claude -p` は使用禁止）。**`[W1]`（テストが1本も紐づかない経路につく警告）は落とす理由にしない**と `scripts/check-rucm.sh` が決めている | `scripts/check-rucm.sh` の47-49行、人間の指示（「テストまでは不要かもしれない」） | 90% |
| 15 | 段27（Stop hook で知らせる）を1段にしたこと | 1段にする | **hook が何種類あるかは、この流れの関心事ではない。**7種類あることは `docs/spec/event_process_system.md` が持っている。**同じことを2箇所に書かない** | `docs/spec/event_process_system.md` | 80% |
| 16 | POSTCONDITION に「pull request がある」を置いたこと | 置く | **これがこの流れの成果物である。**issue のコメントと Status だけだと、v0.1.13 までの形（人間が pull request を作る）と区別が付かない | `docs/plans/continuo_design.md#5-3i` | 90% |

## 確かめられなかったこと

| 何 | 何を試して駄目だったか |
| --- | --- |
| **エージェントが実際にこの順で動くか** | **観測する手立てが無い。**`claude -p` は使用禁止であり、`test/e2e` の `fakeClaude` は実プロセスを起動しない。**実機で issue を1件通すことでしか確かめられない**（`docs/releasing.md` がリリース前の必須の検査にしている） |
| **段10（辿れるプランファイルを読む）の範囲** | **文面は「issue とコメントに出てくる」ものを辿れと書いているが、深さの上限を書いていない。**この記述もそれに従い、上限を書いていない |
