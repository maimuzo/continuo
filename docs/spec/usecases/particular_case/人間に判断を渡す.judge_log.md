# 判断ログ: 人間に判断を渡す

- 対象: `docs/spec/usecases/particular_case/人間に判断を渡す.rucm.md`
- 作成日 / 作成モデル: 2026-08-20 / Claude Opus 5 (1M context)
- 参照した根拠資料: `docs/plans/continuo_design.md`（3-5 / 3-11 / 3-25 / 3-26 / 3-50 / 3-51 / 4-1 / 8-1）、`internal/orchestrator/lifecycle.go`、`internal/orchestrator/signal.go`、`internal/orchestrator/comment.go`、`internal/orchestrator/unknownstate.go`、`internal/orchestrator/reconcile.go`、`internal/tracker/adapter.go`

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | USE CASE NAME | 人間に判断を渡す | 依頼で指定された名前をそのまま使う。動詞で終わる名詞句の規則も満たす | 依頼文 | 100% |
| 2 | 配置ディレクトリ | `particular_case/` | 「表明を受けて worker を止め、人間の回答を待つ」という単一目的の操作単位である | rucm スキルの粒度ガイド | 95% |
| 3 | 「エージェント」という呼び方 | worker の中で動いている Claude Code のセッションを**エージェント**と呼び、本文の「この記述で使う言葉」で1回だけ定義する | 設計が「エージェントは最終応答に決まった1行を書くだけ」と書いており、判断を持つ主体をエージェントと呼んでいる。**`rucm-checker` の R1 検査は主語に空白を含む名前（`Claude Code は`）を主語と認められない**ので、空白を含まない呼び方が要る | `docs/plans/continuo_design.md#3-25`、`#3-1`、`rucm-checker` の W003 | 75% |
| 4 | BRIEF DESCRIPTION | 表明を書く・Status を動かす・コメントを確かめて worker を止める・人間が回答して戻す の4文 | 引き渡しの定義（worker は止めるが worktree は残す）と、人間が戻すところまでを1本にした | `docs/plans/continuo_design.md#3-5`、`#4-1` | 90% |
| 5 | PRECONDITION | 常駐している。Status が running_state である。印に入っている。pane でエージェントが動いている | 表明を受けるのは走行中の run だけである | `docs/plans/continuo_design.md#3-25` | 90% |
| 6 | PRIMARY ACTOR | エージェント | 引き渡しを起こすのは表明であり、表明を書くのはエージェントである。**巡回タイマーではない** | `docs/plans/continuo_design.md#3-25` | 75% |
| 7 | SECONDARY ACTORS | GitHub Projects v2、herdr、利用者 | Status とコメントを読み書きする相手・pane を閉じる相手・回答する相手である | `docs/plans/continuo_design.md#4-1` | 80% |
| 8 | 利用者を副アクターに置き、ボードを直接操作させたこと | ステップ15 から17 で、利用者がボードのコメントを読み、回答を書き、Status を書く | **R3（アクター間の直接相互作用の禁止）から外れる。**人間は GitHub の画面でボードを触るのであって、continuo に要求しない。**システムに仲介させて書くと、実在しない自動化を記述することになる。**同じ判断を `対象リポジトリを信頼登録する` でも行っている | `docs/plans/continuo_design.md#4-1`（人間は GitHub の画面で動かす）、`docs/spec/usecases/particular_case/対象リポジトリを信頼登録する.rucm.md` | 60% |
| 9 | DEPENDENCY | なし | 他の particular_case を取り込まない | rucm スキルのファイル規約 | 95% |
| 10 | GENERALIZATION | なし | 汎化関係にあるユースケースが無い | - | 90% |
| 11 | ステップ分割方針 | 表明の読み取り・Status の書き込み・コメントの確認・worker の停止・人間の操作を、それぞれ独立のステップにする | R4（1文1動作）。**人間の操作を書かないと「渡した」で終わってしまい、戻ってくる経路がテストにならない** | rucm スキルの R4 | 90% |
| 12 | ステップ2 | transcript から表明の行を読む | `last_assistant_message` は使えない（印を書いた17件すべてで印が入っていなかった）。**行に割って探す**（印が他の文と同じブロックに入ることがある） | `docs/plans/continuo_design.md#3-25`、`internal/orchestrator/signal.go` の `ParseSignals` | 100% |
| 13 | ステップ3（VALIDATES THAT） | 表明の値が `status_signal_map` にある | 知らない値は Status を動かさない。**遷移先は設定の写像で決まる**ので、値をハードコードしない | `docs/plans/continuo_design.md#3-25`、`internal/orchestrator/lifecycle.go` の `lookupSignalTarget` | 90% |
| 14 | ステップ4 と5 | Status を ID 指定で取り直し、`terminal_states` に入っていないことを確かめる | **書く前には必ず取り直す。**エージェントが先に `Done` へ動かしていた結果を continuo が巻き戻さないためである | `docs/plans/continuo_design.md#3-4`、`#4-1`、`internal/tracker/adapter.go` の `UpdateStatus` | 100% |
| 15 | ステップ6 から10（IF-ELSE） | `blocked` なら `failure_state`、そうでなければ review の遷移先を書く | **どちらの分岐も正常である**ので基本フローの `IF-ELSE` で書いた。`In Review` と `Blocked` はどちらも引き渡しであり、worker を止めて worktree を残す点で同じ扱いになる | `docs/plans/continuo_design.md#3-5`、`#4-1`、rucm スキルの「基本フローに書くこと」の例外 | 85% |
| 16 | 引き渡しの通知を基本フローに書かなかったこと | エージェントの表明で引き渡すときは continuo がコメントを書かない | 実装は `failureState` が空でないときだけ引き渡しの通知を投稿する。表明で引き渡す経路では空を渡すので投稿しない。**記録はエージェント自身のコメントである** | `internal/orchestrator/lifecycle.go` の `finishRunClaimed` と `postHandoffComment`、`docs/plans/continuo_design.md#3-25` | 85% |
| 17 | ステップ11（VALIDATES THAT） | issue に今回の run が書いたコメントがある | run が終わるときだけ確かめる。**worktree を再利用すると前の run のコメントが残っている**ので、作成時刻が run の開始より新しいものだけを数える | `docs/plans/continuo_design.md#3-25`、`internal/orchestrator/comment.go` の `hasRunComment` | 95% |
| 18 | ステップ12 から14 の順序 | after_run → pane を閉じる → 印を外す | `after_run` は worker を止める直前に1回だけ走らせる | `docs/plans/continuo_design.md#3-9` の手順0 | 95% |
| 19 | worktree を残すこと | 基本フローで worktree も branch も消さない | `In Review` と `Blocked` で消すと、人間が回答して `Ready` へ戻したときに作業成果が失われる | `docs/plans/continuo_design.md#3-9` の手順1、`#4-1` | 100% |
| 20 | ステップ17 | 利用者が Status に `dispatch_state` の選択肢を書く | `Blocked` から `Ready` へ戻すのは人間である。**戻すと次の巡回で候補に上がり、worktree を再利用して再 dispatch される** | `docs/plans/continuo_design.md#4-1`、`#3-10` | 95% |
| 21 | 基本フローの POSTCONDITION | Status が dispatch_state。コメントが2種類ある。pane が閉じている。worktree と branch が残っている。印が外れている | 「渡して、返ってきた」状態を、次の巡回の振る舞いを決める値だけで書いた | `docs/plans/continuo_design.md#3-25`、`#4-1` | 90% |
| 22 | フロー `知らない表明` | RFS BASIC FLOW 3。Status を動かさず、次の turn に表明を促す1文を差し込む | 差し戻し（`Stop` hook が `decision: block` を返す）は採らない。**`settle_ms` と両立しない。**turn ループが既にあるので、次の turn で促す | `docs/plans/continuo_design.md#3-25`（表明せずに終わったら次の turn で促す） | 95% |
| 23 | フロー `完了済みのissue` | RFS BASIC FLOW 5。Status を書かずに worker を止める | エージェントが自分で `gh` を叩いて `Done` にしていた場合を尊重する。**巻き戻さない** | `docs/plans/continuo_design.md#3-25`（多重化の層2）、`#4-1` | 95% |
| 24 | フロー `コメントの取り戻し` | RFS BASIC FLOW 11。pane を閉じてからセッションを復帰させ、書かせてから RESUME STEP 12 | 先に worker を止めないと同じセッション UUID が2つ生きる。**`--settings` と `--permission-mode` は毎回渡し直す**（復元されないので、渡し直さないと hook が1つも効かない） | `docs/plans/continuo_design.md#3-25` の9段 | 95% |
| 25 | フロー `コメントの取り戻しの失敗` | RFS コメントの取り戻し 6。`failure_state` へ落とし、引き渡しの通知を1件書く | 9段の最後が「それでも書かれなければ failure_state へ落として人間に渡す」である。**この経路では continuo が Status を落とすので、引き渡しの通知を書く**（表明で引き渡す経路とはここが違う） | `docs/plans/continuo_design.md#3-25` の9段、`internal/orchestrator/lifecycle.go` の `failRun` | 85% |
| 26 | continuo が代筆しないこと | rucm ブロックに代筆のステップを置かない | `Stop` hook が渡す最終応答はそのターンの最後の発言であって、作業の全体を要約したものではない。**成果をまとめられるのはエージェントだけである** | `docs/plans/continuo_design.md#3-25`（なぜ代筆しないのか） | 100% |
| 27 | フロー `レビューの完了` の種別と BRANCH FROM | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 15 | 人間が `Done` へ動かすのは、引き渡したあとのどの時点でも起こりうる。**分岐元はステップ15（利用者がコメントを読んでいる時点）である。**ここが「回答して戻す」か「完了させる」かに分かれる最初の時点である | `docs/plans/continuo_design.md#4-1`、`#3-9` の手順7 | 75% |
| 28 | `レビューの完了` で片付けること | worktree と branch を片付ける | `In Review` に入った issue は巡回の候補から外れるので、**そのあと人間が `Done` へ動かしたことは worktree の身元ファイルの照合以外に知る方法が無い** | `docs/plans/continuo_design.md#3-9` の手順7 | 95% |
| 29 | 対象付きの表明（グループ）を書かなかったこと | rucm ブロックに書かない | 対象付きの行が指す issue は `Ice Box` にあり、`FetchIssueByIdentifier` で別に引く。**グループの扱いは設計 3-26 の独立した話題であり、1件の引き渡しの記述に混ぜると読めなくなる** | `docs/plans/continuo_design.md#3-26` | 75% |
| 30 | 表を2つ足したこと | 「引き渡しが起きる4つの契機」と「人間が戻したあとに何が起きるか」を本文の表にした | 契機が4つあり、そのうち2つ（`max_dispatch_turns` と、画面が止まったままの打ち切り）は別のユースケースが受け持つ。**表が無いと、このユースケースが全部を受け持つように読める** | `docs/plans/continuo_design.md#3-25`、`#4-1` | 85% |
| 31 | 代替フローの POSTCONDITION の書き方 | 「Status」「コメント」「pane」「worktree」を毎回書く | 引き渡しで人間が知りたいのは「どこに置かれたか」と「何が読めるか」である | `docs/plans/continuo_design.md#3-5` | 85% |
| 32 | 引き渡しの契機の4件目の書き方 | 「stall のリトライを使い切った」ではなく「画面が止まったまま打ち切られ、リトライを使い切った」と書いた | 打ち切りの判定は `agent_status` ではなく **herdr の pane の画面の版**で行う。**「stall」だけでは、何を見て止まったと判断したのかが読み手に伝わらない** | `docs/plans/continuo_design.md#3-21`、`internal/orchestrator/reconcile.go` の `checkStalls` | 90% |
| 33 | フロー `知らないStatus` の種別と BRANCH FROM | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 2 | 利用者は turn の途中のどの時点でも Status を動かせる。**分岐元はステップ2（transcript から表明を読む直前）である。**ここが「表明を読んで正しい Status へ戻す」か「知らない Status のまま止める」かに分かれる最初の時点であり、**副作用（pane を閉じる）が始まる直前でもある** | `docs/plans/continuo_design.md#3-50`、`internal/orchestrator/reconcile.go` の `reconcileRunning` | 75% |
| 34 | 「設定に名前の無い Status」の定義 | `active_states` / `terminal_states` / `running_state` / `dispatch_state` / `failure_state` / `status_signal_map` の遷移先のどれにも出てこない Status | **起動時にボードと照合する一覧と同じ集合にする。**別の集合を作ると、起動時に「知らない」と知らせた Status と、実行中に「知らない」と判断する Status がずれる | `docs/plans/continuo_design.md#3-50`、`internal/orchestrator/unknownstate.go` の `knownStates`、`internal/tracker/adapter.go` の `requiredStatesForBootstrap` | 90% |
| 35 | 知らない Status で Status を書き換えないこと | `知らないStatus` の POSTCONDITION に「利用者が動かした選択肢のままである」と書く | `failure_state` へ落とすと、**利用者が自分で動かした Status を continuo が巻き戻すことになる。**判断 14 / 23 と同じ考え方である | `docs/plans/continuo_design.md#3-50`、`#3-4` | 90% |
| 36 | フロー `turnの猶予` の種別 | SPECIFIC ALTERNATIVE FLOW。RFS 知らないStatus 1 | 猶予は `知らないStatus` のステップ1（VALIDATES THAT）の偽ケースである。**任意時点代替フローでは機械照合できない**ので特定代替フローで書いた | rucm スキルの厳密規則9 | 90% |
| 37 | 猶予に上限を置いたこと | `tracker.unknown_state_grace_ms`（既定 600000 ミリ秒）で打ち切る | `claude.turn_timeout_ms` の打ち切りだけに任せると、**画面が変わり続けている限り何時間でも待つ**（判断 32 の測り方）。利用者が「止めたい」と思って Status を動かしても、止まる時刻が誰にも分からない | `docs/plans/continuo_design.md#3-50`、`#3-21` | 80% |
| 38 | 猶予の残りを1つの条件にまとめたこと | ステップ1 を「turn の終わりを待つ猶予が残っていない」の1文にした | 実装の条件は3つ（猶予が0より大きい・経過が猶予の内側・turn ループが走っている）だが、**R12（単文のみ）に反するので rucm ブロックには1文で書き、内訳は本文の表に置いた** | rucm スキルの R12、`internal/orchestrator/unknownstate.go` の `handleUnknownState` | 70% |
| 39 | 待っていることを記録に残すステップを置いたこと | `turnの猶予` のステップ2 | **待つと、利用者が本気で止めたいときに止まるのが turn 1回ぶん遅れる。**その副作用を黙って隠さないために、記録に残すことを仕様として書いた | `docs/plans/continuo_design.md#3-50` | 85% |
