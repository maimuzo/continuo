# 判断ログ: 前提が揃っているかを検査する

- 対象: `docs/spec/usecases/particular_case/前提が揃っているかを検査する.rucm.md`
- 作成日 / 作成モデル: 2026-08-20 / Claude Opus 5
- 参照した根拠資料: `docs/plans/continuo_design.md`（3-32 / 3-6 / 3-34 / 3-15 / 5-2）、`docs/trying_it_out.md`（段5b / 段6）、`internal/doctor/doctor.go`、`internal/doctor/checks.go`、`internal/doctor/report.go`、`internal/ratelimit/keychain.go`、`cmd/continuo/main.go`

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | USE CASE NAME | 前提が揃っているかを検査する | 依頼で指定された名前をそのまま使った。`internal/doctor/doctor.go` のパッケージコメントが「前提が7つあり、どれが欠けても continuo は静かに失敗する。機械的に検査して、足りないものと直し方を人間に出すのがこのパッケージの仕事である」と書いており、名前と実装の担当が一致する | `internal/doctor/doctor.go` のパッケージコメント | 100% |
| 2 | BRIEF DESCRIPTION | 7項目の検査・記号3値・1件失敗しても続ける・✗ があれば終了コード 1 の4点 | この4点が `continuo doctor` を他のサブコマンドと分ける性質である。R12（単文のみ）を守るため5つの短文に分けた | `docs/plans/continuo_design.md` の「3-32」の出力と終了コードの表、`internal/doctor/report.go` の `ExitCode` | 95% |
| 3 | PRECONDITION | 利用者は continuo の実行ファイルを実行できる。実行するマシンの OS は macOS または Linux である | 設定ファイル・herdr・gh のいずれも検査の対象なので事前条件に置けない。置けるのは実行ファイルの存在と、対応 OS だけである。OS の限定は 3-32b が「対応するのは macOS と Linux（WSL2 上の Ubuntu を含む）だけである。Windows ネイティブは外す」と決めている | `docs/plans/continuo_design.md` の「3-32b. Windows ネイティブは対応しない」 | 90% |
| 4 | PRIMARY ACTOR | 利用者 | 3-32 が「`doctor` は人間が明示的に叩くコマンドなので、ボードを読んでよい」と書き、同じ節が `continuo init` の説明で「利用者」を使っている。同じディレクトリの `設定ファイルを作る.rucm.md` も PRIMARY ACTOR を「利用者」にしており、R9（用語の一貫使用）を揃えた | `docs/plans/continuo_design.md` の「3-32」、`docs/spec/usecases/particular_case/設定ファイルを作る.rucm.md` | 90% |
| 5 | SECONDARY ACTORS | herdr、gh、ghq、GitHub、Keychain | `internal/doctor/checks.go` が外部に出す先はこの5つである。`checkHerdr` が herdr の socket、`checkGHAuth` が `gh auth status`、`checkBoard` が GitHub の GraphQL、`checkClone` が `ghq list -p -e`、`checkKeychainCredentials` が `security find-generic-password` を使う。Keychain を数えたのは、応答を待つ相手であり、期限切れという固有の失敗を持つからである。`~/.claude.json` と `~/.claude/.credentials.json` はローカルのファイルなのでアクターに数えなかった | `internal/doctor/checks.go` の `checkHerdr` / `checkGHAuth` / `checkBoard` / `checkClone` / `checkKeychainCredentials`、`internal/ratelimit/keychain.go` の `runSecurity` | 85% |
| 6 | DEPENDENCY | なし | `continuo doctor` は他のユースケースを取り込まない。`continuo init` を案内するのは直し方の文言としてであり、実行はしない（`internal/doctor/checks.go` の `checkConfig` は文字列を返すだけである） | `internal/doctor/checks.go` の `checkConfig` | 95% |
| 7 | GENERALIZATION | なし | 汎化関係にある他のユースケースがない | - | 95% |
| 8 | 配置先ディレクトリ | particular_case | `continuo doctor` は単一目的・単一操作のユースケースであり、複数のユースケースを跨ぐ時系列ではない | `rucm` スキルの粒度ガイド | 95% |
| 9 | 基本フローのステップ分割方針 | 検査1項目につき「外部への要求」「VALIDATES THAT」「見出し語への ✓ の付与」の3ステップに割る | この3つを分けないと、代替フローが VALIDATES THAT を `RFS` で名指しできない。`internal/doctor/doctor.go` の `Run` が検査を段1から段7まで並べており、その並びをそのまま基本フローの順序にした | `internal/doctor/doctor.go` の `Run`（段1から段7のコメント） | 90% |
| 10 | 基本フローの POSTCONDITION | 7件すべてに ✓、検査結果が出力されている、終了コード 0 が返っている | 基本フローはハッピーパスだけなので、7件が全部通った状態を書いた。`docs/trying_it_out.md` の段6 の出力が「前提はすべて揃っています（✓ 7件）」に相当する形を持つ | `internal/doctor/report.go` の `Write`、`docs/trying_it_out.md` の「段6」 | 90% |
| 11 | 検査の順序を7項目でどう並べるか | 設定ファイル、herdr、gh の認証、ボード、clone、信頼登録、資格情報 | `internal/doctor/doctor.go` の `Run` が段1から段7でこの順に呼んでいる。3-32 の依存の図（設定ファイルの下に herdr と gh の認証、gh の認証の下にボード、ボードの下に clone と信頼登録）とも一致する | `internal/doctor/doctor.go` の `Run`、`docs/plans/continuo_design.md` の「3-32」の依存関係の図 | 100% |
| 12 | VALIDATES THAT（基本フロー 3）接続先の環境変数 | 接続先を差し替える環境変数の値が検証を通る | `cmd/continuo/main.go` の `runDoctor` が `daemon.ValidateGraphQLEndpoint` を呼び、通らなければ検査を1件も行わずに終了コード 3 を返す。7項目の検査より前にある唯一の門なので、基本フローの先頭側に置いた | `cmd/continuo/main.go` の `runDoctor`、`doctorInternalErrorExitCode` | 90% |
| 13 | 接続先の検査をこのユースケースに含めるか | 含める | 検査の実体は `internal/doctor` だが、利用者から見える `continuo doctor` の挙動の一部であり、終了コード 3 という他と区別できる結果を持つ。ユースケースは実装のパッケージ境界ではなく利用者から見た1操作で切るべきだと判断した | `cmd/continuo/main.go` の `runDoctor` | 70% |
| 14 | VALIDATES THAT（基本フロー 5）設定ファイル | 設定ファイルの front matter が検証を通る | `internal/doctor/checks.go` の `checkConfig` が `config.Load` の成否だけで ✓ と ✗ を分ける。3-32 の表も「`WORKFLOW.md` が読めて、front matter が検証を通るか」と書いている | `internal/doctor/checks.go` の `checkConfig` | 100% |
| 15 | 設定ファイルの場所を決められない場合の扱い | 基本フロー 2 の後、設定ファイルの不備に吸収する | `cmd/continuo/main.go` の `runDoctor` は `config.ResolvePath` が失敗しても警告を出して検査を続け、空のパスが `config.Load` で落ちて ✗ になる。独立した代替フローにすると、実装に無い分岐をテストに展開してしまう | `cmd/continuo/main.go` の `runDoctor` | 85% |
| 16 | VALIDATES THAT（基本フロー 8）herdr | ping の応答の protocol が設定の herdr.protocol と一致する | `internal/doctor/checks.go` の `checkHerdr` が `herdr.New(...).CheckProtocol` の戻り値だけで判定する。3-32 の表が「socket の `ping` を呼び、応答の `protocol` が設定の `herdr.protocol` と一致するか。`herdr status` の CLI は使わない」と決めている | `internal/doctor/checks.go` の `checkHerdr`、`docs/plans/continuo_design.md` の「3-32」 | 100% |
| 17 | VALIDATES THAT（基本フロー 11）gh の認証 | github.com の有効なアカウントの Token scopes に project が単独の要素として並んでいる | 3-32 の「`gh auth status` の読み方を1つに決める」の表がホスト固定・`Active account: true` のブロック限定・カンマ分割・`project` が単独要素、を全部決めている。`read:project` は不可である | `docs/plans/continuo_design.md` の「3-32」、`internal/doctor/checks.go` の `checkGHAuth` | 100% |
| 18 | gh が PATH に無い場合を独立の代替フローにするか | しない。ghの認証の不足に含める | `internal/doctor/checks.go` の `checkGHAuth` は `tracker.CheckGHAvailable` の失敗も `tracker.CheckGHProjectScope` の失敗も同じ ✗ にし、見出し語も同じである。分けるとテストパスだけが増えて結果が同じになる | `internal/doctor/checks.go` の `checkGHAuth` | 85% |
| 19 | VALIDATES THAT（基本フロー 14）ボード | 設定の active_states の選択肢名がボードにすべてある | `internal/doctor/checks.go` の `checkBoard` が `adapter.Bootstrap` の成否で分ける。3-6 が「合わないと GraphQL がエラーを出さずに 0 件を返し、キューが永久に止まる」と書いており、この照合が検査の核である | `internal/doctor/checks.go` の `checkBoard`、`docs/plans/continuo_design.md` の「3-6」 | 95% |
| 20 | VALIDATES THAT（基本フロー 18）対象リポジトリの件数 | 対象リポジトリが1件以上ある | 3-32 の「対象リポジトリが0件だったとき」の表が clone と信頼登録を `!` にし、終了コードに影響させないと決めている。0件は他の失敗と結果が違うので、独立した条件ステップにした | `docs/plans/continuo_design.md` の「3-32」、`internal/doctor/checks.go` の `checkClone` / `checkTrust` | 90% |
| 21 | VALIDATES THAT（基本フロー 20）clone | 対象リポジトリのすべての clone が手元にある | `internal/doctor/checks.go` の `checkClone` が `opts.GhqList` の出力が空かどうかで判定する。3-32 が「exit code は存在の有無にかかわらず 0 を返す（実測）。出力の有無で判定する」と決めている | `internal/doctor/checks.go` の `checkClone`、`docs/plans/continuo_design.md` の「3-32」 | 95% |
| 22 | VALIDATES THAT（基本フロー 23）信頼登録 | clone を引けた対象リポジトリのすべての絶対パスが承認済みである | `internal/doctor/checks.go` の `checkTrust` は clone のパスが無いリポジトリを「確かめられない」として `!` に数え、承認の判定には掛けない。したがって条件の対象を「clone を引けた対象リポジトリ」に限った | `internal/doctor/checks.go` の `checkTrust` | 90% |
| 23 | 信頼登録を引く鍵 | clone の絶対パス | 3-6 が「worktree のパスで引いてはならない。信頼はリポジトリ単位で記録されるので、必ず『未承認』になる」と書いている。3-32 も「`ghq list --exact` が返した clone の絶対パス。worktree のパスではない」と重ねて決めている | `docs/plans/continuo_design.md` の「3-6」「3-32」、`internal/workspace/trust.go` の `CheckTrustForClonePath` | 100% |
| 24 | 資格情報の分岐を基本フローの IF に置いた判断 | IF-ELSEIF-ELSE-ENDIF を基本フロー 25 から 33 に置く | 3分岐（`rate_limit.source` が none / `token_source` が env / `token_source` が claude_credentials）はどれも成功しうる「どの分岐でも正常」の内部分岐であり、`rucm_structure.md` が基本フロー内の IF を認めている。失敗する2つだけを代替フローに出した | `internal/doctor/checks.go` の `checkCredentials`、`rucm` スキルの `references/rucm_structure.md` | 85% |
| 25 | VALIDATES THAT（基本フロー 28）資格情報の環境変数 | rate_limit.token_env が指す環境変数に空でない値が入っている | `internal/doctor/checks.go` の `checkCredentials` は `opts.LookupEnv` の戻り値が存在しかつ空文字でないことを合格にする。空文字を合格にしない点を条件文に入れた | `internal/doctor/checks.go` の `checkCredentials` | 95% |
| 26 | VALIDATES THAT（基本フロー 31）資格情報のファイル | 資格情報のファイルが手元にある | `internal/doctor/checks.go` の `checkCredentials` が `os.Stat` の成否だけで判定し、中身は読まない。3-32 が「無ければ、その先は確かめない」と決めている | `internal/doctor/checks.go` の `checkCredentials`、`internal/ratelimit/ratelimit.go` の `CredentialsRelPath` | 95% |
| 27 | VALIDATES THAT（基本フロー 35）終了コードの判定 | 7件の記号のすべてが ✓ または ! である | `internal/doctor/report.go` の `ExitCode` が「`✗` が1件でもあれば 1、それ以外は 0」で返す。R13（否定的な副詞・形容詞の回避）に沿って「✗ が1つもない」ではなく肯定形で書いた | `internal/doctor/report.go` の `ExitCode` | 95% |
| 28 | 検査結果の出力と終了コードの順序 | 出力（34）が先、終了コードの判定（35）が後 | `cmd/continuo/main.go` の `runDoctor` が `report.Write(stdout)` を呼んでから `report.ExitCode()` を返す。R2（時系列順）に沿ってこの順に並べた | `cmd/continuo/main.go` の `runDoctor` | 100% |
| 29 | 代替フロー 接続先の指定が不正 | ABORT、終了コード 3 | `cmd/continuo/main.go` の `runDoctor` は `daemon.ValidateGraphQLEndpoint` が落ちると `doctorInternalErrorExitCode`（3）で戻り、`doctor.Run` を1度も呼ばない。検査結果が1件も出ないので RESUME できず ABORT になる | `cmd/continuo/main.go` の `runDoctor`、`doctorInternalErrorExitCode` | 95% |
| 30 | 代替フロー 設定ファイルの不備 | 設定ファイルに ✗、残り6件に !、RESUME STEP 39 | `internal/doctor/doctor.go` の `Run` は設定が読めなくても段2から段7を全部呼び、各検査が `configSymbol != SymbolOK` で `!` を返す。`docs/trying_it_out.md` の段6 に「7件に問題があります（✗ 1件 / ! 6件）」の実出力がある | `docs/trying_it_out.md` の「段6」、`internal/doctor/checks.go` の `checkHerdr` / `checkGHAuth` / `checkBoard` / `checkCredentials` | 95% |
| 31 | 代替フロー herdrの不通 | IF で直し方を分け、herdr に ✗、RESUME STEP 10 | `internal/doctor/checks.go` の `checkHerdr` は `ping` が nil でない（socket には届いた）ときだけ「herdr.protocol を合わせる」を直し方にし、それ以外は「herdr が待ち受けているかを確認する」を出す。直し方が変わるので IF を代替フロー内に置いた | `internal/doctor/checks.go` の `checkHerdr` | 90% |
| 32 | 代替フロー ghの認証の不足 | gh の認証に ✗、ボードと clone と信頼登録に !、RESUME STEP 25 | 3-32 の依存の図で gh の認証の下流はボード、その下流が clone と信頼登録である。`internal/doctor/checks.go` の `checkBoard` は `ghSymbol != SymbolOK` で `!` を返し、`checkClone` と `checkTrust` も `boardSymbol != SymbolOK` で `!` を返す。資格情報は飛ばさないので RESUME 先を 25 にした | `docs/plans/continuo_design.md` の「3-32」、`internal/doctor/checks.go` の `checkBoard` / `checkClone` / `checkTrust` | 95% |
| 33 | 代替フロー ボードの読み取り失敗 | IF でレートリミットだけ !、それ以外は ✗、RESUME STEP 25 | 3-32 の「ボードを読めなかったときの記号」の表が、project が見つからない・トークンの取り出しに失敗・選択肢名の不一致を ✗、レートリミットだけを `!` と決めている。`internal/doctor/checks.go` の `boardFailure` が同じ分け方を実装している | `docs/plans/continuo_design.md` の「3-32」、`internal/doctor/checks.go` の `boardFailure` | 95% |
| 34 | 代替フロー 対象リポジトリが0件 | clone と信頼登録に !、RESUME STEP 25 | 3-32 の「対象リポジトリが0件だったとき」の表が両方を `!` にし、「終了コードに影響しない。ボードが空なのは設定の誤りではない」と決めている。`docs/trying_it_out.md` の段6 の実出力もこの形である | `docs/plans/continuo_design.md` の「3-32」、`docs/trying_it_out.md` の「段6」 | 100% |
| 35 | 代替フロー cloneの不在 | clone に ✗、RESUME STEP 22 | `internal/doctor/doctor.go` の `Run` は段5 の clone が ✗ でも段6 の信頼登録を呼ぶ。信頼登録は clone のパスがあるリポジトリだけを判定し、無いものを `!` に数える。だから ABORT でも 25 でもなく 22 へ戻す | `internal/doctor/doctor.go` の `Run`、`internal/doctor/checks.go` の `checkTrust` | 90% |
| 36 | 代替フロー 信頼登録の不足 | 信頼登録に ✗、RESUME STEP 25 | `internal/doctor/checks.go` の `checkTrust` が未承認のリポジトリごとに「Claude Code で一度開き、信頼のダイアログを承認してください」を直し方に積む。次の検査は資格情報なので 25 へ戻す | `internal/doctor/checks.go` の `checkTrust` | 95% |
| 37 | 代替フロー 資格情報の環境変数の不在 | 資格情報に ✗、RESUME STEP 39 | 3-32 の「資格情報の記号を、設定が読めたかどうかで分ける」の表が、`token_source` が env で環境変数が無い場合を ✗ にしている。資格情報は最後の検査なので出力の 39 へ戻す | `docs/plans/continuo_design.md` の「3-32」、`internal/doctor/checks.go` の `checkCredentials` | 95% |
| 38 | 代替フロー 資格情報のファイルの不在 | 資格情報に !、macOS なら keychain への切り替えを直し方に添える、RESUME STEP 39 | `token_source` が `claude_credentials` でファイルが無い状態は「確かめられなかった」であって、足りないわけではない（3-32 の表）。`internal/doctor/checks.go` の `checkCredentials` は `runtime.GOOS == "darwin"` のときだけ「rate_limit.token_source を keychain にすると Keychain から読めます」を直し方に足すので、IF を代替フロー内に置いた | `docs/plans/continuo_design.md` の「3-32」、`internal/doctor/checks.go` の `checkCredentials` | 95% |
| 39 | 代替フロー 足りない前提がある | 終了コード 1、ABORT | `internal/doctor/report.go` の `ExitCode` が ✗ を1件でも見つけたら 1 を返す。検査結果は 39 で出力済みなので、この代替フローは終了コードを返して終わるだけである | `internal/doctor/report.go` の `ExitCode` | 95% |
| 40 | 任意時点代替フロー 検査の期限切れの WHEN | 検査の期限が切れた場合 | `internal/doctor/doctor.go` は全体に `DefaultTimeout`（30秒）、外部に触る検査1件に `DefaultCheckTimeout`（10秒）を掛ける。`timedOut` が真になった検査は ✗ ではなく `!` になる。どの外部呼び出しでも起きうるので任意時点代替フローにした | `internal/doctor/doctor.go` の `DefaultTimeout` / `DefaultCheckTimeout` / `withCheckTimeout` / `timedOut` | 95% |
| 41 | BRANCH FROM の分岐元 | BASIC FLOW 13（GitHub に project と Status フィールドの解決を要求する）1点だけ | 最悪のタイミングを選ぶ規則に従った。この時点の期限切れは、下流の clone と信頼登録の両方を `!` に落とし、7件のうち3件が確かめられないまま報告が出る。得られる情報が最も少なくなる一方で、外部への要求は最も多く消費している（`internal/doctor/doctor.go` の `Run` は段4 だけ期限を2倍にしており、2リクエスト送ることが分かる）。herdr や gh の期限切れは1件だけが `!` になり、影響が小さい | `internal/doctor/doctor.go` の `Run`（段4 のコメント「ここだけ期限を2倍にする」）、`rucm` スキルの `references/rucm_structure.md` の選定基準 | 80% |
| 42 | 検査の期限切れの終端 | RESUME STEP 39 | `internal/doctor/doctor.go` の `Run` は期限が切れても Report を返し、`cmd/continuo/main.go` の `runDoctor` がそれを出力する。`!` だけなら終了コードは 0 なので ABORT ではなく出力へ戻す | `internal/doctor/doctor.go` の `Run`、`internal/doctor/report.go` の `ExitCode` | 95% |
| 43 | 代替フローの終端を ABORT と RESUME でどう使い分けたか | 検査を続けられるものは RESUME、続けられないものだけ ABORT | 3-32 の「1つ失敗しても残りを全部検査する。最初の失敗で止めない」を守るため、7項目の検査中の失敗はすべて RESUME にした。ABORT にしたのは、検査を1件も行えない 接続先の指定が不正 と、全検査が終わったあとの 足りない前提がある の2つだけである | `docs/plans/continuo_design.md` の「3-32」、`internal/doctor/doctor.go` の `Run` | 95% |
| 44 | 代替フローの POSTCONDITION の書き方 | 見出し語ごとに付いた記号と、検査結果に載る内容を書く。終了コードは ABORT する2本にだけ書く | RESUME する代替フローは基本フローへ戻るので、その時点で終了コードは決まっていない。決まっていない値を POSTCONDITION に書くとテストが検証できない | `internal/doctor/report.go` の `ExitCode` | 90% |
| 45 | 用語の統一（R9 / R16） | 見出し語 / 記号 / 直し方 / 対象リポジトリ / 検査結果 | `internal/doctor/report.go` が「見出し語」を定数コメントで使い、`Result` の `Detail` と `Remedies` を設計 3-32 が「直し方」と呼んでいる。実装とプランファイルで使われている語をそのまま採り、訳し直さなかった | `internal/doctor/report.go` の `Label` 定数群、`docs/plans/continuo_design.md` の「3-32」 | 95% |
| 46 | 見出し語7つの表記 | 設定ファイル / herdr / gh の認証 / ボード / clone / 信頼登録 / 資格情報 | `internal/doctor/report.go` の `LabelConfig` から `LabelCredentials` までの定数の値をそのまま使った。同ファイルが「出力に出る語をここ以外で組み立ててはならない」と書いている | `internal/doctor/report.go` の `LabelConfig` / `LabelHerdr` / `LabelGHAuth` / `LabelBoard` / `LabelClone` / `LabelTrust` / `LabelCredentials` | 100% |
| 47 | フロー名にスペースを入れない対処 | herdrの不通 / ghの認証の不足 / cloneの不在 | フロー名は空白・タブ・コロン・ハイフンを含められない（`rucm_structure.md` のフロー名規約）。見出し語の「gh の認証」をそのままフロー名にすると空白が入るため、フロー名からだけ空白を落とした。ステップ本文と POSTCONDITION では見出し語の表記を保っている | `rucm` スキルの `references/rucm_structure.md` のフロー名規約 | 95% |
| 48 | mermaid フローチャートの作り方 | ノードを先に全部宣言してからエッジを書く | 代替フローから基本フローへ戻るエッジが多く、前方参照でノードを作るとひし形の指定が後から当たる。宣言を先に置いて形を確定させた。任意時点代替フローは点線のエッジに WHEN 条件を載せて表した | `rucm` スキルのテンプレート | 90% |
| 49 | mermaid シーケンス図の作り方 | 利用者・システム・herdr・gh・GitHub・ghq・Keychain の7者で、成否を alt で入れ子にする | SECONDARY ACTORS の5つをそのまま participant にした。検査が上流から下流へ入れ子になる依存を alt の入れ子で表し、資格情報だけは依存の外にあるので入れ子の外へ出した | `internal/doctor/doctor.go` の `Run`、`docs/plans/continuo_design.md` の「3-32」の依存関係の図 | 90% |
| 50 | 本番のボードへの書き込みを含めるか | 含めない | `internal/doctor/doctor.go` のパッケージコメントが「本番のボードへ書き込む経路は1つも無い。このパッケージが呼ぶのは読み取りだけである（Bootstrap と FetchIssuesByStates）」と書いている。ステップにも書き込みを1つも置いていない | `internal/doctor/doctor.go` のパッケージコメント | 100% |
| 51 | 資格情報の分岐を3本にするか2本にするか | `none` / `keychain` / `env` / `claude_credentials` の4本に割る | `internal/config/validate.go` が `rate_limit.token_source` に `claude_credentials` / `keychain`（macOS のみ）/ `env` の3値だけを許している。`internal/doctor/checks.go` の `checkCredentials` は `SourceNone` → `TokenSourceKeychain` → `TokenSourceEnv` → それ以外（`claude_credentials`）の順に見るので、基本フローの IF / ELSEIF もその順に並べた | `internal/config/validate.go`、`internal/doctor/checks.go` の `checkCredentials` | 95% |
| 52 | Keychain の読み取りを VALIDATES THAT 2本に割った理由 | 「期限内に応答する」と「空でない accessToken を取り出せる」の2本 | 期限切れだけ記号が変わる（`!`）ので、1本の VALIDATES THAT では代替フローを分けられない。`internal/doctor/checks.go` の `checkKeychainCredentials` が `errors.Is(err, ratelimit.ErrKeychainTimeout)` を先に見て `SymbolUnknown` を返し、それ以外の失敗と `accessToken` の不在を `SymbolMissing` にまとめている。この分け方をそのままステップに写した | `internal/doctor/checks.go` の `checkKeychainCredentials`、`internal/ratelimit/keychain.go` の `ErrKeychainTimeout` | 95% |
| 53 | 代替フロー Keychainの応答の期限切れ | 問い合わせを打ち切り、資格情報に !、RESUME STEP 39 | 3-15 が「上限で `security` を殺し、枠の判定を捨てる」と決め、3-32 が「返らなかっただけで、資格情報が無いとは限らない」ので `!` にすると決めている。POSTCONDITION に「Keychain への問い合わせは打ち切られている」を置き、無人運用が固まらないことをテストで確かめられるようにした | `docs/plans/continuo_design.md` の「3-15」「3-32」、`internal/ratelimit/keychain.go` の `runSecurity` | 95% |
| 54 | 代替フロー Keychainの資格情報の不在 | 資格情報に ✗、`continuo allow-keychain-access` を直し方に添える、RESUME STEP 39 | 3-32 が「利用者が `keychain` を明示して選んだのに取れていない」ので ✗ にすると決め、`token_source: env` で環境変数が無いときと同じ扱いにそろえている。POSTCONDITION に「読み取った値そのものは検査結果に載っていない」を置いたのは、`internal/ratelimit/keychain.go` が標準出力をエラー文へ載せない設計だからである | `docs/plans/continuo_design.md` の「3-32」、`internal/doctor/checks.go` の `checkKeychainCredentials`、`internal/ratelimit/keychain.go` の `securityStderr` | 95% |
