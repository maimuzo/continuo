# 判断ログ: レートリミットで待って再開する

- 対象: `docs/spec/usecases/particular_case/レートリミットで待って再開する.rucm.md`
- 作成日 / 作成モデル: 2026-08-20 / Claude Opus 5 (1M context)
- 参照した根拠資料: `docs/plans/continuo_design.md`（3-11 / 3-15 / 3-21 / 3-27 / 5-2）、`internal/orchestrator/turn.go`、`internal/orchestrator/reconcile.go`、`internal/orchestrator/orchestrator.go`、`internal/ratelimit/ratelimit.go`

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | USE CASE NAME | レートリミットで待って再開する | 依頼で指定された名前をそのまま使う。動詞で終わる名詞句の規則も満たす。**「レートリミット」は設計で使っている語をそのまま使い、日本語へ直訳しない** | 依頼文、`docs/plans/continuo_design.md#3-27` | 100% |
| 2 | 配置ディレクトリ | `particular_case/` | 「枠を使い切った run を待たせて再開する」という単一目的の操作単位である | rucm スキルの粒度ガイド | 95% |
| 3 | BRIEF DESCRIPTION | 枠を読む・印を立てて時計を止める・リセット時刻を過ぎたら印を外す・agent_status を見て継続の指示を送る の4文 | 設計の「枠に当たってから復旧するまでの流れ」の図を4文へ落とした | `docs/plans/continuo_design.md#3-27` | 90% |
| 4 | PRECONDITION | 常駐している。走行中の run がある。`rate_limit.source` が none ではない。Status が running_state である | `rate_limit.source: none` を指定すると usage API を1回も叩かないので、このユースケースが成立しない | `docs/plans/continuo_design.md#3-27`、`internal/ratelimit/ratelimit.go` | 90% |
| 5 | PRIMARY ACTOR | 巡回タイマー | 枠を読むのは巡回であり、`rate_limit.poll_interval_ms`（既定5分）に1回である。**人間は関与しない** | `docs/plans/continuo_design.md#3-27`、`internal/orchestrator/orchestrator.go` の `pollQuota` | 75% |
| 6 | SECONDARY ACTORS | Claude の usage API、herdr、Claude Code | 枠を読む相手・`agent_status` を引く相手・継続の指示を送る相手である。**ボードは書かないので GitHub Projects v2 は挙げない** | `docs/plans/continuo_design.md#3-27` | 85% |
| 7 | DEPENDENCY | なし | 他の particular_case を取り込まない | rucm スキルのファイル規約 | 95% |
| 8 | GENERALIZATION | なし | 汎化関係にあるユースケースが無い | - | 90% |
| 9 | ステップ分割方針 | 「読む」「止める」「待つ」「外す」「送る」を1動作ずつのステップに割る | R4（1文1動作）。**stall の時計と turn の時間切れの時計は別の判定なので、止める操作も別ステップにした** | `docs/plans/continuo_design.md#3-27` | 90% |
| 10 | ステップ2（VALIDATES THAT） | 枠の使用率とリセット時刻を読み取れる | 読めない場合は枠待ちと無音を区別できないので、代替フローで stall 検知に落とす | `docs/plans/continuo_design.md#3-27`（取れなかったときにどうするか） | 95% |
| 11 | ステップ3 から5（IF-ENDIF） | 使用率が `pause_above_percent` を超えていたら新規の dispatch を止める | **どちらの分岐も正常である**ので基本フローの `IF` で書いた。走行中の turn は止めない | `docs/plans/continuo_design.md#3-27`、`internal/orchestrator/orchestrator.go` の `dispatchPaused` | 95% |
| 12 | ステップ6（VALIDATES THAT） | 走行中の run が `turn_timeout_ms` のあいだ hook を1件も受けていない | 枠待ちの2条件のうちの条件その2 である。**枠を使い切っていても別の run は動いていることがある**ので、枠の状態だけで全部の run の時計を止めてはならない | `docs/plans/continuo_design.md#3-27`、`internal/orchestrator/turn.go` の `isQuotaWaiting` | 100% |
| 13 | ステップ7（VALIDATES THAT） | 使用率が 100 に達している枠がある | 枠待ちの2条件のうちの条件その1 である。**`pause_above_percent`（既定95%）を超えただけでは枠待ちとみなさない**（95%は枠がまだ残っている状態で、走行中の worker は普通に動ける） | `docs/plans/continuo_design.md#3-27`、`internal/orchestrator/turn.go` の `quotaAtFull` | 100% |
| 14 | ステップ6 と7 の順序 | hook の無音を先に見て、枠の使用率をあとに見る | 設計の評価順（打ち切りの閾値に達した run について上から順に見る）に合わせた。**どちらが先でも判定は同じだが、順序を固定しないとテストが決まらない** | `docs/plans/continuo_design.md#3-27`（打ち切りの閾値に達したときの評価順） | 70% |
| 15 | ステップ8 から10 | 印を立て、打ち切りの判定を飛ばし、最後に動いていた時刻を進めない | **`LastSeenAt` を進めてはならない。**進めると、枠が明けたあとに「最後に動いていた時刻」が分からなくなる。だから「印を立てて判定を飛ばす」と書いた | `docs/plans/continuo_design.md#3-27`（「時計を止める」の実装） | 95% |
| 16 | ステップ10 の中身 | 「run の最後に動いていた時刻を進めない」と書いた | **打ち切りの物差しは `claude.turn_timeout_ms` の1本しかない**ので、「stall の判定」と「turn の時間切れの判定」を別々に飛ばす書き方はできない。**時計を止める実装は `LastSeenAt` を進めないことである** | `docs/plans/continuo_design.md#3-21`、`#3-27`（「時計を止める」の実装） | 95% |
| 17 | ステップ11 から13（DO-UNTIL） | リセット時刻を過ぎるまで巡回のたびに見比べる | 枠待ち中は hook が来ないので、**印を外す契機は「枠の `resets_at` を過ぎたこと」だけである** | `docs/plans/continuo_design.md#3-27` | 95% |
| 18 | どの枠のリセット時刻を見るか | 使用率が 100 に達した枠のうち、`resets_at` がいちばん遅いもの | `resets_at` が null の枠は判定から外す。`weekly_scoped` もモデルを判別せずそのまま見る（continuo は Claude Code が使うモデルを知らない） | `docs/plans/continuo_design.md#3-27`、`internal/orchestrator/turn.go` の `quotaResetAt` | 95% |
| 19 | ステップ15 | 打ち切りの時計を現在時刻に進める | 印を外した直後に古い `LastSeenAt` のままだと、次の巡回で即座に打ち切られる | `docs/plans/continuo_design.md#3-27` | 90% |
| 20 | ステップ17（VALIDATES THAT） | agent_status が idle または done である | Claude Code 2.1.234 は枠のリセット時にセッションを自動継続する。**continuo が枠明けに継続の指示を送ると二重投入になり、投げた本文が消えて turn が混ざる** | `docs/plans/continuo_design.md#3-27`（2.1.234 の自動継続）、`internal/orchestrator/turn.go` の `afterQuotaReset` | 100% |
| 21 | ステップ19 | この継続の指示を turn 数に数える | 数えないと、枠待ちと復帰を繰り返す間に打ち切りが一度も発火せず、同じ issue に無限に turn を消費する | `docs/plans/continuo_design.md#3-27` | 100% |
| 22 | 基本フローの POSTCONDITION | 印が外れている。打ち切りの判定が効いている。turn 数が1つ増えている。Status が running_state のまま。pane が閉じていない | 「待って再開した」状態を、次の巡回の振る舞いを決める値だけで書いた | `docs/plans/continuo_design.md#3-27` | 90% |
| 23 | フロー `枠を読めない` | RFS BASIC FLOW 2。打ち切りの検知に落として pane を閉じ、リトライを積む | usage API が使えない場合は枠待ちと固まりを区別できない。**枠が回復していなければリトライも同じところで止まり、回数を使い切ったら人間へ渡す** | `docs/plans/continuo_design.md#3-27`（取れなかったときにどうするか） | 95% |
| 24 | フロー `応答のあるrun` | RFS BASIC FLOW 6。時計を止めずに ABORT | 条件その2 を満たさない run は、枠を使い切っていても動いている。**時計を止めると、固まった run を見逃す** | `docs/plans/continuo_design.md#3-27`（条件その2 を入れる理由） | 95% |
| 25 | フロー `枠の残り` | RFS BASIC FLOW 7。画面の版が変わっていれば待ち続け、変わっていなければ pane を閉じてリトライを積む | 設計の評価順の2番目と3番目をそのまま写した。**`agent_status` を根拠にしない**（`working` のまま固まる場合があるため）。画面の版は「本当に動いているか」を直接示す | `docs/plans/continuo_design.md#3-27`、`#3-21`、`internal/orchestrator/reconcile.go` の `checkStalls` | 95% |
| 26 | `枠の残り` に IF-ELSE を入れたこと | 代替フローの中で `IF-ELSE-ENDIF` を使った | 待ち続ける場合と worker を止める場合の**どちらも正常な処理**であり、さらに代替フローを分けると分岐元が条件ステップでなくなる | rucm スキルの厳密規則11（`IF-ELSE-ENDIF` は同一フロー内で完結させる） | 85% |
| 27 | フロー `自動継続` | RFS BASIC FLOW 17。継続の指示を送らずに RESUME STEP 20 | `working` は Claude Code が自分で継続している状態である。**hook を待つ** | `docs/plans/continuo_design.md#3-27` | 95% |
| 28 | `blocked` を基本フローに書かなかったこと | `自動継続` の説明の表にだけ書いた | `blocked` の扱い（esc を送ってから failure_state へ落とす）は `issue を1件処理する` の `権限の確認` が受け持つ。**同じ処理を2つのユースケースに二重に書かない** | `docs/spec/usecases/particular_case/issue を1件処理する.rucm.md` | 80% |
| 29 | フロー `応答の途絶` の種別と BRANCH FROM | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 18 | 継続の指示を送ったあと応答が返らない事象はどの時点でも起こりうる。**分岐元はステップ18（継続の指示を送った直後）である。**ここは「枠は明けたのに worker が死んでいる」ことが確定する時点であり、放置すると誰も止めない | `docs/plans/continuo_design.md#3-27`（応答が返れば継続、返らなければ worker を止めて再 dispatch） | 80% |
| 30 | `応答の途絶` で worktree を残すこと | pane を閉じ、worktree を残して再 dispatch の対象にする | 再開の質は「issue のコメントに残した成果を次のセッションが読む」で担保する。**worktree を消すと成果が失われる** | `docs/plans/continuo_design.md#3-27`（再開の質） | 90% |
| 31 | 表を2つ足したこと | 「2つの判定を分ける」と「枠明けに継続の指示を送ってよいか」を本文の表にした | 設計の急所が「閾値の超過」と「枠待ち」を混同しないことなので、rucm ブロックの外に条件を並べて対比させた | `docs/plans/continuo_design.md#3-27` | 85% |
| 32 | 使用量の読み取りが定額運用に反しないこと | 本文に書かず、判断ログにだけ残す | この API は枠の残量とリセット時刻を返すだけで、メッセージを送る API ではない。**ただし「1トークンも消費しない」ことは確かめられていない**（`percent` は整数の百分率なので、少量の消費では動かない） | `docs/plans/continuo_design.md#3-27` | 60% |
| 33 | `CLAUDE_CODE_RETRY_WATCHDOG` を書かなかったこと | rucm ブロックに書かない | これは turn の途中で `429` が返ったときの API リクエストのリトライであり、**このユースケースが扱う「枠でセッションが止まったあと」とは別の仕組みである**。設定を渡すのは着手の段5（設定ファイルの env）である | `docs/plans/continuo_design.md#3-11`、`#3-27` | 90% |
| 34 | 代替フローの POSTCONDITION の書き方 | 「枠待ちの印」「打ち切りの判定」「pane」「Status」を毎回書く | 枠待ちのユースケースで人間が知りたいのは「時計が止まっているか」と「worker が生きているか」である | `docs/plans/continuo_design.md#3-27` | 85% |
| 36 | フロー `待つ上限を超えた` の種別と BRANCH FROM | GLOBAL ALTERNATIVE FLOW。BRANCH FROM BASIC FLOW 12 | **SPECIFIC ALTERNATIVE FLOW にすると RFS の先が `UNTIL` になる。**`UNTIL` の偽側は繰り返しが既に占めていて、CFG の変換規則に代替フローへの但し書きが無い。**BRANCH FROM なら基本フローに1段も足さないので、以降のステップ番号も動かない** | `docs/plans/continuo_design.md#3-27`（1週間の枠が明けるのを待つ上限）、同ファイルのフロー `応答の途絶`（条件ステップ以外から張っている前例） | 85% |
| 37 | `待つ上限を超えた` で after_run を走らせること | 走らせる | **人間の決定は「push して引き渡す」であり、continuo の push の手段は `workspace_hooks.after_run` だけである**（continuo 自身が `git push` を叩く経路は無い）。**担当を失ったとき（3-77h）は走らせないが、あちらは担当が既に別の機械へ移っていて push が衝突する。こちらは自分から手放す側で、次の担当はまだ決まっていない** | `docs/plans/continuo_design.md#3-27`、`#3-77h`、2026-08-26 の人間の決定 | 80% |
| 38 | `待つ上限を超えた` のステップの順序 | 画面の版を見る → 担当者を読み直す → after_run → 担当者を外す → pane を閉じる | **担当者を外せなかったときに pane だけ閉じると、担当がこの機械のまま誰も動かない issue になる。**先に外し、外せてから閉じる。**さらに、後ろの3段へ入る前に2つ確かめる。**画面の版が増えていれば「枠を待っている」のではなく「長い1つの仕事をしている」ので手放さない。担当者が自分でなければ 3-77c の「担当を外された機械は、その branch へ push してはならない」に当たるので、`after_run` を走らせない | `docs/plans/continuo_design.md#3-27`（順番を入れ替えてはならない）、`#3-77c` | 90% |
| 39 | 基本フローの POSTCONDITION を書き換えなかったこと | 代替フローの側に独自の POSTCONDITION を置いた | **基本フローの「pane は閉じていない」は、正常に枠が明けて続いた場合の事後条件である。**書き換えると、その場合の断言まで壊れる | rucm スキルの代替フローの規約 | 90% |
| 40 | 手放す前に画面の版を見ること | `待つ上限を超えた` の先頭に置いた | **枠待ちの条件は「使用率が100」と「hook が来ていない」の2つで、「枠を待っている」と「長い1つの仕事をしている」を区別できない。**hook はツールが終わってから飛ぶので、**1時間を超える1回のツール呼び出しの最中は1件も来ない。**そこへ `weekly_scoped`（1週間のモデル別の枠）が100%だと条件が両方そろい、**正常に走っている run を殺して pane を閉じる。**区別できる道具は画面の版である。**専用の仕組みは作らない。**`checkStalls` の評価順で画面の版を枠待ちより前に置けば、既存の `noteRevision` だけで全部の run に効く | `docs/plans/continuo_design.md#3-27`（stall の閾値に達したときの評価順）、`#3-21` | 90% |
| 41 | 手放す前に担当者を読み直すこと | 画面の版の次に置いた | **枠待ちのあいだ、担当は自分の意思と無関係に外れる。**`idle_timeout_ms` は「担当者の最後の進捗報告から」で数え、**枠待ち中は hook が来ないので進捗のコメントも増えない。**既存の確かめ（turn の終わり）は**枠待ちの run に一度も来ない。**確かめずに `after_run` を走らせると、利用者が書いた `git push` が別の機械の branch へ飛ぶ | `docs/plans/continuo_design.md#3-77c`（担当を外された機械は push してはならない）、`#3-27` | 90% |
| 42 | 確かめられなかったときの倒し方 | 画面の版は「手放す側」へ、担当は「手放さない側」へ倒す | **向きが逆なのは、間違えたときの損が逆だからである。**画面の版を読めないのは worker が死んでいる兆候で、既存の打ち切りも同じ向きに倒している。**担当を読めないのは `gh` に一度届かなかっただけのことが多く、そこで手放すと走っている run が捨てられる** | `docs/plans/continuo_design.md#3-27`、`internal/orchestrator/reconcile.go` の `checkStalls`、`internal/orchestrator/handoff.go` の `verifyHandoff` | 85% |
| 35 | 待ち時間を測るキーを1つにしたこと | `stall_timeout_ms` は書かず、`turn_timeout_ms` だけを書いた | 仕様が2つのキーを分けるのは、Codex に観測点が2つある（app-server の turn のストリームと、orchestrator が受け取るイベント）からである。**continuo の観測点は herdr の pane の画面の版1つしかなく、同じ時計に閾値を2つ置くと小さいほうしか効かない** | `docs/plans/continuo_design.md#3-21`、`#8-4`（`stall_timeout_ms` を実装しない理由）、`internal/config/types.go` の `TurnTimeoutMs` | 100% |
