# 判断ログ: 1台で2本目を動かす

- 対象: `docs/spec/usecases/particular_case/1台で2本目を動かす.rucm.md`
- 作成日 / 作成モデル: 2026-09-02 / Claude Opus 5 (1M context)
- 参照した根拠資料: `docs/plans/continuo_design.md`（3-17 / 3-17b / 3-17c / 3-17d）、`internal/instance/instance.go`、`internal/daemon/daemon.go`、`internal/cli/cli.go`、`internal/lock/lock.go`、`docs/releasing.md`

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | 配置先ディレクトリ | `particular_case/` | 「2本目の常駐プロセスを立ち上げる」という単一目的である。issue を1件処理する筋の中には入らない | `docs/plans/continuo_design.md`（3-17b） | 95% |
| 2 | 独立した1本として立てるかどうか | 立てる | **`--id` は起動の段でしか効かない。**issue の処理にも片付けにも入らないので、既存のどの1本にも収まらない | `internal/daemon/daemon.go` の `Run` | 90% |
| 3 | USE CASE NAME | 1台で2本目を動かす | 利用者から見た目的そのものである。内部の作りの名前（`--id` / `Layout`）を題名に入れない | `docs/plans/continuo_design.md`（3-17b） | 90% |
| 4 | BRIEF DESCRIPTION | 名前の検査・置き場所の決定・ディレクトリの用意・ロックの獲得・待ち受けの開始の5点 | この5点で `--id` を渡した起動の全経路が尽きる。`internal/cli/cli.go` の `checkInstanceID` と `internal/daemon/daemon.go` の `Run` の段1〜段2 がそれである | `internal/cli/cli.go` の `checkInstanceID`、`internal/daemon/daemon.go` の `Run` | 90% |
| 5 | PRECONDITION に「設定で3つを分けてある」を置いたこと | 置く | **`--id` が分けるのはロックだけである**（設計 3-17b）。worktree の置き場所・hook を受ける socket・branch 名は検証用の設定ファイルで分ける。**分けていない状態はこのユースケースの前提を満たさない** | `docs/plans/continuo_design.md`（3-17b） | 95% |
| 6 | PRECONDITION に「別のカンバンを指している」を置いたこと | 置く | **同じカンバンを2本が見ると同じ issue を取り合う。機械は止めない**（設計 3-17b）。だから前提として置くしかない | `docs/plans/continuo_design.md`（3-17b） | 90% |
| 7 | PRIMARY ACTOR | 利用者 | 起点は人がコマンドを打つことである。巡回タイマーからもエージェントからも起きない | `internal/cli/cli.go` の `runMain` | 95% |
| 8 | SECONDARY ACTORS | なし | 起動のこの段で外部のサービスを1つも呼ばない。ロックファイルも設定ファイルも同じ機械の中にある | `internal/daemon/daemon.go` の `Run` | 85% |
| 9 | DEPENDENCY / GENERALIZATION | どちらも なし | 他のユースケースを INCLUDE しない。汎化関係にあるものも無い | - | 90% |
| 10 | 基本フロー 2 の VALIDATES THAT | 渡された名前が小文字の英数字で始まり以降が小文字の英数字とハイフンだけであり長さが上限以内である | `ValidateID` が形と長さの2つを見る。**利用者から見ると「名前が使えない」の1つ**なので、1つの検証にまとめた | `internal/instance/instance.go` の `ValidateID` / `validIDShape` | 95% |
| 11 | 名前の検査を 3（置き場所を決められるか）より先に置いたこと | 先に置く | **順序を逆にすると、ホームディレクトリを引けない環境で「名前が使えない」ではなく「置き場所を決められない」と報告される。**`--id` を1文字も渡していない人にも同じ文言が出る | `internal/instance/instance.go` の `Resolve`、`docs/plans/continuo_design.md`（3-17d） | 100% |
| 12 | 「名前が使えない」と「置き場所を決められない」を別の代替フローにしたこと | 分ける | **利用者に見える結果が違う。**前者は名前を書き直せば直り、後者はホームディレクトリの問題である。エラーの型（`instance.InvalidIDError`）で見分けている | `internal/instance/instance.go` の `IsInvalidID`、`internal/cli/cli.go` の `instanceErrorKey` | 95% |
| 13 | 基本フロー 6（ディレクトリを用意する）を検証にしなかったこと | 検証にしない | `EnsureLockDir` は作るだけで、失敗はそのあとの「ロックファイルを開ける」に現れる。**用意できなかったことは、開けなかったことと同じ文言で報告される** | `internal/instance/instance.go` の `EnsureLockDir`、`internal/daemon/daemon.go` の `Run` | 80% |
| 14 | 「ロックファイルを開ける」と「別の常駐プロセスが握っていない」を分けたこと | 分ける | **両方を二重起動と報告すると、置き場所を作れていない運用者が、動いてもいない2本目を探しに行く。**実装も `lock.ErrAlreadyRunning` で言い分けている | `internal/lock/lock.go` の `ErrAlreadyRunning`、`internal/daemon/daemon.go` の `Run` | 100% |
| 15 | 「前提を読めない」を BOUNDED ALTERNATIVE FLOW にしたこと | まとめる | 設定ファイルを読めない・ロックファイルを開けない・socket で待ち受けを始められないの3つは、**どれも「まだ何も掴んでいない時点で止まる」ので、人間から見た結末が同じ**である | `internal/daemon/daemon.go` の `Run` | 80% |
| 16 | hook を受ける socket の待ち受けを基本フローに入れたこと | 入れる | **`claude.hook_bridge.listen` を分け忘れると、2本目はここで落ちる。**このユースケースでいちばん起きやすい失敗なので、フローに現れないと検査の対象にならない | `docs/plans/continuo_design.md`（3-17b） | 85% |
| 17 | `runtime.lock_file` をフローに入れなかったこと | 入れない | **`--id` を渡した起動では `runtime.lock_file` は使われない**（優先順位の1段目が `--id`）。このユースケースは `--id` を渡した起動を書くものなので、分岐が生じない | `internal/daemon/daemon.go` の `ResolveLockFilePath`、`docs/plans/continuo_design.md`（3-17） | 90% |
| 18 | 片付け（`continuo abandon --id`）をこの1本に入れなかったこと | 入れない | 片付けは `着手を取り消す` が既に持っている。**2本の文書が同じ手順を持つと、緩いほうへ流れる。**`--id` と設定ファイルのパスを両方渡すことは、根拠資料と FAQ に書いた | `docs/spec/usecases/particular_case/着手を取り消す.rucm.md`、`docs/plans/continuo_design.md`（3-17c） | 85% |
| 19 | POSTCONDITION に「本番のロックファイルには触れていない」を置いたこと | 置く | **このユースケースが成立したかどうかは、そこでしか判定できない。**「2本目が動いている」だけでは、本番を巻き込んでいないことを言えない | `docs/plans/continuo_design.md`（3-17） | 90% |
| 20 | 名前の長さの上限を数字で書かなかったこと | 書かない | **RUCM に数値を書くと、コードと二重管理になる。**上限は `internal/instance/instance.go` の `MaxIDLen` が正である | `internal/instance/instance.go` の `MaxIDLen` | 85% |

## 人間に確かめたいこと

| # | 何 | なぜ確かめたいか |
| --- | --- | --- |
| 1 | 判断 13（ディレクトリを用意する段を検証にしなかったこと） | **自信 80%。**「ロックファイルを置くディレクトリを作成できません」は、いま「ロックファイルを用意できません」の文言に包まれて出る。別の検証として立てたほうがよいなら、フローを1つ足す |
| 2 | 判断 15（前提を読めないをまとめたこと） | **自信 80%。**`着手を取り消す` の同名のフローに合わせた。3つを別々に立てるべきなら分ける |
| 3 | 判断 18（片付けをこの1本に入れなかったこと） | **自信 85%。**`continuo abandon --id` の付け忘れは、このユースケースの利用者がいちばん踏みやすい。`着手を取り消す` 側に代替フローとして足す案もある |
