# 判断ログ: ボードを新規に用意する

- 対象: docs/spec/usecases/particular_case/ボードを新規に用意する.rucm.md
- 作成日 / 作成モデル: 2026-08-20 / Claude Opus 5 (1M context)
- 参照した根拠資料: docs/plans/continuo_design.md（3-6 / 3-32 / 3-34 / 4-1 / 5-2）、docs/trying_it_out.md（段2 / 段4 / 段9）、internal/scaffold/detect.go、internal/doctor/checks.go、CLAUDE.md

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | USE CASE NAME | ボードを新規に用意する | 動詞で終わる名詞句。docs/plans/continuo_design.md 3-34 の「新規に作る場合」に対応する単一目的の操作である | docs/plans/continuo_design.md 3-34 | 95% |
| 2 | 配置ディレクトリ | particular_case | ボードを1枚作るという単一目的・単一操作である。複数ユースケースを跨ぐ時系列ではない | rucm スキルの粒度ガイド | 95% |
| 3 | システムの実体（BRIEF DESCRIPTION 冒頭で定義） | 利用者の手元で gh コマンドを実行するコマンド環境 | ボードの作成は `gh project create` と `gh project field-create` で行い、continuo の常駐プロセスは1つも関与しない。RUCM は主アクターと副アクターの直接相互作用を禁じる（R3）ので、gh を実行する手元の環境をシステムとして立てないと GitHub を副アクターにできない | docs/trying_it_out.md 段2 | 65% |
| 4 | BRIEF DESCRIPTION に「組み込みの Status に触らない」を書くか | 書く | このユースケースの中心的な制約である。既存 single-select への選択肢追加は `updateProjectV2Field` しか無く、CLAUDE.md が禁止している | CLAUDE.md、docs/trying_it_out.md 段2 | 100% |
| 5 | PRECONDITION | GitHub のアカウントを持っている／owner を決めている／continuo をビルドしている | gh の導入と認証は PRECONDITION に置かない。置くと「gh が無い」「認証が足りない」を代替フローとして書けなくなる（前提が破れている状態はユースケースに入れない） | docs/trying_it_out.md 段1・先に決めること | 85% |
| 6 | PRIMARY ACTOR | 利用者 | ボードを作ると決めるのも、名前を決めるのも人間である。continuo はボードを作らない | docs/plans/continuo_design.md 3-34 | 95% |
| 7 | SECONDARY ACTORS | GitHub | システムは GitHub Projects v2 にボードとフィールドを作らせる。副アクターへの要求という相互作用に当たる | docs/plans/continuo_design.md 3-34 | 90% |
| 8 | DEPENDENCY | なし | 他の particular_case を取り込まない。`continuo init` と `continuo doctor` は別のユースケースとして分ける（判断 #26） | - | 80% |
| 9 | GENERALIZATION | なし | 汎化関係にある上位のユースケースを置いていない | - | 85% |
| 10 | ステップ分割の方針 | 1ステップ1動作にし、GitHub への要求と GitHub の応答の検証を必ず別ステップにする | R4 と、検証の偽ケースを代替フローで書く R22 を両立させるため。要求と検証を1文にまとめると `VALIDATES THAT` の分岐元が作れない | references/rucm_structure.md の R4 / R22 | 90% |
| 11 | BASIC FLOW 1 と 2 の分割 | 名前の送信と作成の要求を別ステップにする | ボード名重複の代替フローが `RESUME STEP 1` で名前の送信からやり直せる形にするため。1ステップにまとめると、やり直しの復帰先が「作成の要求」まで含んでしまう | references/rucm_structure.md の R25 | 80% |
| 12 | BASIC FLOW 3 の VALIDATES THAT | gh コマンドを実行できる | continuo の起動時検査に「gh が使えるか」が入っている。ボードを作る場面でも同じ前提が要る | docs/plans/continuo_design.md 3-6 | 85% |
| 13 | BASIC FLOW 4 の VALIDATES THAT | 有効なアカウントのトークンの scope に project が単独の要素として含まれている | doctor の判定条件をそのまま使った。`read:project` では Status を書けないため単独の `project` を求める | docs/plans/continuo_design.md 3-32「`gh auth status` の読み方を1つに決める」 | 95% |
| 14 | BASIC FLOW 5（ボードの一覧を取る） | `gh project list --owner` に相当する要求を GitHub に出す | 同名のボードが既にあるかを判定する材料が要る。`continuo init` も同じコマンドで候補を引いている | internal/scaffold/detect.go の `detectProject` | 85% |
| 15 | BASIC FLOW 6 の VALIDATES THAT（同名の判定） | 新しいボードの名前が既存のボードの名前と重複していない | GitHub Projects v2 はボードの表示名の重複を拒まないため、システム側で名前を突き合わせる形にした。重複を許すと `continuo init` がボードの候補を1件に絞れず、番号を自動で埋められなくなる | internal/scaffold/detect.go の `detectProject`（候補が1件のときだけ番号を埋める） | 60% |
| 16 | BASIC FLOW 7（ボードの作成） | GitHub に新しいボードの作成を要求する | `gh project create --owner <ACCOUNT> --title <名前> --format json --jq .number` に対応する | docs/trying_it_out.md 段2 | 95% |
| 17 | BASIC FLOW 8 の VALIDATES THAT | GitHub が新しいボードの番号を応答する | 作成の成否は返ってきた番号で判定する。番号は以降の全ステップと `continuo init --project` の入力になる | docs/trying_it_out.md 段2・段4 | 90% |
| 18 | BASIC FLOW 9（番号を利用者に返す） | 番号を利用者に応答する | 利用者が控える値である。以降の手順で `<PROJECT>` として使う | docs/trying_it_out.md 段2「出た数字が `<PROJECT>` である」 | 95% |
| 19 | BASIC FLOW 10（専用フィールドの作成） | continuo Status という single-select フィールドを新しく作る | 選択肢を指定できるのは新規作成のときだけである。組み込みの Status に足す API は禁止されている。したがって「新しく作る」が唯一の無人で完結する道になる | docs/trying_it_out.md 段2、CLAUDE.md | 100% |
| 20 | 専用フィールドの名前 | continuo Status | 空白を含む名前でも `status_field` に書けて、絞り込みのキーとしても解決されることを実測済みである | docs/plans/continuo_design.md 3-34「専用フィールドを使ってよい」 | 95% |
| 21 | 5つの選択肢の名前と順序 | Ready、In Progress、In Review、Blocked、Done | continuo の既定の設定が実在を求める5つである。`dispatch_state` が Ready、`running_state` が In Progress、`failure_state` が Blocked、`terminal_states` が Done に対応する | docs/plans/continuo_design.md 5-2 / 4-1、docs/trying_it_out.md 段2 | 100% |
| 22 | BASIC FLOW 11 の VALIDATES THAT | GitHub が continuo Status フィールドの作成の完了を応答する | フィールドの作成もレートリミットや権限で落ちる。落ちたことを検知しないと、ボードだけが残った状態で先へ進む | docs/plans/continuo_design.md 3-32（ボードを読めなかったときの記号） | 75% |
| 23 | BASIC FLOW 12 と 13（作成後の照合） | 選択肢の一覧を取り直し、5つが全部あることを検証する | 選択肢名が1文字でも違うと、continuo の巡回が GraphQL のエラーなしに「対象0件」を返し続ける。作った直後に照合しておけば、この無言の失敗を人間が動き出す前に潰せる | docs/plans/continuo_design.md 3-34「無言の失敗を検知する2段構え」、docs/trying_it_out.md 段9の困ったときの表 | 70% |
| 24 | BASIC FLOW 14（最後に返す値） | 新しいボードの番号と continuo Status フィールドの名前 | 利用者はこの2つを WORKFLOW.md の `project_number` と `status_field` に書く。番号だけを返すと `status_field` の書き換え漏れが起きる | docs/trying_it_out.md 段4「`status_field` を書き換える」 | 90% |
| 25 | BASIC FLOW の POSTCONDITION | ボードとフィールドと5つの選択肢の存在、組み込みの Status が3つのままであること、番号と名前の表示 | 「おすすめ構成を備えたボードができた」を検算できる形にした。組み込みの Status を触っていないことも、この手順が守るべき制約なので事後条件に入れた | docs/trying_it_out.md 段2、CLAUDE.md | 90% |
| 26 | `continuo init` と `continuo doctor` をこのユースケースに含めるか | 含めない | ゴールは「おすすめ構成を備えたボードができること」である。設定ファイルの生成と前提の検査はボードが無くても実行でき、別の目的を持つ。scenario 側で INCLUDE して繋ぐ想定である | docs/trying_it_out.md 段2 と段4・段5 が別の段である | 80% |
| 27 | 代替フロー gh未導入 の分岐元 | RFS BASIC FLOW 3 | `VALIDATES THAT gh コマンドを実行できる` が偽の場合であり、R22 が代替フローを必須にする | references/rucm_structure.md の R22 | 95% |
| 28 | gh未導入 の終端 | ABORT | gh が無ければボードは1件も作れない。復帰先が無い | - | 90% |
| 29 | gh未導入 の POSTCONDITION | ボードは作成されていない／導入手順が表示されている | 副作用が1つも発生していない段階での失敗である | - | 90% |
| 30 | 代替フロー 認証不足 の分岐元 | RFS BASIC FLOW 4 | `VALIDATES THAT` の偽ケースであり、R22 が代替フローを必須にする | references/rucm_structure.md の R22 | 95% |
| 31 | 認証不足 の中で IF-ELSE を使うか | 使う | 未ログインと scope 不足で案内するコマンドが違う。doctor は未ログインを `gh auth login -s project`、scope 不足を `gh auth refresh -h github.com -s project` で案内し分けている。1本にまとめると、どちらの場合も間違ったコマンドを出しうる | docs/plans/continuo_design.md 3-32、docs/trying_it_out.md 段9の困ったときの表 | 90% |
| 32 | 認証不足 の IF の条件 | gh が github.com の有効なアカウントを持っている | doctor は `Active account: true` のブロックだけを読み、ホストを github.com に固定している | docs/plans/continuo_design.md 3-32「`gh auth status` の読み方を1つに決める」 | 95% |
| 33 | 認証不足 の POSTCONDITION | ボードは作成されていない／やり直すコマンドが表示されている | 2通りの分岐のどちらを通っても真になる形にした | - | 85% |
| 34 | 代替フロー ボード名重複 の分岐元 | RFS BASIC FLOW 6 | `VALIDATES THAT` の偽ケースである | references/rucm_structure.md の R22 | 95% |
| 35 | ボード名重複 の終端 | RESUME STEP 1 | 名前を変えれば同じ手順で最後まで到達できる。ABORT にすると、名前の付け替えという小さな修正のために手順の最初からやり直させることになる | references/rucm_structure.md の R25 | 80% |
| 36 | ボード名重複 の POSTCONDITION | ボードは作成されていない／同じ名前のボードの番号が表示されている | 既存のボードの番号を出すのは、利用者が「既にあるボードを使う」と判断できるようにするためである | docs/plans/continuo_design.md 3-34「使い方ごとの用意」（既存を使う道がある） | 75% |
| 37 | 代替フロー 作成失敗 の種別 | BOUNDED ALTERNATIVE FLOW | ボードの作成（8）とフィールドの作成（11）の2つの条件ステップから同じ処理へ分岐する。SPECIFIC を2本書くと同じ文面が二重になる | references/rucm_structure.md の BOUNDED の定義 | 85% |
| 38 | 作成失敗 の分岐元 | RFS BASIC FLOW 8,11 | GitHub の応答を検証する条件ステップは基本フローにこの2つだけである | - | 90% |
| 39 | 作成失敗 の中で IF を使う理由 | 分岐元によって、作りかけのボードが残るかどうかが変わる | 8 で落ちたときはボードが無い。11 で落ちたときはボードだけが残る。残ったボードを放置すると、次に `continuo init` を実行したときボードの候補が増え、番号の自動判定が働かなくなる | internal/scaffold/detect.go の `detectProject`（候補が複数なら埋めない） | 80% |
| 40 | 作成失敗 の終端 | ABORT | GitHub 側の失敗であり、同じ要求をその場で繰り返しても通らない | - | 80% |
| 41 | 作成失敗 の POSTCONDITION | フィールドは作成されていない／エラーの内容が表示されている | 2つの分岐元のどちらを通っても真になる文だけを書いた。ボードの有無は分岐元によって変わるので事後条件に書かない | - | 85% |
| 42 | 代替フロー 選択肢不足 の分岐元 | RFS BASIC FLOW 13 | `VALIDATES THAT` の偽ケースである | references/rucm_structure.md の R22 | 95% |
| 43 | 選択肢不足 で提示する直し方 | GitHub の画面から足す道と、対象にする Status を縮める道の2つ | 設計が「取れる道」として挙げているのはこの2つである。専用フィールドを作り直す道は、このフローが専用フィールドの作成後に来るため重複になる | docs/plans/continuo_design.md 3-34「新規に作る場合」の表 | 85% |
| 44 | 選択肢不足 で API を使わないことを明示するか | 明示する | 足りない選択肢を見た利用者が `updateProjectV2Field` に手を伸ばすのが最も起きやすい事故である。この API は選択肢の指定を全件置き換えとして扱い、設定済みの Status の値を全部消す | CLAUDE.md、docs/trying_it_out.md 段2 | 100% |
| 45 | 選択肢不足 で縮める設定の中身 | 対象にする Status を Todo と In Progress にする | 組み込みの Status は Todo と In Progress と Done の3つで始まる。設計が `active_states` を Todo と In Progress、`dispatch_state` を Todo にする形を挙げている | docs/plans/continuo_design.md 3-34「新規に作る場合」 | 90% |
| 46 | 選択肢不足 の POSTCONDITION | ボードはある／足りない選択肢がある／直し方が表示されている | ボードとフィールドは作成済みの状態で失敗している。ボードが残っていることを事後条件に書かないと、後片付けの要否が読めない | - | 80% |
| 47 | 任意時点代替フロー 中断 の WHEN | 利用者がボードの作成の途中で中断を要求した場合 | 手元でコマンドを続けて実行する手順なので、利用者が途中で打ち切る中断が現実に起きる | docs/trying_it_out.md 段2（2つのコマンドを続けて実行する） | 75% |
| 48 | 中断 の BRANCH FROM | BASIC FLOW 10 | 最悪のタイミングを1点だけ選んだ。10 の直前でボードは作成済み、continuo Status フィールドは未作成であり、この状態で止まると「組み込みの Status しか無いボード」が GitHub に残る。残ったボードは `continuo init` のボードの候補に数えられて番号の自動判定を壊し、さらに次回の実行では同名のボードとして重複判定に引っかかる。3 や 4 で中断しても副作用は1つも残らないので、検証する価値が低い | docs/plans/continuo_design.md 3-34、internal/scaffold/detect.go の `detectProject` | 70% |
| 49 | 中断 の終端 | ABORT | 中断は利用者の意思であり、同じ手順へ復帰させない。R25 の復帰先は基本フロー固定であり、復帰させると中断の意味が消える | references/rucm_structure.md の BRANCH FROM 規約 | 85% |
| 50 | 中断 の POSTCONDITION | ボードがある／フィールドは作成されていない／番号と消すコマンドが表示されている | 中途半端なボードが残ることを利用者に必ず知らせる。知らせないと、次の実行で重複判定に引っかかる原因が分からない | internal/scaffold/detect.go の `detectProject` | 75% |
| 51 | フローチャートの表現方針 | 基本フローの14ステップを直列に並べ、代替フローをフロー単位の subgraph に分け、分岐元から矢印を引く | rucm ブロックの全ステップと全分岐を1枚で辿れるようにするため。ボード名重複の `RESUME STEP 1` は基本フローの1へ戻る矢印で表した | rucm スキルのファイル規約 | 90% |
| 52 | シーケンス図の表現方針 | 利用者・システム・GitHub の3者にし、`VALIDATES THAT` を alt、任意時点代替フローを opt で表す | 誰が誰に要求しているかを見せるのが目的である。GitHub への要求と応答を明示することで、副アクターとの往復の回数が読める | rucm スキルのファイル規約 | 90% |
