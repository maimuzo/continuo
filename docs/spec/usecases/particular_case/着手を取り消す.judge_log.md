<!-- 目的: 「着手を取り消す」の RUCM を書くときに AI が行った判断と、その根拠を記録する -->

# 判断ログ: 着手を取り消す

- 対象: `docs/spec/usecases/particular_case/着手を取り消す.rucm.md`
- 作成日 / 作成モデル: 2026-08-24 / Claude Opus 5
- 参照した根拠資料: `docs/plans/continuo_design.md`（3-37 / 3-9 / 3-17 / 3-18 / 3-20）、`internal/abandon/abandon.go`、`internal/abandon/issueurl.go`、`internal/abandon/deps.go`、`internal/workspace/inspect.go`、`internal/cli/cli.go`

## 判断一覧

| # | 判断対象 | 決定した値 | 合理的決定根拠 | 出典 | 自信 |
| --- | --- | --- | --- | --- | --- |
| 1 | USE CASE NAME | 着手を取り消す | 依頼で指定されたファイル名に完全一致させる。**動詞で終わる名詞句**の条件も満たす | 依頼文、rucm スキルのファイル規約 | 100% |
| 2 | 配置ディレクトリ | `particular_case/` | 「1件の issue の着手を取り消す」という単一目的の操作単位である。複数ユースケースを跨ぐ時系列ではない | rucm スキルの粒度ガイド | 95% |
| 3 | BRIEF DESCRIPTION | 渡す・手を離させる・1つに絞って消す・Status を指定されたときだけ動かす の4文 | 段1〜段5 をそのまま4文へ落とした。**段5 が「指定されたときだけ」であることを要約に入れた**（このコマンドの一番の設計判断であるため） | `docs/plans/continuo_design.md#3-37`、`internal/abandon/abandon.go` の `run` | 95% |
| 4 | PRECONDITION | issue の URL を知っている。WORKFLOW.md がある。置き場所に身元ファイルを持つ worktree が1つ以上ある | 設定ファイルは `config.Load` が読む。worktree が0件でも実行はできるが、そのときは代替フローになるので**基本フローの事前条件には1つ以上あることを置いた** | `internal/abandon/abandon.go` の `Run`、`internal/cli/cli.go` の `runAbandon` | 90% |
| 5 | PRIMARY ACTOR | 利用者 | **人間が手で叩くコマンドである。**巡回タイマーは起こさない。`internal/cli` の `runAbandon` が引数を受けて `abandon.Run` を呼ぶ経路しかない | `internal/cli/cli.go` の `runAbandon` | 100% |
| 6 | SECONDARY ACTORS | GitHub Projects v2、herdr、git | Status を読み書きする相手・pane と workspace を消す相手・branch を消す相手である。**branch は herdr が消さないので git を独立した副アクターにした**（`worktree と branch を片付ける` と同じ扱い） | `internal/abandon/deps.go` の `newTracker`、`internal/abandon/abandon.go` の `panesOf` と `remove` | 90% |
| 7 | DEPENDENCY | なし | `INCLUDE USE CASE` を1つも使わない。片付けの実体は `worktree と branch を片付ける` と同じ `Cleanup` を呼ぶが、**契機（人間が1件を指定する）が違うので取り込まない** | rucm スキルのファイル規約、`internal/abandon/abandon.go` の `remove` | 80% |
| 8 | GENERALIZATION | なし | 汎化関係にあるユースケースが無い | - | 90% |
| 9 | ステップ分割方針 | **失敗したら止まる分岐を必ず `VALIDATES THAT` の独立ステップにする** | このコマンドは worktree と branch を消す。**どこで止まったかが事後条件から読めないと、人間は次に何をすればよいか分からない**。1ステップに2つの検査を詰めない | `internal/abandon/abandon.go` の `run`（各段が終了コードを返す形になっている） | 95% |
| 10 | 段の順番をステップ順にしたこと | worktree を1つに絞る段を、手を離させて pane を待つ段より前に置いた | 段1 の後半は「その worktree の pane が消えるまで待つ」ので、**待つ相手が決まっていなければ始められない**。実装のコメントが同じ理由を明記している | `internal/abandon/abandon.go` の package コメントと `run` | 100% |
| 11 | 最初のステップ | 利用者が issue の URL を渡して着手の取り消しを要求する | 位置引数の1つ目が issue の URL、2つ目が WORKFLOW.md のあるディレクトリである。**ディレクトリは省略できる**ので基本フローには書かない | `internal/cli/cli.go` の `runAbandon` | 95% |
| 12 | issue の URL の分解（VALIDATES THAT） | issue の URL を host と owner とリポジトリ名と番号に分解できる | 受け付ける形は `<scheme>://<host>/<owner>/<repo>/issues/<番号>` だけである。**`pull` は受け付けない** | `internal/abandon/issueurl.go` の `ParseIssueURL` | 100% |
| 13 | 設定ファイルの読み取り（VALIDATES THAT） | 設定ファイルを読める | `config.Load` の失敗と、外部へ繋ぐ処理の組み立ての失敗を1つにまとめた。**どちらも「まだ何も触っていない時点で止まる」ので、人間から見た結末が同じである** | `internal/abandon/abandon.go` の `Run`、`internal/abandon/deps.go` の `resolve` | 80% |
| 14 | ロックファイルを開けるか（VALIDATES THAT） | 二重起動防止のロックファイルを開ける | **「ロックを取れない」と「ロックファイルを開けない」を言い分ける。**前者は継続監視が動いていることを表し、後者は `runtime.lock_file` の打ち間違いである | `internal/abandon/abandon.go` の `isRunning` | 100% |
| 15 | ロックの獲得の可否と、その応答 | 獲得の可否から判定し、**取れたロックは実行が終わるまで握り続ける** | **取れたら動いていない。**その場で手放すと、直後に継続監視が起動でき、**その足元から worktree を消す**（abandon は git と RPC を何度も叩くので窓は秒単位で開く）。握っているあいだに起動しようとした継続監視は「既に起動しています」で止まる | `internal/abandon/abandon.go` の `isRunning` と `run`、`docs/plans/continuo_design.md#3-37-2` | 100% |
| 16 | 置き場所の走査（VALIDATES THAT） | worktree の置き場所を走査できる | 走査に失敗したら候補を1つも決められない。**候補が決まらないまま先へ進んではならない** | `internal/abandon/abandon.go` の `find` | 90% |
| 17 | 身元ファイルの読み取り | worktree の中の身元ファイルを読む | ディレクトリ名から issue へは戻れない。**身元ファイルが無いと、どの issue のものか分からない** | `docs/plans/continuo_design.md#3-18`、`internal/abandon/abandon.go` の `find` | 100% |
| 18 | 候補の worktree がちょうど1つか（VALIDATES THAT） | 身元ファイルの `issue_url` が一致する worktree がちょうど1つある | **パスを owner とリポジトリ名と番号から組み立ててはならない**（`workspace.root` や `branch_template` を変えている環境で空振りする）。照合は host と owner とリポジトリ名を大文字小文字を無視して、番号を数として比べる | `internal/abandon/issueurl.go` の `SameIssue`、`internal/abandon/abandon.go` の `find` | 100% |
| 19 | 身元ファイルを読めない worktree の扱い | rucm ブロックには書かず、本文の表に書いた | 候補にしないだけで、**消しもしないし止めもしない**（ログに1行残る）。フローの分岐にすると、実行の結末が変わらない枝が1本増える | `internal/abandon/abandon.go` の `find` | 75% |
| 20 | 継続監視が動いているか（IF） | 継続監視が動いている | **動いていなければ手を離させる相手がいない。**どちらも正常な経路なので `IF-ENDIF` を基本フローに置いた（`ELSE` は無い） | `internal/abandon/abandon.go` の `run`、rucm スキルの厳密規則11 | 95% |
| 21 | 手離し先の Status の読み取り（VALIDATES THAT） | ボードから issue の Status と project item の ID を引ける | **身元ファイルの `project_item_id` は使わない。**worktree の中にあり、エージェントが書き換えられる値なので、それを宛先にすると別の issue の Status を動かせてしまう | `internal/abandon/abandon.go` の `currentState` と `readBoard` | 100% |
| 22 | Status が作業中の値か（IF） | Status が `tracker.active_states` に入っている | 入っていなければ継続監視はもうその issue を持っていないので、**何も書かない**。ボードへの書き込みを1回減らす | `internal/abandon/abandon.go` の `park` | 95% |
| 23 | park の値への書き換え（VALIDATES THAT） | ボードの Status を park の値へ書き換えられる | 動かす先は `--park`、指定が無ければ `tracker.failure_state` である。**`failure_state` が `active_states` に入らないことは設定の検証が保証している**ので、動かせば必ず active から外れる | `internal/abandon/abandon.go` の `park`、`internal/config/validate.go` | 95% |
| 24 | park で「書かない状態の一覧」を渡さないこと | rucm ブロックには書かず、判断ログにだけ残した | 人間が「手を離させろ」と言っている実行なので、**いまの Status が何であっても park の先へ動かす**。ステップの文には現れない引数の話である | `internal/abandon/abandon.go` の `park` | 85% |
| 25 | pane が期限内に無くなるか（VALIDATES THAT） | worktree を作業ディレクトリに持つ pane が期限内に無くなる | 上限は `herdr.read_timeout_ms` の10倍（既定50秒）である。**`read_timeout_ms` そのものは1回の問い合わせの上限であって、turn を終えて pane を手放すまでの時間ではない** | `internal/abandon/abandon.go` の `waitPaneGone` と `PaneWaitTimeoutFactor` | 100% |
| 26 | pane の照合を cwd にしたこと | rucm ブロックの文を「作業ディレクトリに持つ pane」にした | 身元ファイルの `herdr_workspace_id` は worktree の中にあり、エージェントが書き換えられる。**`foreground_cwd` も見る**（Claude Code が別のディレクトリへ降りていても pane はその worktree に属する） | `internal/abandon/abandon.go` の `panesOf` | 95% |
| 27 | 失うものの調査（VALIDATES THAT） | worktree の中の失うものを調べられる | 判定は `internal/workspace` の `Inspect` が `Cleanup` と同じ関数を呼んで出す。**判断を2箇所に書かない** | `internal/workspace/inspect.go` の `Inspect`、`docs/plans/continuo_design.md#3-37` | 95% |
| 28 | 消すものと失うものの一覧の応答 | 消すものと失うものの一覧を応答する | 出すのは issue と Status・worktree・branch と base・herdr の workspace と pane・コミットしていない変更の件数・push していない commit の件数である。**dry-run でも force でも同じものを出す** | `internal/abandon/abandon.go` の `printPlan` | 95% |
| 29 | 一覧で Status を読めなくても止めないこと | 一覧に「Status は不明」と理由を出して先へ進む | 一覧は人間に見せるためのものであり、**読めないことが片付けを止める理由にはならない**。止めるのは park の書き込みと、片付けたあとの書き込みだけである | `internal/abandon/abandon.go` の `printPlan` | 85% |
| 30 | dry-run を指定していないか（VALIDATES THAT） | 利用者が dry-run を指定していない | **`--dry-run` を条件ステップにした。**任意時点代替フローにすると、機械照合できる分岐元を持たないまま「見せて終わる」経路がぶら下がる | `internal/abandon/abandon.go` の `run`、rucm スキルの厳密規則9 | 85% |
| 31 | 失うものが無いか force があるか（VALIDATES THAT） | 失うものが無いか利用者が force を指定している | **`HasLoss` は `Reasons` では判定しない。**`cleanup.require_clean_worktree` などの検査を切ってある環境でも、失うものがあれば黙って消してはならない。判定できなかった場合（base を決められない場合）も「失うものがある」側に倒す | `internal/workspace/inspect.go` の `Leftover.HasLoss` | 100% |
| 32 | 削除の要求 | herdr に worktree と pane と herdr の workspace の削除を要求する | `herdr worktree remove` が pane ごと閉じることは実測で確認済みである。**続けて `workspace.close` を呼ばない** | `docs/plans/continuo_design.md#3-9`、`internal/abandon/abandon.go` の `remove` | 95% |
| 33 | 片付けに Force を必ず真で渡すこと | rucm ブロックには書かず、判断ログにだけ残した | 消してよいかは「失うものが無いか force があるか」で決着している。**渡さないと `cleanup.enabled` が偽の環境や `require_pushed` が真の環境で `--force` が効かない** | `internal/abandon/abandon.go` の `remove` | 90% |
| 34 | worktree が消えているか（VALIDATES THAT） | worktree が置き場所から消えている | `Cleanup` がエラーを返した場合と、見送って `Removed` が偽で返った場合の**両方をこの1つの検査でまとめた**。どちらも「worktree が残っている」という同じ結末になる | `internal/abandon/abandon.go` の `remove` | 90% |
| 35 | branch が消えたかどうかで応答を変える（IF-ELSE） | branch が消えたかどうかで応答を変える | **消えていない branch を「消しました」と書かない。**`cleanup.delete_branch` が偽のとき、branch の検算に落ちたとき、`git branch -D` が失敗したときは branch が残る。**どちらも終了コードは 0 である**ので代替フローにしない | `internal/abandon/abandon.go` の `remove`（`BranchDeleted` の分岐） | 95% |
| 36 | 片付けたあとの Status を動かすか（IF-ELSE） | 片付けたあとの Status の値を指定したときだけ動かす | 既定を `dispatch_state` にすると同じ issue にもう一度着手し、`failure_state` にすると失敗していないものが失敗として残り、`terminal_states` にすると後始末の経路がもう一度走る。**どれも事故になる** | `docs/plans/continuo_design.md#3-37`、`internal/abandon/abandon.go` の `moveStatus` | 100% |
| 37 | 片付けたあとの読む検査と書く検査を分けたこと | 読む検査と書く検査を別のステップにした | 読めない場合と書けない場合で**人間が次にやることが違う**（前者はボードに issue が載っているかを見る、後者は Status の選択肢を見る） | `internal/abandon/abandon.go` の `moveStatus` | 85% |
| 38 | 基本フローの POSTCONDITION | worktree が無い。workspace が閉じている。pane が無い。Status は指定したときだけ動いている。終了コードは 0 | **「Status が動いていないこと」も事後条件である。**これを書かないと、既定の挙動がテストに落ちない | `internal/abandon/abandon.go` の `moveStatus` | 95% |
| 39 | 終了コードを POSTCONDITION に書いたこと | 全フローの事後条件の末尾に終了コードを1文で書いた | このユースケースは CLI であり、**終了コードが唯一の機械可読な結果である**。0 と 1 の区別が事後条件に無いと、テストが「止まったこと」を検証できない | `internal/cli/cli.go` の `runAbandon`、`internal/abandon/abandon.go` の `ExitOK` と `ExitStopped` | 90% |
| 40 | 終了コード 2 を rucm ブロックに書かなかったこと | 引数の指定の誤りはフローにしない | 終了コード 2 を返すのは `internal/cli` であり、`internal/abandon` は引数を解釈しない。**このユースケースの外側にある** | `internal/cli/cli.go` の `runAbandon`、`internal/abandon/abandon.go` の package コメント | 85% |
| 41 | フロー `issueURLの解釈不能` | URL の分解の検査から分岐し、応答して ABORT | 空文字・scheme 違い・host 無し・形が合わない・番号が正の整数でない、のすべてがここへ来る。**設定ファイルを読む前に止まる** | `internal/abandon/issueurl.go` の `ParseIssueURL`、`internal/abandon/abandon.go` の `Run` | 95% |
| 42 | フロー `前提を読めない` の種別 | BOUNDED ALTERNATIVE FLOW。設定・ロックファイル・走査・失うものの調査の4つの検査から分岐する | 設定・ロックファイル・置き場所の走査・失うものの調査の4つは、**どれも「読めなかった対象と理由を出して、何も消さずに止まる」という同じ結末**になる。4本の特定代替フローに分けても事後条件が同じになるだけである | `internal/abandon/abandon.go` の `Run` と `run` | 80% |
| 43 | `前提を読めない` の POSTCONDITION から Status を外したこと | 「worktree が残っている。branch が残っている。workspace が残っている。終了コードは 1」だけにした | **失うものの調査でここへ来た場合、Status は park の値へ動いている。**「Status は変わっていない」と書くと、4つの分岐元のうち1つで偽になる | `internal/abandon/abandon.go` の `run`（park の後に Inspect を呼ぶ順序） | 90% |
| 44 | フロー `worktreeが無い` の終了コード | 0 | **消すものが無かったのは失敗ではない。**同じコマンドを2回叩いても、2回目が失敗にならない | `docs/plans/continuo_design.md#3-37` の段2、`internal/abandon/abandon.go` の `find` | 100% |
| 45 | `worktreeが無い` に「ボードへの問い合わせは1回も起きていない」を入れたこと | 事後条件に1文足した | ボードのアダプタは**必要になるまで作らない**（作るときに `gh` を起動してトークンを引く）。worktree が無ければ API 枠を1回も使わない | `internal/abandon/abandon.go` の `tracker`、`internal/abandon/deps.go` の `newTracker` | 90% |
| 46 | フロー `worktreeが2つ以上` | 候補がちょうど1つかの検査から分岐し、パスの一覧を出して ABORT | **人間が中身を見て決めること。**置き場所の設定を変えた跡か、手で作った worktree が混ざったかである。どちらを消すかを推測してはならない | `internal/abandon/abandon.go` の `find` | 100% |
| 47 | 同じ検査を2本の特定代替フローが指すこと | 0件と2件以上でフローを分けた | 終了コードが 0 と 1 で違い、事後条件も違う。**1本にまとめると、どちらの結末になったかがテストから読めない** | rucm スキルの R19、`internal/abandon/abandon.go` の `find` | 90% |
| 48 | フロー `手離し先のStatusを読めない` | 手離し先の Status の読み取りから分岐し、応答して ABORT | **Status を確かめられないまま消しにいかない。**継続監視が掴んでいる worktree を消すと、走っているエージェントの足元からディレクトリが消える | `internal/abandon/abandon.go` の `park` | 100% |
| 49 | フロー `手離しの書き込み失敗` | park の値への書き換えから分岐し、応答して ABORT | 書き込みがエラーになった場合と、書いたのに反映されなかった場合の**両方がここへ来る**。どちらも継続監視は issue を掴んだままである | `internal/abandon/abandon.go` の `park`（`written` が偽の分岐） | 95% |
| 50 | フロー `paneが期限内に消えない` | pane の待ちから分岐し、pane の ID と待った上限を出し、Status を戻さずに ABORT | **何も消さずに止まる。**pane が残ったまま worktree を消すと、消えたディレクトリの中で Claude Code が動き続ける | `internal/abandon/abandon.go` の `waitPaneGone` | 100% |
| 51 | `paneが期限内に消えない` に「Status を元へ戻さない」を入れたこと | ステップ2 として明示した | 実装は park で動かした Status を戻さない。**戻さないことを書いておかないと、人間はボードを見て「なぜ Blocked なのか」が分からない** | `internal/abandon/abandon.go` の `run`（`park` の後に `waitPaneGone` を呼び、失敗しても巻き戻しをしない） | 90% |
| 52 | フロー `見せるだけで終わる` の終了コード | 0 | 見せることが目的の実行である。**消さなかったことを失敗にすると、スクリプトから `--dry-run` を使えない** | `internal/abandon/abandon.go` の `run` | 95% |
| 53 | フロー `失うものがある` | 失うものと force の検査から分岐し、応答して ABORT | **何も消さない。**`--force` を出すのは人間の判断であり、コマンドが代わりに決めてはならない | `internal/abandon/abandon.go` の `run`、`internal/workspace/inspect.go` の `Leftover.HasLoss` | 100% |
| 54 | フロー `片付けの失敗` | worktree が消えているかの検査から分岐し、見送った理由の一覧を出して ABORT | `Cleanup` が理由を返すので、そのまま出す。**「消えませんでした」だけでは、人間は次に何をすればよいか分からない** | `internal/abandon/abandon.go` の `remove` | 90% |
| 55 | フロー `片付け後のStatusを読めない` と `片付け後の書き込み失敗` | 片付けたあとの読む検査と書く検査からそれぞれ分岐し、どちらも ABORT | **worktree は既に消えている。**事後条件に「worktree は置き場所に無い」を書き、**片付けが終わったあとの失敗であることを区別できるようにした** | `internal/abandon/abandon.go` の `moveStatus` | 95% |
| 56 | フロー `実行の中断` の種別 | GLOBAL ALTERNATIVE FLOW。`WHEN 利用者が SIGINT または SIGTERM で実行を中断した場合` | 中断は実行のどの時点でも起こりうる。**特定の条件ステップに紐づかない**ので任意時点代替フローにした | `internal/cli/cli.go` の `runAbandon`（`signal.NotifyContext`）、`internal/abandon/deps.go` の `sleepUntil` | 90% |
| 57 | `実行の中断` の BRANCH FROM | pane が消えるのを待っている時点 | **ここが最悪のタイミングである。**park で Status を既に動かし、pane の消滅を最大50秒待っている。中断すると、Status だけが動いて worktree も pane も残る**中途半端な状態**で終わる。他の時点（URL の解釈中・走査中）で中断しても、外部に何も残らない | `internal/abandon/abandon.go` の `run` と `waitPaneGone`、rucm スキルの BRANCH FROM 選定基準（最悪タイミング想定） | 85% |
| 58 | `実行の中断` を1点だけにしたこと | pane の待ちの1点だけ | 分岐元1点につきテストパスが1本増える。**副作用が始まる唯一の待ち時間がここである**ので、複数指定する理由が無い | rucm スキルの BRANCH FROM 原則 | 85% |
| 59 | `実行の中断` を `ABORT` で終えたこと | `RESUME STEP` を使わない | 中断は利用者が止めたものである。**基本フローへ戻ると、止めたはずの片付けが走る** | `internal/abandon/deps.go` の `sleepUntil`（偽を返すと `waitPaneGone` が止まる） | 95% |
| 60 | 本文に表を3つ足したこと | 「段の順番」「Status を既定で動かさない理由」「worktree の探し方」を本文の表にした | 段の順番の理由と、照合の比べ方は**ステップの1文には入り切らない**。既定を置かない理由は、このコマンドで人間が一番戸惑う点である | `docs/plans/continuo_design.md#3-37`、`internal/abandon/issueurl.go` の `SameIssue` | 85% |
| 61 | ステップ番号で判断対象を指さないこと | 判断対象は**ステップの中身の名前**で書く（「ステップ11」ではなく「手離し先の Status の読み取り」） | ステップ番号は RUCM を1行足すたびに動く。**番号で指すと、この表は必ず嘘になる。**同じ理由で本文の表も番号ではなく段の名前で引く | rucm スキルの参照規約、`docs/plans/continuo_design.md#3-37` | 90% |
| 62 | 書き込む先の Status を確かめる段を足したこと | 候補を1つに絞った直後に、`--to` と park の先を確かめる段を置いた | `--to` の綴り違いが分かるのが片付けのあとだと、**worktree と branch を失ったうえに Status も動かない。**戻す手段は無い。照合はボードを読むだけで1文字も書かないので、**dry-run でも通す** | `internal/abandon/abandon.go` の `verifyTargets`、`internal/tracker/adapter.go` の `VerifyKnownStates`、`docs/plans/continuo_design.md#3-37-5` | 95% |
| 63 | park の先が作業中の値なら止めること | 書き込む前に止める（終了コードは 1） | そこへ動かしても**継続監視は手を離さず、pane も閉じない。**pane の一覧がたまたま空を返した瞬間に、手を離していない issue の worktree を消す。`tracker.failure_state` が作業中の値でないことは設定の検証が保証しているが、**`--park` はその検証を通らない** | `internal/abandon/abandon.go` の `verifyTargets`、`internal/config/validate.go` の状態の集合の検査 | 95% |
| 64 | worktree が0件のときの `--to` の扱い | Status は動かさず、**動かしていないことを1行応答する** | 黙って終わると、指定した人間は「動いた」と受け取る。**代わりに Status だけを動かすことはしない。**worktree が無い理由は「もう片付けた」とは限らず**URL の打ち間違い**でもあり、そのときは無関係な issue の Status を動かすことになる | `internal/abandon/abandon.go` の `find`、`docs/plans/continuo_design.md#3-37-5` | 85% |
| 65 | 中断と時間切れの応答を分けたこと | `実行の中断` の応答を「中断されたことと残っている pane の ID」にした | 時間切れの文（「◯◯以内に閉じませんでした」）を中断でも出すと、**上限が短すぎたのかと読み違える。**止めた本人には、止まった理由をそのまま言う | `internal/abandon/abandon.go` の `waitPaneGone`（`Sleep` が偽を返す分岐） | 90% |
| 66 | 解釈できない `issue_url` の扱い | どれにも一致しない（文字列の一致で拾い直さない） | `IssueRef` は `ParseIssueURL` が作るので、渡された URL は必ず解釈できる形である。**それと同じ文字列なら相手も解釈できる**ので、拾い直す先が無い。推測で一致させると別の issue の worktree を消しかねない | `internal/abandon/issueurl.go` の `SameIssue` | 90% |
| 67 | 失うファイル数の見せ方 | 読み取りが上限で打ち切られたら「◯◯ファイル以上」と出す | `git status --porcelain` の読み取りは8KBで打ち切る（常駐プロセスに全量を載せないため）。**打ち切った行数をそのまま出すと、失う量を実際より少なく見せる。**見せた数より多く失うのが、いちばん困る誤りである | `internal/workspace/inspect.go` の `Inspect`、`internal/workspace/git.go` の `gitStatusPorcelain` | 95% |
| 68 | 手を離させたあとで止まったときの応答 | 「Status は◯◯のままです」を1行足す（元へは戻さない） | 「何も消していません」だけだと、**ボードも元のままだ**と読まれ、その issue が置き去りになる。**元へ戻さないのは、戻す先が `tracker.active_states` の値だからである**（戻した瞬間に、動いている継続監視がその issue を拾い直しうる） | `internal/abandon/abandon.go` の `reportParkLeftBehind`、`docs/plans/continuo_design.md#3-37` | 90% |
| 69 | その1行を消せたときに出さないこと | worktree を消せた実行では出さない | 消せたときは片付けたあとの Status の段が「動かした先」か「動かしていないこと」を応答する。**同じことを2箇所から言わない** | `internal/abandon/abandon.go` の `reportParkLeftBehind`（`removed` の分岐） | 85% |
| 70 | 計画に出す Status を park の**あと**の値にしたこと | 手を離させる書き込みが通ったら、持ち回っている値もその場で書き換える | ボードは1回しか読まない。書き換えないと、**これから消す worktree の issue が「まだ作業中」に見える** | `internal/abandon/abandon.go` の `park` と `readBoard` | 90% |
