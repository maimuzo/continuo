<!-- 目的: v0.1.14 に何が入り、何が入らなかったかと、その判断の根拠を残す -->

# v0.1.14 に入れたもの

**言いたいこと。**この版の中心は、**エージェントへ送る指示書（プロンプト）の作り直し**である。
**pull request を11本入れ、issue を6件閉じた。**
**破壊的変更が4つある。**中身は写さない。案内が正である。

---

## 1. 入れたもの（11本）

**対になる issue があるものと、無いものを分けて並べる。**
**無いものは、人間がチャットで出した規則の変更である。**

| 対になる issue | 入れた pull request |
| --- | --- |
| **issue #87**（1台で continuo を複数動かすことを、正式な形にする） | **PR #112**（1台で continuo を複数動かす（--id はロック1本だけを分ける。runtime.lock_file はキーごと消す）） |
| **issue #175**（エージェントが作業を終えた run が、何も記録されないまま消えることがある（コメントの確認も worktree の片付けも飛ぶ）） | **PR #182**（巡回が continuo 自身の書いた Status に反応しないようにする（後片付けが4つとも飛ぶ窓を塞ぎ、経路が2つあることを図にする）） |
| **issue #184**（エージェントが作業を終えても pull request を作らず、人間が代わりに作ることになっている） | **PR #185**（エージェントが作業の最後に PR を出すようにする（雛形から AI が決めた既定を外す）） |
| **issue #188**（エージェントへ送る指示書が長く、順序も強調も揃っていないため、初見で読み取れない） | **PR #203**（エージェントへ送る指示書を、人間が確定させた文面へ入れ替える（概要から順にセクションを分け、行頭の HTML コメントを送らない）） |
| **issue #194**（進捗コメントの間隔と重ね方が、人間の決定と逆に実装されている） | **PR #202**（エージェントが黙っていても、人間が別のコメントを1件書くだけで「進捗報告した」と数えられる）と **PR #206**（進捗報告を書かせる間隔を、設定から変えられるようにする（送る文面へ分に直して埋める）） |
| **issue #195**（「描画」をやめて「変数展開」に統一する決定が、コードと文書に反映されていない） | **PR #200**（「描画」をやめて「変数展開」に揃える 端末の表示範囲を指していた1件は文ごと書き直す） |

**対になる issue が無いもの4本。**どれも規則と設計の文書だけで、製品の挙動は変えない。

| 入れた pull request | 何を変えたか |
| --- | --- |
| **PR #181**（測る前に断定し、操作の結果を確かめず、試さずにできないと言う——1セッションで34回訂正した） | [.claude/rules/reporting.md](../../.claude/rules/reporting.md) |
| **PR #189**（worker へ渡すコンテキストが足りず、worker が既に否定された方向へ進む） | [.claude/skills/worker-briefing/SKILL.md](../../.claude/skills/worker-briefing/SKILL.md) |
| **PR #201**（巡回と hook がメモリを共有しているのかが、どの文書にも書かれていない） | [docs/spec/event_process_system.md](../spec/event_process_system.md) |
| **PR #205**（git worktree remove の禁止を外す（規則が片付けの手段と定めているものを、設定が塞いでいた）） | [.claude/settings.json](../../.claude/settings.json)。**配布物には入らない** |

---

## 2. 入れなかったもの（2件）

| 外した issue | なぜ外したか |
| --- | --- |
| **issue #183**（エージェントへ実際に送られる文面を、事前に確かめられない（変数が展開されない）） | **PR #203**（エージェントへ送る指示書を、人間が確定させた文面へ入れ替える（概要から順にセクションを分け、行頭の HTML コメントを送らない））**が指示書を作り直したので、確かめたい対象そのものが変わった** |
| **issue #187**（日本語を読まない利用者の手元でも、エージェントが日本語でコメントと commit メッセージを書く） | **同じ PR #203**（エージェントへ送る指示書を、人間が確定させた文面へ入れ替える（概要から順にセクションを分け、行頭の HTML コメントを送らない））**が全面的に書き直した文面へ言語の切り替えを差し込むと、確定させたばかりの並びを崩す** |

**どちらも issue は開いたままである。**次の版で判断する。

---

## 3. 破壊的変更（4つ）

**案内の正は [docs/upgrading.md:109-473](../upgrading.md#L109-L473) の「v0.1.13 から v0.1.14 へ」である。ここには写さない。**
**ここに置くのは、何が壊れるかの一覧だけである。**

| どの pull request が持ち込んだか | 4つのうちいくつか |
| --- | --- |
| **PR #112**（1台で continuo を複数動かす（--id はロック1本だけを分ける。runtime.lock_file はキーごと消す）） | 1つ |
| **PR #203**（エージェントへ送る指示書を、人間が確定させた文面へ入れ替える（概要から順にセクションを分け、行頭の HTML コメントを送らない）） | 2つ |
| **PR #206**（進捗報告を書かせる間隔を、設定から変えられるようにする（送る文面へ分に直して埋める）） | 1つ |

**何が壊れ、誰に効き、どう直すかは案内にある。**ここへ写すと、案内を直したときに片方が古くなる。

---

## 4. この版で決めたこと

**設計そのものを変えた判断だけを置く。**手順の話は置かない。

| 何を決めたか | なぜそう決めたか |
| --- | --- |
| **破壊的変更を許容する** | **人間の判断である**（2026-09-03）。「既存ユーザを気にして変な設計にするより、理想の形を目指せ」。**`runtime.lock_file` を受け取って捨てる案を捨て、キーごと消した** |
| **巡回が continuo 自身の書いた Status に反応しない門を置く**（[docs/plans/continuo_design.md:7674-7727](continuo_design.md#L7674-L7727) の 3-74c） | **後片付けが4つとも飛ぶ窓が実在した。**`finishRunClaimed` と `stopAndReleaseAsync` が権利を取り合い、**巡回が先に取ると、ログを1行も残さずに run が消える** |
| **`WORKFLOW.md` の本文にだけ、空になった見出しを落とす処理を当てる**（[docs/plans/continuo_design.md:9740-9783](continuo_design.md#L9740-L9783) の 5-3m） | **継ぎ合わせた全文に当てると、組み込みの `## 4-4` が消える。**そこは「ここから先は利用者が書いたもの」という唯一の区切りである |
| **進捗報告の間隔の下限を 60000（1分）にする**（[docs/plans/continuo_design.md:10181-10218](continuo_design.md#L10181-L10218) の 5-3n） | **送る文面へは分に直して埋める。**59999 までは全部「0分以上黙らない」になり、**エージェントは書けない指示を受け取る** |

---

## 5. 捨てたもの

**PR #112**（1台で continuo を複数動かす（--id はロック1本だけを分ける。runtime.lock_file はキーごと消す））**は、
目的の外にあるものを11件削った。**受け皿の issue ができたものと、未起票のまま人間へ渡したものに分かれる（[docs/plans/release_v0113.md:130-137](release_v0113.md#L130-L137)）。
**受け皿が無かった4件について、敵対的レビュワーが「入れるべきか」を判定し、4件とも「捨てる」と出した。**

| 捨てたもの | なぜ捨てたか |
| --- | --- |
| **立場の検査を雛形へ書き写す** | **同じ形を v0.1.10 で一度やり、失敗して組み込みへ移した。**戻す提案だった |
| **なりすましの開示を `SECURITY.md` へ足す** | **同じ開示が既に3箇所にある。**`SECURITY.md` は「直さないと決めた危険」を載せる場所なので、対策済みのものを載せると意味が逆になる |
| **取り消しの下見が `workspace.root` を作る** | **作るのは事実だが、実害の出る経路が1つも無い** |
| **前提の検査にロックの項目を足す** | **二重起動は既に3箇所が知らせる。**しかも `continuo doctor` は `--id` を受け取れないので、足すと「他に動いているものはありません」と嘘を返す |

**issue は作っていない。**判定が4件とも「いらない」だったためである。

**捨てた実装は commit `1943fa6` に在る。**branch は `wip/issue87-lock-file-priority-and-rucm`（`origin` にもある）。
**次の commit `8de3ee0` が、その実装を消して記録だけを残している。**取り出すなら親のほうを見る。

---

## 6. 出す前に必ずやること

**手順の正は [docs/releasing.md](../releasing.md) にある。ここには写さない。**
**この版でとくに効くのは次の4つで、書いてある場所が離れている。**

- **実機で issue を1件通す**（[docs/releasing.md:81-203](../releasing.md#L81-L203)）。検証用のカンバンを使う。**本番のカンバン（project #3）には触れない**
- **[docs/FAQ.md](../FAQ.md) と [docs/upgrading.md](../upgrading.md) の両方に、この版の内容が入っていること**
- **破壊的変更を `<!-- breaking:start -->` と `<!-- breaking:end -->` で囲む**（[docs/releasing.md:354-395](../releasing.md#L354-L395)）。
  **囲まないと、インストーラーは1件も警告しない。**この版は破壊的変更が4つあるので、いちばん効く
- **`--generate-notes` のまま放置しない**（[docs/releasing.md:449-471](../releasing.md#L449-L471)）。commit の一覧は利用者に読めない
