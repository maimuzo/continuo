<!-- 目的: 任意の HTTP ダッシュボードを実装するタスク -->

# 09. HTTP ダッシュボード（任意）

**言いたいこと。**run の状況を人間が見られるようにする。**任意である。**
**`server.port` が `null` なら起動しない。**

## 読むもの（設計）

| 節 | 何が書いてあるか |
| --- | --- |
| 5-2 | `server.port`（`null` なら起動しない） |
| 3-15 | **トークンの計上。**transcript の `.message.usage` を `requestId` で重複排除して集計する |
| 8-2 | 仕様 13.7 の任意拡張であること |

## 作るもの

| パッケージ | 何を |
| --- | --- |
| `internal/server` | HTTP のハンドラ。実行中の run の一覧と、それぞれの状態 |

## 受け入れの基準

- [ ] **`server.port` が `null` なら listen しない**
- [ ] 実行中の run の一覧を出せる（issue / Status / turn 数 / 最後に hook を受けた時刻）
- [ ] **トークンの集計を出せる**（`requestId` で重複排除する）
- [ ] **設定を読み直しても `server.port` は反映しない**（自前のリソースを掴んでいる。3-24）

## 実装の記録

（着手したら書く）
