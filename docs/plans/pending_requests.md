# 未処理のリクエスト一覧

**言いたいこと。**人間から受けた指示のうち、まだ終わっていないものを1箇所に集める。
**終わったら消す。**履歴は残さない。
**「人間の返答待ち」と「私が動かせるもの」を分ける。**混ぜると、待っていないものまで止まる。

---

## 0. 2026-08-28 のセッションで出た指示（未対応のもの）

**このセッションは長い。**指示が流れて消えないよう、ここへ集める。**終わったら消す。**

### 人間の返答を待っているもの

| 何 | 何を待っているか | 材料は揃っているか |
| --- | --- | --- |
| **外部コメントへ立場の札を付ける形** | 3案（プロンプト / `PreToolUse` / `PostToolUse`）のどれか | **揃っている。**比較資料と推奨（`PostToolUse`）を提示済み |
| **Bash の sandbox を入れるか** | 入れる / 入れない | **揃っている。**推奨（入れない）を提示済み。`gh` が動かないことは実測済み |
| **`preflight` が HEAD の branch 名を見るのをやめるか** | やめる / やめない | **説明がまだ不十分。**前提から書き直す |
| **印を騙る経路を塞ぐ実装への着手** | 着手してよいか | 揃っている（30〜50行） |
| **continuo の実行ファイルを守る実装への着手** | 着手してよいか | 揃っている |
| **push の求め方**（issue #64 の「決めること」4件） | 4つ。**push できないときに `blocked` を出させるか `working` のままにさせるか** / **commit するものが無いときも push を求めるか** / **`review` のときに PR まで作らせるか** / **`working` でも毎 turn の push を求めるか** | **揃っている。**4件とも選択肢と、決めるまでの振る舞いを設計 5-3b に整理済み |
| **issue #64 の受け入れ条件をどこまで求めるか** | 2つ。**GitHub へ書き込んでよいセッションで [docs/test_environment.md](../test_environment.md) の環境へ1度 push を通す**か、**ローカルの bare repository での確認で足りるとする**か | **揃っている。**git の側の記録は [docs/evidence/push_u_origin_head.md](../evidence/push_u_origin_head.md) にある。残っているのは認証と branch protection だけ |

### AI だけで進められるもの

| 何 | 状態 |
| --- | --- |
| **8点の説明**（サブエージェントの権限モード / 必須ラベル / LLM 判定のコスト / 判定へのインジェクション / ネットワークの調整 / 読み取りの範囲 / sandbox runtime / `preflight`） | workflow が実行中 |
| **「丁寧に説明しているか」を機械で見る代替案** | **辞書案は却下された。**代替が未提示 |
| **#65 の修正のうち、決定待ちでないもの**（番兵エラーの新設・文言・FAQ の項目） | 設計は出ている。**プランファイルへの記載が未了** |
| **起動時の掃除が身元ファイルを見る修正** | 設計は出ている。**プランファイルへの記載が未了** |
| **`git push -u origin HEAD` を GitHub 相手に1度通す**（issue #64 の受け入れ条件） | **git の側は済み**（[docs/evidence/push_u_origin_head.md](../evidence/push_u_origin_head.md)。remote はローカルの bare repository）。**残るのは認証と branch protection だけで、[docs/test_environment.md](../test_environment.md) の環境で issue を1件通せば確かめられる。**ただし**この branch のセッションは「GitHub へ書き込まない」制約の下にあるので踏めない。GitHub へ書き込んでよいセッションが要る** |
| **飛ばしたことを issue へ1回コメントする** | 設計は出ている。**プランファイルへの記載が未了** |
| **レートリミットの上限（issue #36 へ追記済み）** | 実装の設計が未着手 |

### 済んだもの（記録のため。次のセッションで消す）

| 何 | 結果 |
| --- | --- |
| PR #89（ロックの置き場所と画面の言語の既定を決める設計の記録） | マージ済み |
| 英語版の資源を作る issue（#90） | 起票済み |
| レートリミットの上限を issue #36 へ追記 | 追記済み |
| 返答の検査の hook | `.claude/hooks/check-reply-clarity.py`。区切り線の検査へ差し替え済み |
| sandbox の実機検証 | **`gh` は動かない。**全ドメイン許可でも同じ |

---

## A. 人間の返答を待っているもの

**私からは動かせない。**答えが来た時点で着手する。

| 何 | 何を聞いているか | いつ聞いたか |
| --- | --- | --- |
| **プラグインの branch 2本** | `rucm-abort-resume-criteria` と `rucm-same-branch-for-spec-and-impl` を push してよいか | 2回聞いて未回答 |
| **doctor のテストの印のずれ** | `TestDoctor_ghが未ログインなら足りないと出しログインの手順を出す` の印が、テストの中身と噛み合っていない。直すか放置か | 1回聞いて未回答 |
| **#36 の全体の同時実行数** | 「同時に動かしすぎると困る」の中身は何か（枠 / レビューの追いつかなさ / 同じリポジトリの衝突） | 直近 |
| **#36 の担当者の有効期限** | 「生存を書き続ける」か「最後に動いた時刻を見る」か | 直近 |
| **#36 の枠が尽きたときの手順** | 提示した5段の手順でよいか | 直近 |
| **#37** | 編集履歴の削除の自動化。**雛形が許す道具に Claude in Chrome が無く、エージェントに渡せない。**どうするか | 報告済み・未回答 |
| **PR #49 / #50** | continuo が作った。**人間のレビュー待ち** | — |
| **#38 の doctor 側** | 「古い設定のまま新しい実行ファイルが入っている人に、起動して落ちる前に伝える」を `continuo doctor` にも入れるか。**入れないなら #38 は閉じられない**（受け入れ条件が1つ残る） | 直近 |
| **#38 の RUCM の追随** | 「破壊的変更を警告する」枝を `docs/spec/usecases/particular_case/continuo を入れる.rucm.md` へいま足すか、次の作り直しでまとめるか。**いま作り直すと既存11本のパス ID が振り直されうる** | 直近 |

---

## B. 指示を受けたが、まだ終わっていないもの

| 順 | 何 | 状態 |
| --- | --- | --- |
| — | **返答の検査をプラグインへ移す** | **まだやらない。**[.claude/hooks/check-reply-clarity.py](../../.claude/hooks/check-reply-clarity.py) でしばらく使い、良し悪しが分かってから `maimuzo-chat-response` へ移す（2026-08-28、人間の指示） |
| 1 | **RUCM 作り直し 3本目**（`issue を1件処理する`） | **実行中** |
| 2 | **RUCM 作り直し 4本目**（`着手を取り消す`） | 待ち |
| 3 | **`fix/issue-43-rucm-redo` を push して PR を作る** | 作り直しの完了後 |
| 4 | **#46 のファイル書き込みの修正**（下記の4箇所） | **着手できる** |
| 5 | **プラグイン側に `ABORT` / `RESUME STEP` の基準を反映する** | **着手できる** |
| 6 | **#44 の既存97本を全件見直す** | 作り直しの完了後 |
| 7 | **#45 の構造の調整**（基本フローの `IF` を畳む） | 作り直しの完了後 |
| 8 | **#40 の実装**（プロンプトの切り出し） | 決定済み・未着手 |
| 9 | **#38 の実装**（リリースの文面の印と、インストーラーの警告） | 決定済み・未着手 |

### 4 の内訳（#46 のファイル書き込み）

| 場所 | やること |
| --- | --- |
| `internal/scaffold/scaffold.go` の `WriteTemplateWithValues` | **差し替え方式へ。**`syscall.O_NOFOLLOW` の守りは保つ |
| `internal/orchestrator/settings.go` | **差し替え方式へ** |
| `internal/lock/lock.go` | **例外の理由をコメントに書く**（差し替えるとロックが切れる） |
| `internal/workspace/identity.go` の `.git/info/exclude` への追記 | **例外の理由をコメントに書く**（追記のみ。全置換にすると他が書いた行を消す） |

そのあと `設定ファイルを作る.rucm.md` の `POSTCONDITION` を直し、CFG とテストの印を貼り直す。

---

## C. continuo が自分で進めているもの

| # | 状態 |
| --- | --- |
| #48 | **完了**（PR #49・`In Review`） |
| #39 | **完了**（PR #50・`In Review`） |
| #33 | **処理中** |
| #35 | **処理中** |

---

## D. 後始末

| 何 | いつ |
| --- | --- |
| `~/continuo_rucm_backup/` と、置き去りの4本の branch | **RUCM の作り直しが全部終わったら消す**（指示どおり） |
| `~/continuo_rucm_backup_20260826/` | **失敗した1回目。消そうとして auto mode に断られた。**許可が要る |
| `CLAUDE.md` の変更（ファイル書き換えの方針） | **未 commit。**worker が動いている間は commit しない |
